# 第 3 步：完成苹果演示和提醒

状态：已完成。前提：第 2 步完成（434878c）。完成版本标记：`mvp-v2-step-03`；准确提交使用 `git log -1 mvp-v2-step-03` 查询。

实施选择：iOS 18 风格参考；后续鸿蒙以 HarmonyOS 5、安卓以 Android 15 / Material 3 为参考。壁纸、药安心及辅助图标全部为本地自制 SVG/CSS，无外部素材请求。按 apple-design、animate 指导材质、空间关系、即时反馈、200–280ms 原生 transition/WAAPI 与减少动态效果；不引入依赖。

消息协议：`channel: yaoanxin-demo`、`version: 1`、唯一 `requestId`、白名单 `type`、受校验 `payload`；双方严格校验同源 origin 和唯一 parent/iframe source。READY→THEME→ACK，主题确认后显示。应用拥有家庭存储和通知资格；外层只展示摘要、发送有限动作，不操作内层 DOM 或业务存储。

## 做完后能体验什么

打开独立比赛入口，看到苹果风格手机桌面。收到模拟提醒后，可以进入应用打卡，再回桌面，已填内容和记录仍在。

## 本步功能

- [x] 新增 `demo.html`，直接嵌入原应用，共用一份记录；原 `index.html` 仍能单独打开。
- [x] 完成默认 iOS 桌面、壁纸、图标、Dock、状态栏、通知横幅和应用内部风格；开关应用、按压和面板有实际动效。
- [x] 实现到点提醒长辈（N1）、超时通知子女（N2）、子女主动提醒长辈（N3）。全部使用演示账号和模拟状态。
- [x] 外部控制区可调演示时刻、准备场景和查看接收效果；无需等待现实时间，不请求真实系统通知权限。
- [x] 完成聚焦打卡：按资格自动显示，可关闭，逐药处理，可三选一记录或延后；关闭不算打卡、不立刻重弹。
- [x] 走通横幅→提醒页→关闭→对应时段列表→本人打卡；两端看到同一结果。
- [x] 延后保护期间抑制相应提醒；同任务家属共用 5 分钟主动提醒冷却；重复触发不重复通知，重播外观不新增记录。
- [x] 旧通知再次打开时检查最新任务和权限；已记录、取消或解绑后不恢复旧操作。
- [x] 返回桌面、打开通知时保留表单输入；完成主题就绪和消息校验，重复或错误来源消息不修改记录。
- [x] 提供关闭通知、隐私预览、模拟离线／失败／待同步和跨日延迟同步场景；明确全部为模拟。
- [x] 恢复初始数据时同时重置演示时间、通知、冷却、延后和关闭状态。

鸿蒙、安卓完整外观归第 5 步；聊天草稿与通知联动在第 4 步补齐。

## 怎么检查

1. 子女建计划，切长辈回桌面，触发到点提醒，打开并关闭提醒页，从列表完成打卡，再切子女查看。
2. 演示超时通知子女和子女主动提醒长辈；在延后期、冷却期和已记录时尝试提醒，显示正确原因。
3. 填一半表单，回桌面并点通知，处理后回来继续；重播旧横幅不重复打卡或丢输入。
4. 模拟 23:55 已确认、次日才同步，区别于次日首次补记；重复同步不重复记录。
5. 检查原应用入口、演示入口、手机／电脑和两种字号，以及关闭通知、错误消息和恢复初始数据。

## 对应需求与验收

依据 v2 第 9、11.4–11.6、12.4 章。A21、A29–A33、A37、A38、A44、A47、A48、A52–A56、A58、A61–A63、A65–A70、A83。其中三主题组合归第 5 步，聊天输入保护归第 4 步。实现视觉前按 v2 第 11.6 节使用适用设计与动画技能，记录参考版本和素材来源。

## 完成时填写

- 实际完成：上述 iOS 演示、通知协调器、聚焦队列、来源严格校验、草稿隔离及模拟同步功能。N2 按接收侧可见状态评估，离线本机确认不会伪装成远端已接收；同步只补接收信息。旧横幅依据最新状态更新摘要并停止已结束操作。
- 检查结果：`tests/step03.browser.js` 59 项，`tests/step03.extra.browser.js` 22 项，均 0 脚本错误；`tests/rules.test.cjs` 13 组及 `tests/notifications.test.cjs` 9 组通过。独立 Chrome 执行更新后的 `tests/step02.browser.js` 75 项回归通过，包含原表单、BMI、打卡撤销更正、必填校验、日历及真实离开草稿弹窗。LSP 无 error；未执行实体 iPhone／Safari／真实系统通知检查，本步不声称已接入这些能力。
- 证据：`output/playwright/step-03/result.json`、`extra-result.json`、`regression-result.json`；360/1280 与标准/大字号截图、`ios-desktop-overview.png`、`ios-app-overview.png`、`motion-midframe.png`。手动查看桌面、应用、横幅、聚焦卡及动画中间帧，补测关闭反向展开连续性与减少动态效果。复核发现与修复见[动效复核记录](../plans/001-step03-motion-review.md)。
- 验收映射：A21/A29–A33/A37/A38/A44/A47/A48/A52–A56/A58/A61–A63/A65–A70/A83 的 iOS、提醒、消息与草稿基础部分，具体用例在上述 JSON 的 checks 中；三主题与聊天组合仍归第4–5步复查，不冒充已完成全项。
- 修复记录：恢复确认优先于晚到的打开通知请求；恢复后马上保存有效初始存档并验证刷新；退出动画副本去除交互属性；短视口手机高度与底部导航修正。CLI 的 beforeunload 管理层无法完成旧回归时，改为隔离 Node Playwright harness；失败产物不计通过。
- 视觉参考和素材：iOS 18；壁纸、应用及桌面图标均本地自制 SVG/CSS。使用 apple-design、emil-design-eng、animate，以及 find-animation-opportunities、improve-animations、review-animations 的复核准则。未采用截图冒充界面、外部字体或第三方素材。
- 复现：先在根目录 `python3 -m http.server 8765`，随后 `node tests/rules.test.cjs`、`node tests/notifications.test.cjs`；浏览器用已安装的 Playwright CLI `run-code --filename tests/step03.browser.js` / `tests/step03.extra.browser.js`，或 `node tests/browser-runner.cjs tests/step02.browser.js output/playwright/step-03/regression-result.json`（现有 Chrome、新隔离 context）。
- 本地版本：本次代码和检查记录同次提交；`mvp-v2-step-03` 指向检查完成版本，无远程推送。
