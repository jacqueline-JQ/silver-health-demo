# 药安心 MVP v2.1 优化设计与确认文档

> **状态：已并入主文档，不再作为实现基准。**  
> **版本**：v2.1（过渡稿）  
> **日期**：2026-09-13  
> **定位**：当时用于记录用户确认项。这些决议已写入 `药安心_MVP文档_v2.md`。后续实现以 v2 主文档为准。

---

## 目录
1. [已确认与调整的核心决议（Decision Log）](#1-已确认与调整的核心决议decision-log)
2. [时段区间、提醒时间与逾期判定规则明确](#2-时段区间提醒时间与逾期判定规则明确)
3. [长辈端“聚焦打卡全屏弹窗”设计规范](#3-长辈端聚焦打卡全屏弹窗设计规范)
4. [顶部真实手机通知横幅（Push Banner）与三端开关](#4-顶部真实手机通知横幅push-banner与三端开关)
5. [吉祥物小人形象升级：智能引导与快捷提示](#5-吉祥物小人形象升级智能引导与快捷提示)
6. [去 PRO 后的功能平铺设计](#6-去-pro-后的功能平铺设计)
7. [技术实现与代码改动影响面清单](#7-技术实现与代码改动影响面清单)

---

## 1. 已确认与调整的核心决议（Decision Log）

| 决议项 | 变更类型 | 决议内容 | 影响说明 |
| :--- | :--- | :--- | :--- |
| **C1 & C2 (奖励机制)** | **废弃** | **完全取消卡通存钱罐及红包奖励系统**。 | 移除所有零钱结算、红包掉落动效、充值/提现表单及逻辑。 |
| **C3 (权限红线)** | **维持** | **子女端绝对不可代长辈确认“已服用”/“打卡”**。 | 子女端仅可建计划、发模拟提醒、看记录；打卡与延后仅限长辈端完成。 |
| **C4 (商业化分级)** | **变更** | **取消 PRO 会员等级划分，所有功能平铺开放**。 | AI 语音录入、拍照识药、子女端辅助功能对所有用户完全可用，不展示收费/升级入口。 |
| **时段与逾期规则** | **澄清** | 严格按照四大自然时段区间判定逾期，滚轮时间仅作提醒触发点。 | 详见第 2 节。 |
| **长辈聚焦打卡 UI** | **新增** | 打开应用有待服药或从通知进入时，直显强聚焦打卡界面。 | 详见第 3 节。 |
| **系统通知横幅** | **新增** | 顶部提供 iOS / 鸿蒙 / 安卓三风格通知横幅，点击直通打卡 UI。 | 详见第 4 节。 |
| **吉祥物定位转换** | **重构** | 小人不再作为存钱罐载体，转为“健康伴侣/功能引导员”。 | 详见第 5 节。 |

---

## 2. 时段区间、提醒时间与逾期判定规则明确

### 2.1 四大固定自然时段
系统中所有药物打卡按以下 4 个自然时段归类，每个时段具有明确的起止边界：

| 时段名称 | 时间范围 | 默认推荐提醒点 | 逾期触发临界点（未延后时） |
| :--- | :--- | :--- | :--- |
| **早餐 / 晨间** | `05:00 ~ 11:00` | `08:00` | 当前时间超过 `11:00` 且未打卡 |
| **午餐 / 午间** | `11:00 ~ 16:00` | `12:00` | 当前时间超过 `16:00` 且未打卡 |
| **晚餐 / 晚间** | `16:00 ~ 20:00` | `18:00` | 当前时间超过 `20:00` 且未打卡 |
| **睡前 / 夜间** | `20:00 ~ 24:00` (次日0点) | `21:30` | 当前时间超过 `24:00` 且未打卡 |

### 2.2 时间滚轮的作用 vs 逾期判定机制
- **时间滚轮的作用**：用于设定**“何时向长辈发出系统通知/闹钟提醒”**。例如午餐时段（11:00~16:00）的降糖药，滚轮设定为 `12:30`，则系统在 12:30 弹出通知横幅提醒老人。
- **逾期判定机制**：
  1. **常规情况**：只要当前系统时间处于该时段内（例如 14:00 仍处于午餐时段），药物状态为“待服用”，**不判定为逾期**；只有当时间迈出该时段（例如到了 16:01），该药才变为“逾期未服”。
  2. **延后提醒的豁免特权**：
     - 若老人在 15:50（午餐时段尾声）点击了“延后30分钟”，提醒时间推迟到 16:20。
     - **豁免规则**：尽管 16:20 已进入晚餐时段（>16:00），但由于处于延后有效保护期内，**该药绝不判定为逾期，也不显示未打卡红点**。
     - **次数限制**：每种药在同一时段内**仅限延后 1 次**。延后保护期结束后（如 16:20 后仍未服用），若已跨时段则立即结算为逾期。

---

## 3. 长辈端“聚焦打卡全屏弹窗”设计规范

当老人打开长辈端首页且存在当前待服药物，或通过点击顶部系统通知/锁屏横幅进入时，**自动弹出/直接呈现强聚焦的极简打卡卡片**。

```
+-------------------------------------------------------+
|                       [ 闹钟动画 ⏰ ]                 |
|                                                       |
|                    该吃药啦！ (28px 粗体)              |
|              08:00 · 饭后服用 (16px 灰色副标)          |
|                                                       |
|   +-----------------------------------------------+   |
|   |                                               |   |
|   |         💊  降压药 (络活喜) (32px 粗黑)        |   |
|   |                                               |   |
|   |           1 片 · 饭后温水送服 (20px)           |   |
|   +-----------------------------------------------+   |
|                                                       |
|   * 每种药仅限延后1次，延后不计逾期 (14px 红色中等字)   |
|                                                       |
|   +-----------------------------------------------+   |
|   |               [  ✓  已 服 用  ]               |   |  <- 60px高 翠绿大按钮
|   +-----------------------------------------------+   |
|                                                       |
|   +-----------------------+ +---------------------+   |
|   |     延后 5 分钟       | |     延后 30 分钟    |   |  <- 48px高 暖橙色次级按钮
|   +-----------------------+ +---------------------+   |
|                                                       |
|   +-----------------------------------------------+   |
|   |               本次无需服用 (浅灰文本链)         |   |
|   +-----------------------------------------------+   |
+-------------------------------------------------------+
```

### 3.1 核心元素与适老化布局
1. **顶部视觉锚点**：精致的闹钟提醒 SVG 图标及“该吃药啦！”大标题。
2. **中心药物大字看板**：
   - 药品名称：`30px~32px` 特大字号，深色高对比度居中。
   - 服用剂量与要求：`18px~20px`，“X 片/X 包 · 饭前/饭后服用”。
3. **关键约束文案**：
   - 居中展示红色提示语：`* 本次服药仅有 1 次延后提醒机会`（若已使用延后，下方延后按钮置灰禁用，文案变为 `* 已延后过 1 次，请尽快服用`）。
4. **动作按钮梯队**：
   - **第一主行动（已服用）**：满宽绿色大按钮（高度 `56px~60px`，字号 `22px` 粗体），点击后播放打卡成功反馈并关闭弹窗，首页对应药卡打勾。
   - **第二行动组（延后提醒）**：左右并排两个按钮——`延后 5 分钟`、`延后 30 分钟`。点击后设置定时器，关闭弹窗，在指定时间再次弹出顶部通知与该卡片。
   - **底线行动（本次无需服用）**：底部居中轻量灰色按钮，点击需弹出二次极简确认（“确认这顿不用吃这味药吗？”），确认后状态更新为“无需服用”。

---

## 4. 顶部真实手机通知横幅（Push Banner）与三端开关

### 4.1 顶部系统通知交互
在原型顶部（或演示控制栏）提供系统通知模拟开关与触发器：
- **通知形态**：高度仿真的手机顶部下拉 Push Banner，包含应用图标、应用名“药安心”、时间标签“刚刚”、标题与详细药名及剂量。
- **三系统样式切换**：
  - **iOS 风格**：毛玻璃圆角卡片，SF Pro 字体质感，右上角时间，微阴影。
  - **鸿蒙 (HarmonyOS) 风格**：微拟物胶囊通知条，纯白高亮卡片，圆角特征。
  - **安卓 (Android / Material You) 风格**：左侧单色圆圈图标，大标题+展开式二级内容。
- **点击响应**：无论当前处于长辈端何种页面，点击通知横幅立刻唤起第 3 节的**聚焦打卡全屏弹窗**。

---

## 5. 吉祥物小人形象升级：智能引导与快捷提示

取消存钱罐后，保留 4 个生动的卡通形象（如智能小管家、小红花天使、暖心小鹿、康康小寿星）：

### 5.1 交互与修改
- 长辈端与子女端首页均显示当前选中的吉祥物形象。
- **点击小人**：弹出形象选择与换装卡片，支持自由切换 4 种小人形象。

### 5.2 智能助手引导与便签气泡（Proactive Tips）
小人头顶常驻或周期性轮播对话气泡，轻触小人时弹出“管家锦囊”，主动提示老人如何使用 AI 语音：
1. **打卡引导**：“爷爷奶奶，点右下角话筒说‘我把降压药吃了’，我就帮您记好啦！”
2. **加药引导**：“买新药了？对我说‘帮我加上阿司匹林，每天早上一片’就行！”
3. **健康数据引导**：“刚量了血压？直接告诉我‘高压125，低压82’，自动帮您画走势图哦！”
4. **点击气泡直达**：点击气泡中的推荐动作，可直接唤起 AI 语音助手对话面板并填充推荐语。

---

## 6. 去 PRO 后的功能平铺设计

彻底去除商业化 PRO 标识与付费墙拦截：
1. **AI 拍照识药**：长辈端与子女端添加药物时，均可直接点击“拍照/扫码识药”体验模拟识别。
2. **复杂方案语音解析**：子女端支持直接粘贴或语音输入整段医嘱（如“每天早中晚各吃一片二甲双胍”），AI 自动拆解为 3 个时段计划。
3. **多长辈管理与健康报告导出**：完全无限制开放。

---

## 7. 技术实现与代码改动影响面清单

| 文件 | 主要修改内容 |
| :--- | :--- |
| `index.html` | 1. 移除存钱罐/提现相关的 HTML 结构与 SVG 图标。<br>2. 新增顶部真实模拟通知横幅组件（含 iOS/鸿蒙/安卓 样式切换器）。<br>3. 新增聚焦打卡卡片全屏 Modal 骨架。 |
| `styles.css` | 1. 编写 3 种操作系统（iOS/HarmonyOS/Android）的 Push Banner 仿真样式与滑入动画。<br>2. 编写聚焦打卡弹窗的特大字号排版、闹钟摇摆动效与按钮层次。<br>3. 编写吉祥物智能气泡（Speech Bubble）与形象切换弹窗样式。<br>4. 清理存钱罐相关 CSS。 |
| `seed-data.js` | 1. 移除 `piggyBank`、`rewardBalance`、`transactions` 等字段。<br>2. 长辈 profile 增加 `mascot: "robot"`（默认小人形象）及 `activeSnooze: {}`（延后记录）。 |
| `app.js` | 1. **时段与逾期引擎**：严格绑定 05-11, 11-16, 16-20, 20-24 自然时段，实现单药 1 次延后豁免逾期逻辑。<br>2. **打卡聚焦窗控制器**：实现入站/点击通知自动唤起聚焦打卡、延后 5/30 分钟定时器。<br>3. **模拟通知系统**：实现可切换操作系统的顶部通知横幅触发器。<br>4. **吉祥物组件**：实现形象切换与点击引导跳转 AI 语音助手。<br>5. **清理逻辑**：完全移除存钱罐结算逻辑与所有 PRO 权限拦截。 |

---

*文档编写完成，待确认后即刻开始代码实现。*
