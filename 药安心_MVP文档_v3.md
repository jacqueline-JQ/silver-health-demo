# 药安心：双端家庭协同 MVP 文档 v3

> **文档状态：**v3 产品需求基线已确认；实际实施优先级与本次 Demo 取舍待下一轮讨论确认。
>
> **文档日期：**2026-09-11。
>
> **文档性质：**本文记录药安心 v3 的目标行为、已确认规则、数据边界与待执行验收。写入本文不表示相关代码已经实现、上线或通过验证。
>
> **当前代码状态：**现有原型仍是 MVP v2 实现。本轮只新增本文，不修改应用源码。

## 0. 版本目标与阅读约定

### 0.1 v3 目标

MVP v3 在现有“子女协助建计划 → 长辈本人打卡 → 双端查看同一记录 → 子女模拟提醒”闭环上，优先解决当前 Demo 的不稳定交互和错误解析，再加入本轮已确认的新规则与扩展需求。

本版围绕四个目标：

1. **先稳定：**账号切换、提醒关闭、助手解析和计划表布局不再出现确定性错误。
2. **再简化：**长辈进入首页后直接看见当前药名、计划片数和核心打卡按钮，减少非关键入口与滚动。
3. **补规则：**将用药提醒与双端预警分开，统一“未服用”的可记录时机，保留自然时段、延后保护、权限与历史真实性。
4. **留扩展：**完整记录血糖、重新启用、“我的”页模拟能力等已确认需求，但是否进入本次限时 Demo，须在本文形成后另行排期。

### 0.2 需求标记

| 标记 | 含义 | 使用方式 |
| --- | --- | --- |
| 已确认需求 | 用户已确认的目标行为、文案或信息结构 | 不得因实现复杂而静默删除 |
| 已确认规则 | 已确认的数据、状态、权限或时间口径 | 所有入口必须一致执行 |
| 已复现缺陷 | 已通过代码检查、截图或浏览器交互确认 | 修复后必须增加针对性回归 |
| 实现约束 | 为兼容、真实性、安全或可访问性补充的工程边界 | 不冒充用户原话，但实现时必须遵守 |
| 优先级建议 | 基于当前时间压力给出的实施建议 | 尚未视为已批准排期，见第 15 章 |
| 后续真实服务 | 需要真实账号、设备、推送、平台或后端的能力 | 不计入当前静态 Demo 完成度 |

### 0.3 与现有文件的关系

| 文件或范围 | v3 的处理 |
| --- | --- |
| [《药安心 MVP 文档 v2》](药安心_MVP文档_v2.md) | 保留为 v2 历史基线；本文明确替换的规则以 v3 为准，未替换的家庭权限、任务快照、延后、星星、模拟边界等继续适用 |
| [MVP v2 复审报告](output/playwright/mvp-v2-recheck/MVP-v2复审报告.md) | 作为当前缺陷与界面证据来源；报告中的建议不等于已经修复 |
| `药安心_MVP文档_v1.md`、`药安心_MVP文档_v2.1_优化设计与确认.md` | 仅作历史记录，不作为 v3 实现依据 |
| `药安心_PRD.md` | 远期产品方向参考；医生端、诊断、处方建议、真实硬件和真实跨设备服务不因本文自动进入当前 Demo |
| `AGENTS.md` | 当前工程与安全边界继续生效；开始开发 v3 前再更新其中的当前实施基准与验证记录 |
| 当前 HTML 原型 | 继续使用原生 HTML/CSS/JavaScript、本地演示账号和本地存储，不引入框架或构建工具 |

### 0.4 v3 明确替换的 v2 条款

| v2 规则或呈现 | v3 替换结果 |
| --- | --- |
| 长辈首页保留两张快捷添加卡 | 从长辈首页移除；添加计划与身体数据入口分别保留在“用药信息”和“身体数据”页 |
| 普通、逾期和历史任务均可直接选择“未服用” | “未服用”仅在原自然时段截止后开放；普通待打卡阶段不显示也不允许通过助手绕过 |
| 关闭聚焦提醒后进入对应日期／时段列表 | 关闭后返回长辈“服药打卡”首页；查看对应任务改为独立的显式动作 |
| 计划时段使用四列表和餐前／餐后两段控件 | 所有尺寸均改为每时段一个信息分块，包含五组信息；餐时改为餐前／随餐／餐后三段 |
| 晚餐用药提醒默认 18:30 | 改为 17:30 |
| N2 只在自然时段截止后处理未记录 | 新增可配置的双端提前预警；提前预警不改变待打卡状态，自然截止仍独立决定“未按时打卡” |
| 计划量只保存数值和单位 | 增加可选单次剂量 mg、可选药品规格 mg、必填实际片／包数和必填单位 |
| 子女看板初版顺序为今日用药先于日历 | 日历移到档案信息之后的靠前位置，统计区与日历均压缩 |
| 双端首页助手使用聊天气泡图标 | 双端统一改为话筒图标，点击／长按语义和有限模拟能力不变 |
| 当前范围不显示健康报告入口 | “我的”页恢复“导出健康报告”占位行，但本版不生成真实报告 |

未被本表替换的 v2 规则继续有效，尤其包括：固定演示日、四自然时段边界、本人声明权限、5 秒撤销、每任务一次延后、延后保护、严格星星资格、任务快照、计划向后生效、子女不能代打、单一业务存储和模拟服务边界。

### 0.5 本轮已确认决策

| 编号 | 已确认决定 |
| --- | --- |
| C01 | “未服用”是业务规则限制，不只是隐藏按钮；仅在自然时段截止后允许记录，手动、详情和助手一致 |
| C02 | 重新启用不恢复原计划为 active，而是创建与旧计划关联的新计划，旧计划、停用日期和历史保留 |
| C03 | 睡前用药提醒默认 21:00，双端提前预警默认 22:00，不在同一分钟发送两类提醒 |
| C04 | 血糖采用七个可调整的固定默认时刻；空白行不保存为测量记录 |
| C05 | 提前预警发送后、自然时段尚未结束时，页面仍显示“待打卡” |
| C06 | 打卡记录页保持简洁列表；点行进入详情后再补记或更正 |
| C07 | 恢复初始演示数据后默认处于已模拟登录状态 |
| C08 | 用药时段在所有尺寸均使用分块布局；“五列”只表示五组必备信息，不再要求视觉上的五条窄列 |

## 1. v3 需求追踪总表

以下 15 项均为已确认的产品目标；“是否进入本次限时 Demo”尚未等同于确认实施优先级。

| 编号 | 需求摘要 | 主体章节 | 验收 |
| --- | --- | --- | --- |
| R01 | 修复所有账号切换入口恢复旧页面、自动打开旧草稿弹窗的问题 | 3 | V3-A01～A03、A40 |
| R02 | 删除长辈首页快捷添加卡并压缩顶部 | 4 | V3-A04～A05 |
| R03 | 普通任务移除“未服用”，仅逾期任务开放 | 5 | V3-A08～A10 |
| R04 | 首屏主打卡按钮重排：大号已服用，下方本次无需与两种延后 | 4 | V3-A06 |
| R05 | 药名与计划片／包数同等突出 | 4 | V3-A05～A07 |
| R06 | 关闭“该吃药啦”返回首页 | 3、4 | V3-A03、A07 |
| R07 | 已停用计划显示停用日期并支持关联式重新启用 | 7 | V3-A19～A21 |
| R08 | 打卡记录改为按时段排序的简洁清单 | 7 | V3-A16～A18 |
| R09 | 更正入口改为低强调、填色描边的“更正”按钮 | 7 | V3-A18 |
| R10 | 新增血糖七时段填写、提醒、协助录入、助手录入、历史与趋势 | 9 | V3-A25～A29 |
| R11 | 重做“我的”页模拟身份区与功能行 | 10 | V3-A30～A33 |
| R12 | 双端首页助手改为话筒图标 | 8、11 | V3-A24 |
| R13 | 子女首页日历前置，压缩统计区和日历 | 8 | V3-A22～A23 |
| R14 | 时段分块增加双端预警和随餐选项，修改默认时间并修复挤压 | 6 | V3-A11～A15 |
| R15 | 计划增加 mg 剂量、药品规格和实际片／包数换算 | 6 | V3-A11～A13 |

## 2. 当前缺陷基线

### 2.1 必须修复的确定问题

| 编号 | 类型 | 当前表现 | v3 目标 |
| --- | --- | --- | --- |
| B01 | 违反 v2 的解析缺陷 | “每次 1 片，不是 2 片”被解析为 2 片 | 先识别否定和改口关系；不能可靠判断时阻止核对并要求确认 |
| B02 | 违反 v2 的草稿缺陷 | 当前计划草稿中说“早餐提醒改为 09:00”被当成查询，草稿未更新 | 计划上下文中的字段修改优先于查询意图，并写回同一草稿 |
| B03 | 违反 v2 的 UI 缺陷 | 餐前／餐后控件纵向堆叠 | 改为餐前／随餐／餐后三段横向控件 |
| B04 | 已复现交互缺陷 | 切换账号恢复该账号旧页面，并自动打开旧聊天或身体数据弹窗 | 所有入口切换后先进入对应首页；草稿只允许主动恢复 |
| B05 | 已复现且被 v3 改写的导航 | 关闭聚焦提醒会主动定位到“用药信息 → 打卡记录” | 关闭回长辈首页；显式“查看对应任务”才导航 |
| B06 | 用户截图确认的布局缺陷 | 加药表在窄屏严重向右挤压 | 所有尺寸改为时段分块，并完成标准／大字号的窄屏验证 |

账号异常已在首页顶部入口和“我的”页直接账号入口重复复现：切到张阿姨会恢复“身体数据”并打开“记录身体数据”，切回小李会自动打开旧 AI 助手。B04 与 B05 表现相似，但属于两条独立路径，修复与回归不得合并成一个断言。

### 2.2 演示准备优化，不作为业务缺陷

默认早餐任务已经记录时，控制区选择“早餐到点”并触发 N1 会正确返回“无符合资格任务”。v3 可补充清晰的前置条件或 fresh-state 场景准备，但不得为了演示通知而静默改写服药记录。

## 3. 账号、页面与草稿状态

### 3.1 账号切换落点

所有入口必须调用同一账号切换规则，包括：

- 长辈／子女页面右上角账号入口；
- “我的”页左上角账号下拉；
- `demo.html` 演示控制区；
- 原应用与演示桥允许的合法账号切换消息。

切换完成后的基础状态：

| 目标账号 | 基础页面 | 基础子状态 |
| --- | --- | --- |
| 长辈 | 服药打卡首页 | `page=home`；用药页默认 tab 为正在服用；历史日期为当前演示日 |
| 子女 | 首页看板 | `page=home`；保持当前有权限的长辈档案，否则选择首个合法档案 |

切换时关闭当前 modal、错误层、时间选择器和临时展开状态，取消尚未执行的延迟文字处理，不把原账号的焦点、滚动位置或选中任务带给新账号。

符合资格的到点聚焦提醒仍可在首页建立后按原规则出现；这不是恢复旧草稿。关闭该提醒必须回到已经建立的首页。

冷启动或整页刷新时，保留合法的当前账号、授权档案、字号和系统主题，但基础业务页必须回到该账号首页，并清除旧业务页、旧 modal 和旧焦点任务。显式点击仍有效的通知属于例外：应用先校验最新任务与权限，再定位关联内容。切换主题、同一 iframe 返回桌面或重新打开应用图标不属于冷启动，继续遵守 v2 的实例内草稿保护和页面连续性。

### 3.2 草稿保留与主动恢复

- 未保存的聊天、计划和身体数据草稿继续按 `账号 + 长辈档案` 隔离。
- 切换账号／档案前保存合法草稿，但切换后不得自动 `resume` 或重开弹窗。
- 返回原账号／档案时只显示“继续未完成的输入”。
- 用户点击继续后，才恢复原页面、原弹窗、原输入模式和原滚动位置。
- 草稿恢复前重新检查账号权限、目标档案、计划版本与任务状态；无权限或对象已失效时不恢复旧操作。
- 恢复初始演示数据时清除全部会话草稿与继续入口。

### 3.3 关闭提醒与查看任务

聚焦提醒提供两个不同命令：

1. **关闭：**不写打卡事实、不消耗延后、不立即重弹，返回长辈服药打卡首页。
2. **查看对应任务：**显式导航到当前业务日期和时段的任务详情或列表；导航前重新校验任务是否仍有效。

旧通知、已记录任务、已取消任务或解绑后的通知不能恢复可执行打卡按钮。

## 4. 长辈端服药打卡首页与聚焦提醒

### 4.1 首页信息顺序

长辈首页删除“点击添加用药打卡”和“点击添加身体数据”两张快捷卡。对应入口仅保留在“用药信息”和“身体数据”页面。

首页建议顺序：

1. 紧凑品牌顶栏、账号与历史入口；
2. 紧凑问候和当日记录摘要；
3. 当前时段或最需要处理的药物；
4. 未按时打卡提醒；
5. 今日其他记录；
6. 安全说明。

从应用品牌顶栏到问候区结束的顶部主体，不含演示舞台状态栏和“本地演示”条，在常见手机视口中约占屏高 `1/7～1/8`。压缩不得通过字体拥挤、文字重叠或缩小触控目标实现。

在 `390×844` 与 `360×800` 的首屏中，不滚动即可看见至少一个当前任务的药名、计划片／包数和完整主操作区；大字号模式也要保留明确的主按钮入口。

### 4.2 药物信息层级

- 药品名称和“每次 X 片／粒／包”等计划数量使用同等级的高对比度主要文字。
- 时段、提醒时间和餐时作为次级信息，不与计划数量争夺视觉层级。
- 不把计划剂量写成已经服用的实际剂量。
- 多药同一时段逐药显示与处理，不能用一个按钮批量确认。

### 4.3 普通任务操作布局

首页当前任务和非逾期的“该吃药啦”聚焦提醒共用以下布局：

1. 上方为大号、视觉上接近正方形的圆角“已服用”主按钮，不得退化成普通扁长条或胶囊按钮；
2. 下方从左到右为“本次无需”“延后 5 分钟”“延后 30 分钟”；
3. 下方三项合计宽度与主按钮一致，保持相同间距和稳定尺寸；
4. 两个延后按钮保留当前版本可读的宽度和触控高度，不为塞入第三项而压成窄文字条；
5. “已服用”在颜色、面积和位置上始终是第一视觉动作；
6. 延后按钮继续遵守每独立任务只能使用一次的规则。

主按钮的最终边长、最大宽度和圆角值属于视觉评审参数，但必须同时满足近正方形、下方三项等宽总组、首屏可见及大字号无溢出四个条件。

普通任务不显示“未服用”。全局将按钮和状态展示文案从“本次无需服用”缩短为“本次无需”；安全解释和数据枚举含义保持不变。

### 4.4 逾期任务操作

任务达到原自然时段截止，且没有记录、取消或延后保护时，才进入“未按时打卡”。逾期卡可以显示：

- 已服用；
- 未服用；
- 本次无需；
- 尚未使用时的延后 5 分钟／30 分钟。

页面继续提示“未打卡不等于未服药”，不因到达预警时间自动选择“未服用”。

## 5. 打卡事实、时间与通知规则

### 5.1 四自然时段与两个提醒时间

| 时段 | 自然范围 | 默认用药提醒，仅长辈 | 默认双端预警，长辈与子女 | 未按时打卡临界点 |
| --- | --- | --- | --- | --- |
| 早餐 | 05:00～11:00 | 08:00 | 09:00 | 11:00 |
| 午餐 | 11:00～16:00 | 12:30 | 14:00 | 16:00 |
| 晚餐 | 16:00～20:00 | 17:30 | 19:00 | 20:00 |
| 睡前 | 20:00～次日 00:00 | 21:00 | 22:00 | 次日 00:00 |

边界继续使用左闭右开区间；业务日期按北京时间。用药提醒和双端预警必须属于对应自然时段，双端预警应晚于用药提醒。自然截止时间不是用户可编辑的提醒字段。

### 5.2 提前预警不改变事实状态

| 通知类型 | 触发点 | 接收方 | 业务作用 |
| --- | --- | --- | --- |
| N1 用药提醒 | `reminderTime` | 长辈 | 提醒处理当前任务，不改变任务状态 |
| N2 双端预警 | `missedAlertTime` | 长辈与已授权子女 | 提示“尚未收到打卡”，不提前判定逾期或未服用 |
| N3 家属提醒 | 子女主动发起且资格通过 | 长辈 | 沿用共享冷却和幂等，不改变服药事实 |

- 用药提醒时间用于通知长辈“该处理当前任务”。
- 双端预警时间用于通知长辈和已授权子女“尚未收到打卡”。
- 提前预警发送后，任务仍为“待打卡”；不得提前改成“未按时打卡”或“未服用”。
- 通知正文必须说明“未收到打卡不代表未服药”，不得使用诊断或责备性措辞。
- 自然时段截止后仍无记录，才显示“未按时打卡”。

**待确认实现细节：**同一任务已经发送提前 N2 后，自然截止时是否保留一个独立的截止轮次通知，用户尚未明确确认。实现前必须在下一轮排期讨论中确定，不能以本文擅自取消 v2 的原截止轮次。无论最终选择一轮还是两轮，都继续按“任务 + 接收方 + 通知轮次”去重；同一轮不得重复发送，页面状态变化本身也不得隐式生成新通知。

### 5.3 “未服用”的统一资格

新增“未服用”声明须同时满足：

1. 操作者为任务所属长辈本人；
2. 当前完整时刻已经达到任务原自然时段截止；
3. 任务未取消；
4. 当前不处于有效延后保护期。

这一资格适用于首页、逾期卡、任务详情和助手。截止前助手收到“没吃”等表达时不直接写入“未服用”，而是说明时段尚未结束并保留待确认状态。

历史或已记录任务的本人更正不能只用当前 `stateOf(event) === overdue` 判断。应检查任务原截止时刻和更正权限，使已经解决的历史任务在截止后仍可更正为“未服用”，同时保留前后版本、操作者和更正时间。

### 5.4 延后、去重与停止条件

- 每个独立当次任务仍只有一次延后机会，可选 5 或 30 分钟。
- 延后不改变原自然截止、业务日期或服药事实。
- 处于延后保护期时，抑制未按时红点、双端预警和子女主动提醒。
- 若预警时刻到达时仍在保护期，保护结束后再按最新任务状态与最终确认的通知轮次策略决定是否发送；同一任务、接收方和轮次最多一次。
- 任务已记录、取消、失效或权限撤销后，不再发送该轮用药提醒或双端预警。
- 模拟离线、失败、重试、原通知 ID 复用、N3 家属共享冷却和消息幂等继续沿用 v2。

## 6. 添加与编辑用药计划

### 6.1 基础字段

| 顺序 | 字段 | 必填 | 规则与提示 |
| --- | --- | --- | --- |
| 1 | 药品名称 | 是 | 用户原文进入 HTML 前必须转义；不接药品数据库或自动给药建议 |
| 2 | 单次用药剂量（mg） | 否 | 提示“请输入每顿要吃多少毫克”；正数，允许合理小数 |
| 3 | 药品单片／包规格（mg） | 否 | 提示“请输入所购买药品实际每片／包的毫克数”；必须大于 0 |
| 4 | 实际用药片／包数 | 是 | 提示“请输入实际服用时每顿吃多少片／包”；正数，允许半片等小数 |
| 5 | 用量单位 | 是 | 保留现有可选和自定义输入能力，例如片、粒、包、毫升 |

开始日期、长期服用／截至某日期、结束日期、备注和家属协助来源继续保留。不同自然时段使用不同剂量、非每日方案等首轮不支持情形继续阻止保存，不静默合并。

### 6.2 剂量换算

只有当“单次用药剂量”和“药品规格”均有效，且实际数量单位与规格的单个单位一致时，才能自动换算。v3 首轮只对“每片规格 → 片数”和“每包规格 → 包数”应用下式：

`建议实际数量 = 单次用药剂量 mg ÷ 药品规格 mg`

该结果只是计划每次数量建议，不是实际服药事实。实现必须满足：

- 禁止零除、负数、无穷值和非数字；
- 选择粒、毫升或其他自定义单位时，不得套用“每片／每包规格”换算；本版不新增浓度、体积或跨单位换算；
- 单位不匹配时保留人工实际数量，并明确提示“当前单位不支持按此规格自动换算”，不能静默给出结果；
- 不静默四舍五入到整数、半片或任意包装单位；
- 计算结果不适合直接使用时，显示原始结果并要求用户确认实际数量；
- 自动填入后仍允许人工修改；一旦用户人工修改，不因其他字段重渲染而静默覆盖；
- 自动值与人工值不一致时在核对页同时展示来源和差异，由用户确认；
- mg 字段为空时，实际数量仍可独立填写并保存；
- 核对页完整显示药名、两个 mg 字段、实际数量、单位、时段、两个提醒时间和餐时。

### 6.3 每时段信息分块

早餐、午餐、晚餐、睡前各使用一个独立分块。所有手机与桌面尺寸均使用分块，不恢复五列窄表格。

每个分块必须包含五组信息：

1. 选择复选框；
2. 时段名称与自然范围；
3. 用药提醒，并标注“仅提醒长辈”；
4. 双端预警，并标注“提醒长辈与子女”；
5. 餐时控件。

两个时间输入可直接键入，也可打开小时／分钟滚轮。滚轮不再显示“如何上下滑动”的操作提示行，但保留明确标题、当前值、确认、取消和键盘等效操作。

### 6.4 餐时控件

- 每个选中时段分别提供“餐前／随餐／餐后”横向三段控件。
- 三项互斥，但可以全部不选。
- 再次点击已选项可清除，或提供等效的“清除餐时”。
- 选中态不能只靠颜色辨认。
- 首页、用药信息、历史清单和助手查询均在对应时段显示餐时，不把一个时段的餐时套到整条药物计划。

### 6.5 助手、手动表单与核对共用草稿

- 文字、示例语音、配置卡、时间滚轮和完整手动表单修改同一份计划草稿。
- 已处于计划上下文时，“早餐提醒改为 09:00”等字段修改优先于通用查询意图。
- 新回复只修改有明确证据的字段，不覆盖其他人工修改。
- “1 片，不是 2 片”“不是一片，是半片”“饭前改饭后”等表达保留原文、否定范围和最终方向。
- 不能可靠解析时显示冲突并阻止核对，不按最后一个正则匹配值猜测。
- 所有入口最终经过同一必填校验和统一核对页，再调用唯一保存路径。

## 7. 用药信息、打卡记录与重新启用

### 7.1 打卡记录清单

长辈“用药信息 → 打卡记录”不再复用首页打卡卡片。按早餐、午餐、晚餐、睡前及计划时间从早到晚显示，每个任务一行，至少包含：

- 药品名称；
- 计划实际数量与单位；
- 计划时间；
- 餐前／随餐／餐后，可空；
- 当前状态：已服用、未服用、本次无需、未按时打卡、待打卡或延后中；
- 有记录时的记录时间和本人身份。

清单行本身不铺开整组打卡按钮。点击行进入任务详情，详情根据权限和任务状态提供补记或更正。

### 7.2 补记与更正

- 跨日补记、本人更正、5 秒撤销、版本留痕和星星结算继续沿用 v2。
- 已记录任务的入口统一显示为“更正”，不使用“更正记录”等长文案。
- “更正”使用有边框、轻填色、低强调的小型按钮；功能可发现，但不与主要打卡动作争夺层级。
- 子女可以查看版本和来源，但不能补记或更正长辈的服药事实。

### 7.3 已停用与重新启用

已停用计划卡至少显示药名、原计划数量、时段摘要和停用日期。重新启用按钮位于进入详情箭头之前，文案固定为“重新启用”。

重新启用流程：

1. 打开与添加／编辑计划一致的浮动表单；
2. 预填旧计划的药名、剂量、规格、实际数量、单位、时段、提醒、预警、餐时、周期和备注；
3. 新开始日期默认为当前演示日，但必须允许修改并核对；若旧计划的固定结束日期已经早于新开始日期，必须重新选择长期服用或新的结束日期，不能保存无效区间；
4. 自动在备注中追加“从 YYYY-MM-DD 到 YYYY-MM-DD 期间停用”的演示说明；结束日取用户最终确认的新开始日期，不取打开表单或点击按钮的临时时刻；
5. 确认后创建新计划，并记录与旧计划的关联；旧计划继续保持 inactive；
6. 新任务只按新计划生效时间生成；去重只在 `resumedFromPlanId` 关联链中的旧计划与新计划之间执行，不凭相同药名删除另一条独立计划。

若旧存档没有可信 `stoppedAt`，界面显示“停用日期不可追溯”，不得编造日期。此时重新启用前要求用户确认可知信息，自动备注不能生成虚假的精确停用区间。

关联旧计划已经开始的任务继续保留。新计划只生成适用且不与关联链旧任务重复的日期／时段任务；核对页必须明确列出新开始日当天哪些时段会生成、哪些因关联旧任务保留而不再生成。

## 8. 子女首页看板与双端助手入口

### 8.1 子女看板顺序

建议固定为：

1. 紧凑标题、当前长辈和日期；
2. 本月打卡日历；
3. 压缩后的已记录／待记录／未按时统计；
4. 未按时打卡提醒；
5. 今日用药；
6. 身体数据。

日历必须位于页面靠前位置。统计、提醒、今日用药和身体数据仍属于同一当前长辈，不因前置日历而删除。切换长辈时整页同时更新，子女仍不能代打。

### 8.2 日历紧凑化

- 保留七列、月份、今日标识、未来日期禁用和黄色星星规则。
- 减少外层留白、行距和重复说明，不缩小到难以点击。
- 标准与大字号下日期文字不重叠，星星不遮挡日期。
- `360px`、`390px` 手机首屏应能看到完整日历主体，并露出后续统计或提醒区域。

### 8.3 双端话筒入口

长辈和子女首页右下角助手均使用话筒图标。点击仍进入文字模式，长按仍进入明确标记的模拟语音模式；图标变化不代表已经接通麦克风、ASR 或模型。

入口保留清晰的 `aria-label`、键盘等效操作和长按取消逻辑。计划页与身体数据页原有加号入口不替换为话筒。

## 9. 身体数据与血糖

### 9.1 保留范围

血压、血脂、身体指标、BMI、测量时间、来源、备注和现有趋势继续保留。新增血糖不得引入诊断分级、风险颜色、阈值预警、剂量建议或自动医疗结论。

### 9.2 血糖七个测量时段

| 顺序 | 测量时段 | 实现建议默认提醒时间（待确认） |
| --- | --- | --- |
| 1 | 早起空腹血糖 | 06:30 |
| 2 | 早餐后 2h 血糖 | 09:30 |
| 3 | 午餐前空腹血糖 | 11:30 |
| 4 | 午餐后 2h 血糖 | 14:30 |
| 5 | 晚餐前空腹血糖 | 17:30 |
| 6 | 晚餐后 2h 血糖 | 20:00 |
| 7 | 睡前血糖 | 22:00 |

七个测量场景和“各自具有可调整默认提醒时间”已经确认；表中七个具体钟点尚未由用户逐项确认，只是实现建议。默认单位使用 `mmol/L` 也属于实现建议，进入开发前应与具体钟点一并确认。任何默认值都不是医疗建议，不根据用药提醒推断真实用餐时间，也不进行未经确认的单位换算或医学解释。

### 9.3 当日直接填写

- 血糖页默认展示当前演示日期的七行。
- 每行左侧使用清楚的大字号测量名称，右侧为数值输入和单位。
- 允许只填写一行或部分行后保存；空白行不生成记录，不写入 0，也不标记异常。
- 每个非空值必须分别确认实际测量日期时间，并保存自己的测量场景、测量时间、录入者、来源和可选备注。
- 提醒时间只用于提示测量，绝不能自动填充、替代或回写为实际测量时间。
- 修改当天已有值时进入明确的编辑／更正流程，不静默生成重复记录。

### 9.4 协助录入、助手与历史趋势

- 长辈录入自己的血糖；子女只能为已授权长辈协助录入，并在保存前确认对象和协助来源。
- 助手识别“早餐后两小时血糖 7.1”等支持表达时，填入对应日期和行；时段不明确时先追问，不猜行。
- 历史可按日期查看七个时段的实际记录。
- 趋势按同一测量时段跨日期比较，不把空腹、餐后和睡前值混成同一条线。
- 血糖提醒设置与测量记录分开；提醒触发不自动生成空记录。

## 10. “我的”页模拟身份与功能入口

### 10.1 顶部身份区

本地演示必须提供可操作的“微信登录（模拟）”按钮。恢复初始演示数据后，张阿姨和小李默认已完成模拟登录，因此直接显示身份区；身份区另提供低强调的“重新演示微信登录”命令，进入临时登录画面后可再次点击“微信登录（模拟）”返回当前演示账号。重演登录不清空业务数据、不切换账号，也不得调用真实微信 SDK、读取真实微信资料或把模拟状态写成真实认证成功。

“我的”页顶部约 `1/4` 高度显示：

- 左侧默认头像，样式可按演示账号年龄区分；
- 姓名、年龄和账号端，例如“张阿姨 · 68 岁 · 长辈端”；
- 家庭二维码图标，点击后展示明确标注的模拟绑定码；
- 左上角账号下拉，用于选择当前演示账号；
- 右上角从左到右为设置齿轮和扫一扫图标。

默认头像至少区分长辈账号和成年子女账号，不要求用户上传图片。重新演示登录后继续使用当前账号对应的默认头像。

设置齿轮优先定位到本页既有设置区域，不重复创建另一套设置数据。扫一扫打开模拟扫描反馈，不请求真实摄像头权限。

### 10.2 功能行

在原“切换演示账号”区域放置带右侧箭头的列表行：

| 功能行 | 本版行为 |
| --- | --- |
| 导出健康报告 | 仅保留可点击占位入口，明确提示尚未生成报告；不创建 PDF、分享或真实导出能力 |
| 连接外部设备 | 仅保留占位入口，不连接血压计、手环或其他硬件 |
| 导入健康数据 | 可进入占位子页，显示 Apple 健康、华为运动健康、小米健康三个入口；均标注未接入 |
| 提醒设置 | 仅保留入口和未开放反馈，本版不新增另一套可写提醒设置 |

列表行使用语义按钮或链接，不使用普通可点击 `div`。占位入口不得显示为已经成功连接、导入或导出。

### 10.3 保留的既有区域

显示设置、家庭与账号、演示设置继续保留现有功能与样式基础。模拟通知、演示时间、隐私说明和恢复初始数据仍可用；账号切换移动到左上角后，不在列表中重复保留旧账号按钮组。

## 11. AI 助手规则修复与扩展

### 11.1 否定、改口和冲突

解析顺序必须先识别否定范围、改口连接词和候选字段，再决定最终值。至少覆盖：

- “每次 1 片，不是 2 片”保留 1 片；
- “不是一片，是半片”得到 0.5 片；
- “饭前改饭后”得到餐后；
- “还没吃”不自动等于最终未服用；
- 同时出现互相冲突的剂量且方向不明确时要求确认。

原始文字、解析候选、人工修改和最终核对值继续留在同一会话草稿中。

### 11.2 计划上下文优先

当前会话已有未保存计划时，包含药名、剂量、规格、数量、单位、时段、用药提醒、双端预警、餐时、日期或周期的修改表达，优先更新当前草稿。只有明确询问已保存计划时才进入查询分支。

修改成功后同时更新聊天摘要、配置卡和手动表单；修改失败时说明哪一字段无法识别，不返回与当前草稿无关的已保存计划列表。

### 11.3 血糖和权限

助手可协助定位血糖七行并填入用户明确提供的值。它不根据数值判断正常／异常，不建议调整药物，也不能让子女通过助手声明长辈已服用、未服用、本次无需或设置延后。

## 12. 数据、迁移与权限

### 12.1 概念字段

以下字段描述业务含义，最终命名可在实现时微调，但不能合并掉独立语义：

| 对象 | v3 新增或调整的信息 |
| --- | --- |
| 药品计划 | `doseMg` 可空、`strengthMg` 可空、`doseCount` 必填、`doseUnit` 必填；原始输入与自动／人工来源 |
| 时段设置 | `reminderTime`、`missedAlertTime`、`meal`；两个时间分别校验，餐时支持空／餐前／随餐／餐后 |
| 当次任务快照 | 药名、mg 字段、实际数量、单位、时段、餐时、两个提醒时间、自然截止、计划版本 |
| 停用与重新启用 | `stoppedAt`、`stoppedBy`、`resumedFromPlanId` 或等价关联、新计划生效时间、停用区间备注来源 |
| 血糖记录 | 长辈、业务日期、测量场景、数值、单位、测量时间、录入者、来源、备注 |
| 血糖提醒 | 长辈、七个场景、启用状态和可调整提醒时间；与测量记录分离 |
| 模拟身份 | 演示账号模拟登录状态、默认头像来源；不伪造真实微信标识或 token |

### 12.2 v2 → v3 本地存档迁移

- 数据结构版本升级为 3，但兼容键 `silver-health-data-v1`、`silver-health-view-v1` 和 `SILVER_SEED_DATA` 名称保持不变。
- 旧计划的 `doseValue`／`doseUnit` 迁移为实际计划数量／单位；`doseMg` 和 `strengthMg` 保持空，不凭药名推算。
- 旧 `slotSettings.time` 迁移为用药提醒时间。
- 新计划直接使用本章默认双端预警。旧计划只有在默认预警晚于其自定义用药提醒且位于同一自然时段时才可安全带入；否则标记待确认或暂不启用提前预警，不静默造值。
- 旧餐前／餐后值原样迁移；没有餐时的计划保持空，不默认成随餐或餐后。
- 旧停用计划保留可信 `stoppedAt`；缺失时显示不可追溯，不用迁移时刻代替真实停用日。
- 已生成任务继续使用原快照和版本，不因父计划迁移而改写历史药名、数量、提醒或餐时。
- 无效存档保留原始内容并合理降级，不能用默认数据覆盖后声称迁移成功。
- 恢复初始演示数据必须生成完整 v3 默认结构，并清除草稿、提醒轮次和模拟登录临时交互状态。

### 12.3 单一保存路径与权限

所有计划、任务、声明、更正、重新启用、血糖记录和提醒设置必须经 `commit(change)`、`validData()` 和本地持久化统一处理。渲染函数不得产生通知或直接改写业务状态。

| 行为 | 长辈 | 子女 |
| --- | --- | --- |
| 查看计划、任务和健康数据 | 仅本人 | 仅已绑定并授权的长辈 |
| 添加／编辑／停用／重新启用计划 | 本人可管理后续安排 | 可为已授权长辈协助，保存前确认对象 |
| 已服用／本次无需 | 仅本人 | 禁止 |
| 未服用 | 仅本人，且达到原自然截止后 | 禁止 |
| 延后 | 仅本人符合资格的当次任务 | 禁止 |
| 补记／更正 | 仅本人任务 | 禁止 |
| 录入血糖等身体数据 | 本人 | 可为已授权长辈协助并记录来源 |
| 发起模拟提醒 | 接收本人提醒 | 仅对已授权且符合资格的任务发起 |

## 13. 演示、安全与可访问性边界

### 13.1 仍是本地静态 Demo

本版可以模拟微信登录、家庭二维码、扫一扫、健康平台入口、设备入口和通知，但不接入：

- 真实微信登录、实名认证、摄像头扫码或家庭云同步；
- Apple 健康、华为运动健康、小米健康或真实外部设备；
- 真实短信、电话、系统推送或服务端通知；
- 真实麦克风、ASR、模型 API 或任意自由口语理解；
- 药品数据库、OCR、相互作用、诊断、处方核验、剂量建议或阈值预警；
- 真实健康报告生成、下载或分享。

界面继续保留“仅供演示，不提供医疗建议，不用于真实用药决策”。模拟入口必须在入口或结果页明确标注“模拟／未接入”，不能只写在隐私说明中。

### 13.2 适老和响应式要求

- 所有操作使用语义按钮、输入、标签、选项和对话框；保留可访问名称、焦点恢复、`aria-live` 和 modal `inert`。
- 长辈主按钮保持稳定尺寸，动态药名、剂量和大字号不能推动按钮产生明显布局跳动。
- 时段分块、血糖七行、日历、历史列表和“我的”页分别检查 `320／360／390／1280px`。
- 同时检查标准字号和适老大字号；文字不得遮挡、截断关键药名或溢出按钮。
- 三系统主题继续共享业务结构，不因主题切换改变账号、档案、草稿、提醒资格或数据。
- 减少动态与减少透明效果继续有清楚的静态回退。

## 14. v3 验收清单

以下均为后续开发的待验收项。写入本文不算通过；每项必须有规则测试、浏览器断言、截图或明确的人工验证依据。

### 14.1 稳定性与核心打卡

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| V3-A01 | 分别从首页右上角、“我的”页左上角和演示舞台连续切换账号 | 每次先进入目标账号首页；不恢复旧页面，不自动打开聊天、计划或身体数据草稿 |
| V3-A02 | 在两个账号分别留下聊天和身体数据草稿后往返切换 | 草稿不串账号／档案；只显示继续入口；点击后才恢复正确草稿与位置 |
| V3-A03 | 长辈关闭“该吃药啦” | 不写记录、不耗延后、不立即重弹，落在服药打卡首页；显式查看按钮才进入对应任务 |
| V3-A04 | 进入长辈首页 | 不显示两张快捷添加卡；用药信息与身体数据页仍各有添加入口 |
| V3-A05 | `390×844` 与 `360×800` 标准／大字号查看首屏 | 顶部约占屏高 1/7～1/8；不滚动即可看见当前药名、计划数量和主操作区 |
| V3-A06 | 查看普通待打卡任务和非逾期聚焦提醒 | 近正方形圆角“已服用”主按钮在上；下方依次为本次无需、延后 5、延后 30，合计宽度一致；延后按钮不被挤窄；无“未服用” |
| V3-A07 | 首页与聚焦提醒比较同一任务 | 药名和计划片／包数同等突出；关闭提醒回首页后任务与操作一致 |
| V3-A08 | 截止前从按钮、详情和助手尝试“未服用” | 均不写入；保持待打卡并给出符合阶段的说明 |
| V3-A09 | 到达自然截止且无记录／保护后选择“未服用” | 长辈本人可记录，子女不可；状态、历史和星星同步更新 |
| V3-A10 | 已记录历史任务在截止后更正为“未服用” | 更正可执行并保留版本；不因任务已 resolved 而错误拒绝 |

### 14.2 计划、提醒与历史

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| V3-A11 | 新建计划查看基础字段 | 药名、可选剂量 mg、可选规格 mg、必填实际数量和必填单位完整，提示文案正确 |
| V3-A12 | 输入 100mg／50mg、75mg／50mg、不可整除组合及“毫升 + 每片规格” | 同单位时分别建议 2、1.5 和需人工确认的原始结果；单位不匹配时拒绝自动换算并保留人工数量；不静默取整或覆盖人工值 |
| V3-A13 | mg 字段留空，只填实际数量和单位 | 可以核对和保存；任务快照显示计划数量，不虚构 mg |
| V3-A14 | 查看四个时段设置 | 所有尺寸均为四个分块；每块含选择、范围、用药提醒、双端预警和餐时五组信息，无右侧挤压 |
| V3-A15 | 调整时间并选择餐前／随餐／餐后 | 默认 `08:00/12:30/17:30/21:00` 与 `09:00/14:00/19:00/22:00` 正确；三段互斥可清除；滚轮无操作提示行 |
| V3-A16 | 09:00 早餐仍未记录 | 长辈与子女收到“尚未收到打卡”模拟预警，任务仍显示待打卡，不能选未服用 |
| V3-A17 | 11:00 早餐仍未记录；或预警时处于延后保护 | 前者变未按时打卡并开放本人未服用；后者保护期内不发预警，结束后按最新状态和最终轮次策略处理；同一任务、接收方、轮次不重复 |
| V3-A18 | 打开长辈打卡记录并点任务行 | 列表按早到晚显示药名、数量、时间、餐时和状态；点行进入详情；更正按钮短、低强调且可辨 |
| V3-A19 | 查看已停用计划 | 每张卡显示可信停用日期或“不可追溯”，并有“重新启用”按钮 |
| V3-A20 | 重新启用有完整历史的计划 | 表单预填，备注以新开始日结束停用区间；确认后创建关联新计划，旧计划仍停用；旧结束日过期时先修正周期 |
| V3-A21 | 重新启用与旧计划当天保留任务重叠，另有同名独立计划 | 只在 `resumedFromPlanId` 关联链内避免同日同槽重复；旧已开始任务保留，同名独立计划不被删除；核对页说明当天结果 |

### 14.3 看板、助手、血糖与“我的”

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| V3-A22 | 子女进入首页并切换长辈 | 日历位于档案信息后，统计区压缩；日历、提醒、今日用药、身体数据始终属于同一档案 |
| V3-A23 | 手机标准／大字号查看子女日历 | 完整日历主体靠前且紧凑，星星与日期不重叠，并能看到下一模块线索 |
| V3-A24 | 两端点击或长按首页话筒 | 图标均为话筒；点击文字、长按模拟语音；不请求真实麦克风，不改变角色权限 |
| V3-A25 | 血糖页填写七行中的任意两行并保存 | 两行分别确认实际测量日期时间，只生成两条记录；提醒时间不冒充测量时间；空白行不写 0、不生成任务、不判断异常 |
| V3-A26 | 在具体默认值确认后修改七个血糖提醒时间 | 提醒配置独立保存，不产生测量值；恢复初始数据回到最终确认的七个默认时间和单位 |
| V3-A27 | 子女为授权长辈录入血糖 | 对象清楚、来源为家属协助；无权限档案不能保存 |
| V3-A28 | 助手处理明确和模糊血糖表达 | 明确表达填入正确行；缺日期／场景时追问；不输出医学评价 |
| V3-A29 | 查看多日血糖趋势 | 同一测量场景跨日比较；不把空腹、餐后和睡前混成同一趋势 |
| V3-A30 | 恢复初始数据后打开“我的”，再重演登录 | 默认已模拟登录；头像、姓名、年龄、账号端和模拟家庭二维码清楚；可操作的“微信登录（模拟）”按钮不请求真实授权，重演不清空业务数据 |
| V3-A31 | 使用左上账号下拉、设置和扫一扫 | 切账号仍落首页且不自动开草稿；设置定位既有区域；扫一扫明确为模拟且不请求摄像头 |
| V3-A32 | 点击健康报告、设备和提醒设置 | 均只有清楚的占位反馈，不产生虚假报告、连接或可写配置 |
| V3-A33 | 打开导入健康数据 | 显示 Apple 健康、华为运动健康、小米健康占位入口，均不发网络请求或写入伪造数据 |

### 14.4 解析、迁移与全局回归

| 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| V3-A34 | 输入“每次 1 片，不是 2 片”等否定／改口组合 | 最终值和方向正确；无法可靠判断时阻止核对，不误存 |
| V3-A35 | 先建立计划草稿，再说“早餐提醒改为 09:00” | 同一草稿、配置卡、表单和核对页均更新为 09:00，不跳到已保存计划查询 |
| V3-A36 | 加载 v2 正常、缺字段和无效存档 | 可验证迁移或合理降级；不推造 mg、预警、停用日期或血糖数据；旧任务历史不改写 |
| V3-A37 | 重跑长辈打卡、子女查看、星星、延后、补记、更正、离线和三系统流程 | 未被 v3 替换的 v2 能力无回归，子女仍不能代打 |
| V3-A38 | `320/360/390/1280px × 标准/大字号 × 三系统` | 首页、时段分块、提醒弹窗、历史、日历、血糖和“我的”无关键溢出、遮挡或不可操作控件 |
| V3-A39 | 离线打开静态服务完成选定 Demo 路径 | 无新增 npm／网络依赖；所有模拟入口和安全说明可用，不冒充真实服务 |
| V3-A40 | 冷启动／整页刷新两端，再对比切主题和同实例返回桌面 | 冷启动保留合法账号／档案但回对应首页且不恢复旧 modal；切主题／回桌面保持实例内页面和草稿；有效通知仅在重新鉴权后定位 |

## 15. 实施优先级讨论草案

> 本章是基于当前时间压力给出的**优先级建议**，不是已批准排期，也不删除任何已确认需求。下一轮应以演示期限、可用人时和可接受风险共同确定实际范围。

### 15.1 建议第一优先：先让现有 Demo 稳定可信

| 建议级别 | 内容 | 原因 |
| --- | --- | --- |
| P0 | 账号切换统一回首页、草稿不自动弹出 | 当前可稳定复现，直接破坏双端演示 |
| P0 | 关闭聚焦提醒回首页，并保留显式查看任务 | 当前落点与新确认规则冲突，且影响长辈主流程 |
| P0 | 修复否定剂量解析和计划草稿时间修改 | 会把错误计划带到核对页，属于数据语义缺陷 |
| P0 | 修复加药页右侧挤压，并补窄屏／大字号回归 | 已影响表单基本可用性，不能留到视觉润色阶段 |

P0 完成前不建议开始大型新增模块。每个 P0 都需要针对性测试，不能只依赖原有 635 项通过断言。

### 15.2 建议第二优先：核心规则和长辈首屏卖点

| 建议级别 | 内容 | 原因 |
| --- | --- | --- |
| P1 | 统一“未服用”截止后资格和双端提前预警 | 属于新增关键规则，必须跨按钮、助手、通知和更正一致 |
| P1 | 长辈首页删除快捷卡、压缩顶部、首屏展示主打卡 | “打开就会用”是本轮最明确的产品卖点 |
| P1 | 药名与计划片／包数放大，统一聚焦提醒按钮布局 | 直接降低老人看错药量或找不到主动作的风险 |
| P1 | 四时段分块、餐前三段、两个提醒时间及新默认值 | 是提前预警和表单可用性的共同基础，不能只改一半 |

### 15.3 建议穿插的小改动

以下改动范围较小，可在相关页面修改时一并完成，但不能挤占 P0/P1 的回归时间：

- 双端话筒图标；
- 子女日历前置和统计／日历压缩；
- “更正”短文案与低强调按钮；
- N1 演示场景前置条件提示。

### 15.4 建议第三优先或完整后置

| 候选 | 建议 | 取舍理由 |
| --- | --- | --- |
| mg 剂量／规格换算 | 有完整开发与迁移时间时作为 P1 后半段；否则整体后置 | 产品价值高，但会同时影响表单、助手、核对、计划、任务快照、迁移和历史，不能只做表面输入框 |
| 打卡历史清单重做 | P1 稳定后再做 | 重要但不阻断当前核心打卡，可在现有历史功能可用的前提下延后 |
| 重新启用计划 | 建议后置 | 当前仍可新建计划替代，新增关联、停用区间和任务去重需要完整验证 |
| 血糖七时段模块 | 建议整体后置 | 是独立的大模块，包含录入、提醒、助手、历史、趋势和迁移；最容易拖累核心 Demo |
| “我的”页完整重做 | 建议后置，仅小入口可酌情先做 | 多数为模拟身份与占位入口，不影响家庭用药闭环 |

### 15.5 限时 Demo 的建议最小完成面

若时间只能覆盖一轮，建议至少完成：

1. 全部 P0；
2. “未服用”资格与双端提前预警；
3. 长辈首屏简化、药名／片数突出和主按钮布局；
4. 与上述规则直接相关的时段分块和默认时间；
5. 双端账号、本人权限、提醒、延后、打卡、子女查看的完整回归。

mg 换算、血糖、重新启用和“我的”页不应以半成品形式挤进这条最小路径。是否把其中某一项提升，留到本文完成后的优先级讨论决定。

## 16. 文档完成条件与下一步

本文完成只表示：

- 15 项新需求均有正文归属；
- 当前确定缺陷与新增规则已分开；
- 8 项讨论决定已经写入；
- v2 冲突条款、迁移、权限、安全和验收边界明确；
- 优先级仅作为建议，没有擅自宣称全部进入本次 Demo。

下一步不是立即开发，而是先确认第 15 章：本次实际做哪些、哪些整体后置、可投入多少时间，以及每个实施阶段的完成门槛。确认后再建立 v3 实施步骤、更新项目实施基准并开始代码修改。
