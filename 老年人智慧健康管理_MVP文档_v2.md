# 银龄健康管家：双端家庭协同 MVP 文档 v2

> 文档性质：第二版产品需求规格与验收依据，依据用户本轮提出的九组改动整理。本文记录目标行为，不代表功能已经开发、上线或获得监管认可。
>
> 本轮交付范围：完整记录需求、提出可供评审的初版呈现、说明实现条件和合规边界。页面代码、真实 APP、通知服务、语音服务及支付服务不属于本轮文档交付。

## 0. 版本目标与阅读约定

### 0.1 第二版目标

在第一版“子女协助建计划 → 长辈打卡 → 双端查看 → 子女提醒”的基础上，补充子女首页看板、长辈快捷入口、精细用药计划、AI 语音引导、BMI、APP 系统通知、陪伴形象及家庭奖励机制。

用户强调的三个方向是：长辈操作更直接；子女能集中看见长辈的记录；真实 APP 能联动两端手机通知。三类真实通知是明确的产品必做目标，不能以页面弹窗或模拟日志替代最终验收。

### 0.2 需求标记

| 标记 | 含义 | 开发使用方式 |
| --- | --- | --- |
| 明确需求 | 用户已经明确提出的内容、数字、文案或位置 | 必须保留；不能因实现复杂而静默删除 |
| 初版建议 | 为用户尚未定稿的呈现提供一版可讨论方案 | 可用于原型评审，不等于用户已经确认 |
| 待确认 | 原始要求未给出口径，或多种要求之间需要选择 | 记录候选与影响；在相关生产功能启用前确定 |
| 实现约束 | 为数据真实性、权限、可靠性和可访问性补充的工程规则 | 不应冒充用户原话；实现时必须有对应验证 |
| 上线条件 | 真实服务所需的基础设施、平台审核、隐私或资金条件 | 原型演示不能代替这些条件 |

本文件中的“必须”可能来自明确需求，也可能来自注明的实现约束或上线条件。未决事项集中在第 15 节，避免开发者从示例中猜测产品决策。

### 0.3 与现有文件的关系

| 文件或范围 | v2 的处理 |
| --- | --- |
| `老年人智慧健康管理_MVP文档_v1.md` | 保留第一版历史。本文明确变更的部分以 v2 为目标；未变更的家庭权限、人工确认、历史留存等继续适用 |
| `老年人智慧健康管理PRD.md` | 远期方向参考。医生端、诊断、处方建议、真实硬件、SOS 等没有因此进入本次范围 |
| `AGENTS.md` | 描述当前静态演示项目的实际边界。v2 是待实现需求，不能据此把当前模拟服务描述成真实能力 |
| 当前 HTML 原型 | 仍是原生 HTML/CSS/JavaScript、本地演示账号与本地存储；没有因本文新增真实服务 |
| 演示日期 | 原型继续使用固定演示日 `2026-09-13`；真实 APP 使用用户所属时区的实际日期，二者不能混用 |

v2 明确替换的 v1 规则包括：子女首个导航名称、长辈首页添加入口、加号的分页面路由、顶部时钟用途、四时段及提醒配置、餐前餐后独立标记、第三种录入模式、全局语音入口、“跳过本次”文案和 BMI。v1 的“提醒时间 + 60 分钟”规则与用户本次“超过时段仍未按时打卡”的要求存在差异，按第 7 节单独处理。

### 0.4 交付层级

| 层级 | 可以交付什么 | 不能据此宣称什么 |
| --- | --- | --- |
| v2 交互原型 | 完整页面、表单、确认、状态变化、日历、模拟语音、模拟通知、模拟奖励账本 | 已在后台提醒真实手机、完成真实识别、发生真实充值或提现 |
| v2 真实 APP | 安装包、真实身份与家庭授权、跨设备数据、真实语音与通知等已接通功能 | 所有手机在任意条件下必然准时送达、已发送就代表老人已经看见或服药 |
| 真实资金与会员运营 | 符合选定地区及平台规则的订阅、充值、奖励、提现与对账 | 仅接一个支付接口即可证明整个资金模式合法可运营 |

## 1. 九组需求追踪总表

| 原始编号 | 对应需求 | 主体章节 | 验收编号 |
| --- | --- | --- | --- |
| R1 | 子女首个页面改首页看板；本月日历；完成日黄色星星叠加日期；未完成日只显示数字；今日用药；今日或最新身体数据；先提出一版呈现 | 2 | A01–A04 |
| R2 | 长辈首个页面仍叫服药打卡；中上方两张左右圆角添加卡片；尺寸适中；支持子女借长辈手机协助添加；时钟直达用药信息的打卡记录 | 3 | A05–A07 |
| R3 | 用药信息加号直接加药；身体数据加号直接添加身体数据；取消跨类别二选一 | 4 | A08 |
| R4 | 药名沿用；单次用量括号提示；单位可直接输入；四列四时段；默认可调提醒时间与滚轮；餐前后分段标签；PRO AI 语音创建；逐步引导；统一补齐与核对；必填星号、缺项红框与醒目提示 | 5 | A09–A18 |
| R5 | 长辈首页右下角全局语音助手；识别意图；加药、记实际服药、延后提醒、超时补打；新记录后追问遗漏；三种回复；全局“本次无需服用”；语音记录血压、血脂、身体指标与备注 | 6、7 | A19–A26 |
| R6 | 根据身高体重自动计算 BMI，在身体数据的身体指标中显示，不要求用户填写 BMI | 8 | A27–A28 |
| R7 | 产品转 APP，外观先呈现 iPhone；适配 iOS、Android、HarmonyOS；到点提醒长辈、超时未打卡提醒子女、子女远程发起提醒长辈；通知包含时段、药物、剂量 | 9 | A29–A33 |
| R8 | 喜庆有面部的卡通形象，四种候选；先静态后动态；老人端存钱罐入口；每日全部打卡随机 0.1～1 元；10 元提现；月底不清零；主要由子女充值；月季年倾向；AI 为 PRO；双方“我的”预留会员 | 10 | A34–A39 |
| R9 | 修复所有加号相关用药/身体图标的形变和错位；小圆红点替代未按时提醒警告图标；可闪烁；展开折叠不使红点翻转 | 11 | A40–A42 |

跨模块的数据、权限、隐私、资金和版本回归验收见 A43–A47。附录 A 保留九项原始需求，供反向核对。

## 2. 子女端：首页看板

### 2.1 名称、对象与入口

**明确需求：**子女端原“服药打卡”页面改为“首页看板”，首个底部导航和页面标题同步修改。长辈端的同一导航位置仍叫“服药打卡”。

继续在显眼位置显示当前长辈及切换器。月历、今日用药、身体摘要、提醒按钮均属于同一个选中档案，切换长辈时整体更新。子女只能查看已绑定且已授权的档案，不能代替长辈声明服药事实。

### 2.2 本月打卡日历

**明确需求：**显示本月完整日历。某一天全部完成打卡时，在当天日期上叠加黄色星星图标。未完全完成的日子没有日期背景，只显示日期数字。

| 元素 | 呈现与行为 |
| --- | --- |
| 月份 | 清楚显示年月；原型默认 `2026 年 9 月`，真实 APP 默认所属时区本月 |
| 日期 | 七列月历，日期字号可辨认；月首、月末补位留空或弱化，不计入当月数据 |
| 完成日 | 黄色星星叠加日期区域，日期仍可阅读；不能只在日历下方另列星星 |
| 未完成日 | 无填充底色、无星星，保留数字；不能改成灰底、红底等另一套完成标记 |
| 今天 | 初版建议用月份区域“今天”说明或日期下的小线条定位，避免给未完成日添加背景 |
| 未来日期 | 不显示完成星星，不预发奖励 |
| 说明 | 初版建议在月历旁保留一句“星星表示当日符合全部打卡条件”，实际条件随第 7.4 节决策同步说明 |
| 点日期 | 初版建议跳转“用药信息 → 打卡记录”，携带该日期与长辈对象，便于查看星星依据 |

日历必须依据该日实际生成且应处理的任务及历史记录计算，不能用今天的药物计划反推整个月的历史。无任务日、补打卡、“本次无需服用”及明确未服用是否算完成，用户尚未确定，不能默认全部点亮。

初版范围为“本月”；是否支持切换历史月份列为可选待定项，不以扩展月历功能阻碍本月展示。

### 2.3 今日用药模块

**明确需求：**在看板直接看见长辈今天的用药情况。

**初版建议：**模块标题为“今日用药”，先显示“已记录 X/Y、待记录 N、未按时打卡 N”，下方按早餐、午餐、晚餐、睡前分组展示简洁列表。计数中的“已记录”包括不同用户声明，不能直接命名为“已服药”。

每条至少显示：药品名称、单次计划剂量（例如 `1 片`、`2 包`）、所属时段、提醒时刻、餐前/餐后标签（如有）、当前打卡状态。可展开查看实际服药时间和补记信息。

对允许提醒的未记录任务提供“提醒 TA”；真实 APP 发起第 9 节远程提醒，原型明确显示“模拟提醒”。模块末尾提供“查看全部用药/打卡记录”。没有任务时显示“今天暂无用药打卡计划”，不将其描述为“全部完成”。

### 2.4 今日或最新身体数据模块：第一版呈现建议

用户尚未确定布局，先采用一张“身体数据”摘要卡，按类型分三行，避免把看板做成密集图表。

| 行 | 摘要内容 | 日期规则 | 入口 |
| --- | --- | --- | --- |
| 血压 | 收缩压/舒张压，单位 mmHg；脉搏有记录时附带显示 | 优先展示今天最近一次；否则明确标“最近一次”并显示实际日期时间 | 查看血压详情 |
| 血脂 | TC、TG、HDL-C、LDL-C 中已有项目及单位；空间不足时显示前两项和“查看全部” | 使用本次检验记录的日期，不因今天录入就标为今天测量 | 查看血脂详情 |
| 身体指标 | 体重、身高、可计算时的 BMI；体脂率有值时在详情显示 | 标注测量时间；跨次引用身高时显示来源时间 | 查看身体指标 |

各类型独立选取最新记录。不能因为血压是今天的，就把整张卡中的旧血脂也标成“今日”。无数据则显示“暂无记录”和“添加身体数据”，不显示零或虚构正常值。

只展示数值、单位、日期与来源，不引入红绿诊断色块、达标评价或自动医疗建议。首页可保留简短摘要，原有血压趋势继续放在详情页。

**初版建议的页面顺序：**当前长辈与日期 → 今日用药 → 本月日历 → 身体数据。存在未按时记录时，在今日用药区域突出提示。模块顺序可在视觉评审中调整，三个模块均必须保留。

## 3. 长辈端：简化“服药打卡”首页

### 3.1 两张快捷添加卡片

**明确需求：**首页名称保持“服药打卡”。原右下角添加加号迁移为页面中上方的两张左右分布圆角方框，位于“今日已记录”和问候语之间。

| 位置 | 固定文案 | 点击结果 |
| --- | --- | --- |
| 左侧 | 点击添加用药打卡 | 进入“添加药品打卡计划”，默认对象为当前长辈本人 |
| 右侧 | 点击添加身体数据 | 进入“添加身体数据”，默认对象为当前长辈本人 |

目的：长辈能快速添加自己需要打卡的药物；子女在得到长辈同意后，也可以拿长辈手机从这里协助录入计划和身体数据。

**初版建议：**问候语 → 左右快捷卡片 → 今日已记录摘要 → 当前任务与未按时提醒。该顺序是对“位于两者之间”的具体呈现建议，保留两张卡片在同一行且视觉均衡的要求。

两张卡片等宽、等高、间距一致，圆角适中，文字允许自然换行。图标只作辅助，文字是主要识别方式。标准字号点击区域至少 44 px 高，适老大字号至少 56 px 高；不要为显得“小”而缩小实际可点击范围。在窄屏和大字号下仍需清楚可点，必要时增加卡片高度。

“添加用药打卡”在此指创建未来需要打卡的药品计划；不表示点击后就已经服药。实际服药记录通过药物卡片或语音助手完成。

### 3.2 顶部时钟改为历史入口

用户所指图标是右上角“切换账号”图标左侧的时钟。当前代码中的用途是调整演示时间；v2 将其改成“查看历史打卡信息”。

点击后直接进入“用药信息 → 打卡记录”区域，不停留在“正在服用”，也不再弹出演示时间设置。保持当前长辈对象；初版建议默认定位今天，允许继续选择历史日期。为图标补充可访问名称“查看打卡记录”。

演示时间调节仍可作为原型验证工具保留，初版建议移至“我的 → 演示设置”。不能因此把原型固定日期改为系统日期。

### 3.3 当前任务与悬浮入口

当前时段仍是首页主体，每个药物单独成卡，显示计划剂量、提醒时间及可选餐前/餐后标签。未按时任务保持折叠提示，样式按第 11 节调整。

首页右下角保留的是第 6 节“语音助手”入口。原添加加号不与它重复堆叠。语音入口必须避开底部导航、安全区与当前任务按钮；卡通存钱罐也不放在同一悬浮位置。

## 4. 全局导航与加号路由

### 4.1 底部导航

| 顺序 | 长辈端 | 子女端 |
| ---: | --- | --- |
| 1 | 服药打卡 | 首页看板 |
| 2 | 用药信息 | 用药信息 |
| 3 | 身体数据 | 身体数据 |
| 4 | 我的 | 我的 |

角色可以改变首页名称和内容；字号模式只改变呈现尺度，不改变权限与业务含义。

### 4.2 添加入口映射

| 页面与入口 | 目标页面 | 跨类别二选一 |
| --- | --- | --- |
| 长辈首页左侧快捷卡片 | 添加药品打卡计划 | 不出现 |
| 长辈首页右侧快捷卡片 | 添加身体数据 | 不出现 |
| 用药信息右下角 `+` | 添加药品打卡计划 | 不出现 |
| 身体数据右下角 `+` | 添加身体数据 | 不出现 |
| 首页保留的加药 `+` 或加药动作 | 同一个添加药品打卡计划页面 | 不另造简化表单 |
| 语音助手中的“添加用药计划” | 同一个药品草稿及最终确认流程 | 先确定意图，再补齐字段 |

用户在第 4 项概括了“从首页或用药信息点击 + 进入计划”，与第 2 项长辈首页加号迁移一起理解：长辈首页用快捷卡片承接原加药功能。子女看板是否继续保留悬浮 `+` 尚未明确；初版建议保留一个直接加药入口，身体数据通过摘要卡内入口添加。

身体数据内部选择“血压/血脂/身体指标”仍然需要保留；这不属于被取消的“用药信息/身体数据”二选一。

对象规则沿用 v1：长辈固定为自己；子女只可给已授权的长辈添加，提交前清楚确认对象。初版建议带入看板当前长辈，在表单顶部允许重新选择；是否每次强制弹出对象选择器见待确认项 D13。无绑定对象时提供绑定入口，不创建悬空数据。

## 5. 添加药品打卡计划

### 5.1 三种平行录入模式

页面统一命名“添加药品打卡计划”，包含平行入口：`手动输入`、`拍照识别`、`AI语音创建`。

| 模式 | 用户输入 | 结果与边界 |
| --- | --- | --- |
| 手动输入 | 逐项填写药品计划 | 标准表单草稿 |
| 拍照识别 | 拍照或选择药盒、处方等图片 | 仅提取图片中可辨识的事实，形成可编辑草稿；缺失字段继续询问 |
| AI语音创建 | 按住说话，按引导回答 | 将回答填入同一份草稿；缺项继续追问，不绕过表单 |

**明确需求：**AI语音创建属于 PRO 功能，用不同但克制的颜色、标志或右上角 PRO 角标表现。初版建议使用现有柔和紫色与小型 `PRO` 标签，不使用大面积金色、夸张闪光或遮挡主流程的付费层。

三种方式共同使用一份字段定义和校验规则，切换模式保留已填写内容，不能悄悄覆盖用户修改。拍照、语音需要补齐手动输入中的所有适用字段；选填项可以明确留空，不能为了“填满”而虚构备注或餐前/餐后要求。

### 5.2 字段与必填规则

| 字段/字段组 | 标识与输入 | 校验或保留规则 |
| --- | --- | --- |
| 记录对象 | 必须明确；长辈端固定，子女端选择已授权长辈 | 属于合法家庭权限范围 |
| 药品名称 | `药品名称 *`；沿用现有字段 | 去除首尾空格后非空；不改名为药品搜索或其他概念 |
| 单次用量 | `单次用量（请输入数字） *` | 必填数字且大于 0；表示每顿吃几片、几粒或几包，不把单位输入此框 |
| 用量单位 | `用量单位 *`；可直接输入，也提供常用候选 | 必填；不能只允许下拉，也不能必须先选“其他”才能输入 |
| 服用时段 | `服用时段 *`；见四列表 | 至少勾选一个时段 |
| 每个已选时段的提醒时间 | 所选行必须有合法小时与分钟，默认值可满足必填 | 未勾选的行不生效，不要求用户填写其餐时或提醒 |
| 餐时 | 选填；餐前/餐后两段按钮 | 每行最多选一项，也允许都不选 |
| 开始日期 | `开始日期 *`；保留 v1 | 原型默认演示日；真实 APP 默认今天，可修改 |
| 服用周期 | `服用周期 *`；保留 v1 | 长期服用，或截至某日期 |
| 结束日期 | 选择“截至某日期”时出现 `结束日期 *` | 条件必填，不早于开始日期 |
| 服用备注 | 选填 | 由用户或实际处方信息提供；不由 AI 生成医疗建议 |
| 图片、录入方式和操作来源 | 图片按输入方式使用；来源由系统记录 | 区分手动、拍照、语音、模拟及家属协助，不伪造识别能力 |

“单次用量”允许合法小数，例如实际方案中的半片，不擅自改成正整数限制。单位候选可以沿用片、粒、袋、支、毫升、滴、喷、瓶，并补充“包”；用户直接输入的“包”不能未经确认替换成“袋”或自动换算剂量。

“每天几次”继续根据所选时段数自动计算。语音可以询问“一天几次”作为一致性核对；若回答每天两次却选择三个时段，必须澄清，不能生成互相矛盾的计划。

v1 的同一计划各时段相同剂量继续作为范围限制。用户若说“早一片、晚两片”，系统必须提示当前模型不能直接表达，保留信息并进入人工处理；不能静默合并成相同剂量。分时不同剂量、按需、隔日、每周服用仍需另行确认，不自动生成错误的每日计划。

### 5.3 四列表时段设置

**明确需求：**以列表形式呈现四列：第一列复选框，第二列时段，第三列提醒推送，第四列餐前/餐后选项。第四列表头初版建议使用简洁的“餐时”。

| 选择 | 时段及范围 | 提醒推送：默认时间，可调整 | 餐时：选填 |
| --- | --- | --- | --- |
| 复选框 | 早餐：05:00～11:00 | 08:00 | 餐前 / 餐后 |
| 复选框 | 午餐：11:00～16:00 | 12:30 | 餐前 / 餐后 |
| 复选框 | 晚餐：16:00～20:00 | 18:30 | 餐前 / 餐后 |
| 复选框 | 睡前：20:00～24:00 | 21:00 | 餐前 / 餐后 |

这些时段辅助长辈和子女录入其既有用药安排，不是系统推荐的医学服药窗口。不能由默认时间推断药品应餐前还是餐后。

**实现建议：**以左闭右开区间消除重叠，即早餐 `[05:00,11:00)`、午餐 `[11:00,16:00)`、晚餐 `[16:00,20:00)`、睡前 `[20:00,次日00:00)`。因此 11:00 属午餐、16:00 属晚餐、20:00 属睡前。此端点口径以及 00:00～05:00 如何处理须确认，见 D02。

提醒时间初版建议限制在对应时段内，选择界面说明范围并拦截越界；用户原话没有明确是否允许跨时段时间，必须在开发此限制前确认 D03。

### 5.4 小时/分钟调整

每行显示可编辑的小时和分钟，不让用户误以为默认时间固定不可修改。点击小时或分钟区域后，打开时间选择器，上下滑动对应滚轮选择时、分，确认后写回该行；取消则保留原值。

**明确需求：**不调整时使用默认时间，调整时使用用户选择的时间。iPhone 风格原型需能清楚演示滚轮交互；真实 APP 按目标系统提供等效体验。

**实现约束：**支持触摸、键盘、屏幕阅读器和桌面操作；提供可点击或可输入的等效操作，不能只依赖滑动手势。分钟步长用户未指定，初版建议 1 分钟。调整后回显明确的 `HH:mm`，不因滚轮外观隐藏最终数值。

通知开关与这些时间的关系见第 9 节：计划中保存提醒时刻，真正的提示声音、震动、横幅和锁屏展示服从用户的系统通知设置。

### 5.5 餐前/餐后分段按钮和药品标签

按钮有略带弧度但不过分圆润的轮廓，左右两半分别是“餐前”“餐后”。未选择时均为浅色；选择左半时左半用深色主色调、右半保持浅色，选择右半则相反。

每个时段独立选择。初版建议再次点击已选项即可取消，让用户回到不选；也可在可访问操作中提供“清除餐时”。不要让“选填”在选过一次后变成无法取消。

选中后，在“服药打卡”和“用药信息”对应药品后方显示带颜色的小圆角文字标签。若同一药物不同的时段餐时不同，标签必须跟随时段行，不能在药名旁显示互相冲突而无归属的两个标签。子女今日用药模块复用相同规则。

未选择时不显示空标签，也不自动补成餐后。标签须同时包含文字，不能只靠深浅颜色辨别。

### 5.6 AI语音创建：引导式收集

**明确需求：**进入语音模块后有明显的“按住说话”按钮，并明确提示用户应该说什么。用户提出了“提示后一次说完”与“AI逐步问答”两种设想；初版建议采用逐步问答，同时接受用户一次说出多个字段。

| 步骤 | 引导示例 | 写入与校验 |
| --- | --- | --- |
| 1 | “请说出药品名称，也可以照着药盒念。” | 药名；听不清时重说或手动修改，不猜测相似药名 |
| 2 | “每次吃多少？单位是片、粒、包，还是其他？” | 单次用量和单位，分别回显 |
| 3 | “按照您已有的用药安排，一天几次？” | 暂存用户描述，用于核对所选时段 |
| 4 | “请选择早餐、午餐、晚餐或睡前，可以选多个。” | 展示四列表，语音选择或点击选择 |
| 5 | “早餐提醒默认是早上八点，需要调整吗？” | 按已选时段逐一确认提醒时刻，可保留默认 |
| 6 | “有餐前或餐后的要求吗？没有可以不选。” | 各时段可选餐时，不擅自给建议 |
| 7 | “从哪天开始？长期记录，还是到某一天结束？” | 开始日期、周期、必要时的结束日期 |
| 8 | “还有需要写下的备注吗？” | 选填；允许直接跳过这一步询问 |
| 9 | “信息已经整理好，请核对下面的用药计划。” | 进入所有模式共用的核对页 |

界面呈现当前问题、已填写摘要、识别文字、可编辑字段和“重新说/改为手动”。AI 播报可暂停、重复或关闭；不能因老人漏听上一句而让流程卡住。

录音时显示“正在聆听”，松手后结束并处理；提供取消录音、取消本次创建。未获得麦克风许可、网络不可用或语音识别失败时说明具体情况，保留已有草稿并允许手动完成。没有真实语音服务的原型使用明确标记的模拟问答，不伪装已监听麦克风或识别真实声音。

### 5.7 所有入口统一核对与保存

共同流程为：填写或识别草稿 → 点击“核对用药计划” → 必填与格式校验 → 单一核对页 → 返回修改或确认创建 → 计划生效并生成适用任务。

核对页在一处完整展示：记录对象、药名、每次剂量和单位、每天次数、每个时段的时间范围/提醒时刻/餐时、开始日期、周期/结束日期、备注与来源。不能只核对药名而隐藏后续设置。

**明确需求：**手动、拍照和语音最后都必须到这一页让用户看清，再完成添加。此药品计划核对页初版采用明显的“确认创建”按钮；语音可以读出摘要，但未完成用户明确确认不能保存。第 6 节的实际服药记录可以通过同等明确的语音确认提交。

### 5.8 必填星号与醒目错误反馈

1. 所有实际必填项及条件必填项都加 `*`；初始星号保持界面现有灰色。预填的开始日期、周期、已选行提醒时间也不能漏掉必填标识。
2. 用户点击“核对用药计划”时，一次性检查所有适用字段。若不完整，不进入核对页，主动弹出醒目的错误提示层：`还有必填信息未完成，请填写标红的框后再核对用药计划。` 同时显示字段红框与就地错误说明，不能只显示普通行内提示来替代弹出反馈。
3. 缺失或无效字段显示与现有配色协调的红色边框；对应星号变红，并有就地文字说明，例如“请填写药品名称”“至少选择一个服用时段”。不把本来选填的餐时、备注标红。
4. 错误提示层应可被屏幕阅读器读出；用户关闭提示或点击“去完善”后，自动滚动并聚焦第一处错误，保留其他输入。提示层不能使焦点无法返回表单。
5. 修正后移除对应红框和错误文字；再次提交仍有遗漏时继续明确指出，全部通过后才进入核对。

初版建议复用现有 `--danger: #d85e65` 与 `--danger-soft: #fff0f0` 形成柔和珊瑚红提示；实际文字和边框需检查对比度，可调整深浅。错误提示不能只是一闪而过的 Toast，也不能仅由浏览器默认气泡代替。

## 6. 长辈端全局 AI 语音助手

### 6.1 入口、定位与意图选择

**明确需求：**在长辈“服药打卡”首页右下角设置语音助手，作为全局业务引导入口，与计划中的 AI语音创建相似，但能够处理更多意图。此处“全局”指能引导多类操作，不表示已经要求每个页面都增加悬浮按钮。

进入后 AI 主动问：“您想添加用药计划、记录已经服用的药、稍后提醒，还是记录身体数据？”同时显示文字和可点击选项。用户可以直接说出意图，不强制先读长菜单。需要补记时提供“补记用药”快捷选项。

AI 只帮助找到页面、采集信息和生成待确认记录，不自主决定药品、剂量、是否应服药或是否应停药。所有动作继续执行当前账号的权限检查。语音助手属于 PRO 方向，会员呈现见第 10.5 节；基础手动能力应保持可用。

### 6.2 意图一：添加用药计划

进入第 5.6 节逐步创建流程，采集药名、单次量和单位、每天次数与时段、每行提醒时刻、可选餐时、起止与周期、可选备注。不能因为从首页语音入口进入，就跳过手动表单的必填项或最终用药计划核对页。

### 6.3 意图二：记录今天实际服药

**明确需求：**引导用户说出今天什么时间、吃了什么药、吃了几片或几包。

| 环节 | 行为 |
| --- | --- |
| 询问 | “今天什么时候吃的？”“吃的是哪种药？”“实际吃了多少，单位是什么？” |
| 对应任务 | 在本人已有任务中匹配药品、日期、时段；同药多次或同名多计划时，让用户选择具体哪一次 |
| 展示核对 | 明确展示计划剂量与实际记录剂量、实际服药日期时间、此次对应任务；不能把计划剂量默认当作实际已吃剂量 |
| 确认 | 用户点击或明确说“确认记录”后保存；识别到“好像”“可能”等不确定表达时继续澄清 |
| 结果 | 更新首页、用药信息历史、子女看板；适用时显示“补记”或“延时服用”等准确说明 |

如果实际数量与计划不一致，如计划 2 片但用户说吃了 1 片，保留真实数量并标明“与计划记录不同”，不能自动补足为 2 片，也不建议现在再吃多少。是否计入全天完成、是否另设部分服用状态，见 D04。

如果用户说的是尚无计划的新药，原始需求没有定义临时服药记录模型。初版建议先说明“还没有找到对应计划”，提供手动核对或创建计划后选择关联任务；不能直接伪造已存在任务、猜测处方，或把这次服药静默变成长期每日计划，见 D05。

### 6.4 意图三：过一会儿再提醒

**明确需求：**用户现在还吃不了药，希望语音助手重新设定提醒时间。

流程：选择具体药品/当次任务 → 询问“多久以后，或几点再提醒？” → 将“半小时后”等转成明确日期时间并读出 → 用户确认 → 新增或更新该次稍后提醒。

**必须保持：**延后的是通知，不是原计划的应处理时段，也不是实际服药记录。已经超过原服药时段的任务继续显示“未按时打卡”，不能因设置稍后提醒变回正常待打卡、已完成或重新获得按时资格。

延后提醒要保留原时段、原提醒时刻、新提醒日期时间、修改者及时间。跨午夜时清楚读出“明天”，仍关联原来的那次任务，不将其自动挪到次日新任务。延后次数上限、跨日最长范围及是否仍发子女超时通知见 D06；未确定前不静默取消子女提醒。

提示文案初版建议：“已设置稍后提醒。本次原来的打卡时段不变，请按实际情况记录。”不使用“已为您调整用药方案”。

### 6.5 意图四：已过时间，补打卡

**明确需求：**用户想打卡但已过时间时，语音助手能帮助其在“未按时打卡”卡片内补记对应药物，而不是只允许查看。

列出可处理的未记录任务，逐条确认对应药物、日期和时段，再问实际是否服用、实际时间、数量和单位。核对后更新原事件，不重复创建一条已完成事件。

分别保存实际服药时间 `takenAt` 与提交记录时间 `recordedAt`。例如用户 13:00 说“早上八点已经吃了一片”，属于记录较晚，不应仅凭 13:00 的点击时间断言药吃晚了。

实际时间不确定时允许明确记录“实际时间不详”，不得默认等于现在或原定时间；能否在这种情况下认定按时、日历和奖励如何处理，列为 D04。是否允许补记前几天及可追溯期限见 D07；当天超时补记属于明确需求。

### 6.6 新记录后提醒仍未打卡的任务

**明确需求：**若用户忘了本次服用，进入助手后先记录其他新的药物打卡，助手在该记录成功后还要提示：`你有未打卡的记录`，并让用户自行回复。

初版建议只列出已经到期、需要补充事实的未记录任务，不拿尚未到时的晚餐任务制造催促。一次问一个任务，并展示药名、原时段、计划剂量和日期。中止追问不撤销刚刚已经成功保存的新记录，也不替用户选择状态。

| 用户要求的回复选项 | 目标操作 | 防止误记的约束 |
| --- | --- | --- |
| 已经服用 | 补记该任务为已服用 | 继续询问实际服药时间、数量、单位，核对后提交 |
| 没有按时服用 | 用户明确希望映射到“未服用” | 这句话也可能表示“晚些时候已吃”，先澄清是否至今仍未服用，不能仅按字面直接误记 |
| 本次无需服用 | 记录对应药物当天当次为本次无需服用 | 确认是用户的实际安排，仅作用于当次，不停用整条计划 |

“没有按时服用”的建议追问：`这一次到现在还没有服用，还是后来已经服用了？` 回答“到现在还没有”时，按用户要求写入“未服用”；回答后来已经服用时，转入补记实际服药。这样保留用户指定的未服用分支，也不把延时服药与未服药混淆。

用户暂不回答时，保留“未按时打卡/事实未确认”。“本次无需服用”只能由用户声明；AI 不根据药名、身体数值或奖励条件选择它。

### 6.7 意图五：记录身体数据

先问记录血压、血脂还是身体指标，然后复用相应表单与“核对测量记录”页。

| 类型 | 引导内容 | 应形成的记录 |
| --- | --- | --- |
| 血压 | “高压，也就是收缩压是多少？”“低压，也就是舒张压是多少？”“哪天、几点测的？”“大夫有没有交代，或有其他备注吗？”；脉搏有则补充 | 收缩压、舒张压、测量日期时间、可选脉搏、用户转述的医生交代或其他备注 |
| 血脂 | “报告上有哪些指标？”依次收集总胆固醇、甘油三酯、HDL-C、LDL-C 等已有项目；问每项单位、检测日期时间与可选备注 | 原始指标名、数值、单位、检测时间；没有提供的项目不填零、不推测 |
| 身体指标 | “要记录身高、体重，还是体脂率？”继续问数值、单位、测量日期时间与备注 | 对应测量记录；身高体重有效时自动计算 BMI |

“高压/低压”是允许的口语别称，界面同时回显规范名称。医生交代只记录用户转述，可标“用户转述”，不改写成 AI 的医生建议或验证过的处方。

支持分次回答及一次说出多项；遇到数值、单位、日期不清或自相矛盾时追问。语音输入也必须经过字段校验、用户核对和确认保存；不能仅因是语音识别就放宽血压必填等规则。

### 6.8 语音通用状态与可访问性

至少覆盖：待开始、正在聆听、识别中、正在询问、待核对、已保存、已取消、无权限、识别失败。实时让用户知道有没有在录音，不常驻后台监听。

按住说话之外，应有便于无法持续按压者使用的等效方式，如点按开始/结束。显示识别文字、当前字段及修正入口；所有问题都有文字版本，所有语音操作都有手动替代。PRO 未开通或服务不可用时，不丢草稿，也不阻断基础手动打卡与查看历史。

## 7. 打卡事实、时段、历史与完成口径

### 7.1 全局用语替换

**明确需求：**将全局面向用户的 `跳过本次` 改为 `本次无需服用`。覆盖：首页按钮、未按时卡片、语音问题与回复、确认弹窗、历史、状态徽标、通知落地页、帮助文案及新版本示例。

已保存为内部 `skipped` 的旧数据可以保留兼容键，但显示为“本次无需服用”。旧事件原含义和操作来源仍要保留，不能把历史“跳过”追溯解释成医生已经确认无需服药。v1 历史文档和本文件原文附录保留旧词用于追踪，不属于面向用户的旧文案遗漏。

建议确认提示：“仅记录您本次的实际安排，不代表系统建议停药。”选项只影响当次，不删除药物、不停用后续计划。

### 7.2 三类时间不能混在一起

| 时间 | 含义 | 哪些动作可改变 |
| --- | --- | --- |
| 计划时段与原提醒时间 | 该药物本次原本安排在何时记录、何时通知 | 正式编辑计划按生效规则改变后续任务；历史应保留快照 |
| 实际服药时间 | 用户声明真正吃药的时间 | 由本人确认录入或更正，不能从通知送达时间推断 |
| 记录提交时间 | 用户这次保存记录的时间 | 系统记录；补记时通常晚于实际服药时间 |
| 稍后提醒时间 | 针对同一次任务重新通知的时刻 | 用户明确设置；不能改掉上述事实 |

### 7.3 状态规则与超时分歧

| 状态/事实 | 显示意义 | 禁止推断 |
| --- | --- | --- |
| 待打卡 | 尚未记录，尚未达到选定超时条件 | 不代表未服药 |
| 未按时打卡 | 达到超时条件但仍未记录，是需要核实的状态 | 不自动写成未服用或漏服 |
| 已服用 | 用户确认实际服药 | 不等于药品、剂量已经经过医学验证 |
| 延时服用 | 按已确定的时间规则和用户实际服药时间判断 | 不能仅因补记较晚就认定服药较晚 |
| 未服用 | 用户明确声明该日该次未服用 | 不能由计时器、子女提醒或 AI 猜测生成 |
| 本次无需服用 | 用户确认该日该次不需服用 | 不等于停用计划，不表示 AI 建议停药 |
| 补记 | 对逾期未记录事件进行事后录入的操作标记 | 补记本身不是实际服药是否按时的结论 |

**明确要求的下限：**一旦超过该药物原计划所属时段且仍无记录，必须继续显示未按时打卡；延后提醒不能清除它。

**与 v1 的冲突：**原规则为提醒时间后 60 分钟未操作即逾期；用户新给出较宽时段。早餐 08:00 提醒，v1 在 09:00 逾期；若按早餐时段结束判断则在 11:00。两者不能未经说明同时称为唯一标准。

**初版建议：**按时段结束判断“未按时打卡”，提醒时刻只控制提示；若还需较早提醒子女，可另设清楚命名的未响应阈值。逾期阈值、是否保留 60 分钟提醒以及“延时服用”判定必须通过 D02 确认后写入统一配置，原型和真实 APP 同一口径。

### 7.4 全天完成、日历星星与奖励资格

用户明确说的是“每一天打卡如果全部完成”“每打完一整天的卡”。以下问题尚未定义：是否只算用药；已声明未服用、本次无需服用是否算处理完成；补打是否算；没有任务的日子是否算。

**实现约束：**分别保存和计算“任务已处理”“实际已服用”“日历符合完成条件”“奖励符合条件”。它们可以在产品决定后采用一致规则，但不能因用同一个布尔值而错误宣称全部已服药。

| 场景 | 已知事实 | 星星/奖励口径 |
| --- | --- | --- |
| 有任务且每次均按要求记录已服用 | 有完整的本人记录 | 可用于典型完成日演示；实际生产仍依确认后的统一规则 |
| 有一项仍未记录 | 当天未全部处理 | 不得显示全完成星星，也不得发放全完成奖励 |
| 明确未服用 | 有真实记录，但未实际服用 | 待确认，不能把它计为“全部服药” |
| 本次无需服用 | 已声明该次无需 | 待确认；不能为了奖励诱导服用本来无需服用的药 |
| 超时补记或实际时间不详 | 记录已补充，时间事实不同 | 是否点星、可追溯多久及是否补发待确认 |
| 无任务日 | 没有应打卡事项 | 初版建议不点星、不发钱，须确定最终规则 |
| 身体数据未录入 | 用户未定义每日身体数据任务 | 不自行加入用药日完成分母；是否纳入奖励范围待确认 |
| 当天新增/停用计划 | 当日应处理任务可能改变 | 生效时间与是否重算完成条件待确认，不回写历史处方事实 |

**合规设计建议：**奖励鼓励如实完成记录，不以“宣称自己吃药”换取更多钱，也不因选择本次无需服用而诱导错误用药。是否采用“全部当日应处理任务均有明确记录”的完成口径，应由产品与医学/合规评审确认；这不是对用户金额规则的变更。

星星由记录实时重算，金钱则需要可审计的结算，不允许刷新页面重新随机。日历规则与奖励规则如有差异，必须在产品规则里清楚解释。任何尚未确定的边界场景，原型应作为待验证案例，不能据此开通真实资金发放。

## 8. 身体数据与自动 BMI

### 8.1 延续的记录能力

保留血压、血脂、身体指标分类、手动录入、实际测量时间、备注、历史和血压最近 7 次趋势。加号直达身体数据录入，语音与手动共用校验和确认。

血压至少需要收缩压、舒张压及测量时间，脉搏选填；原有数值格式校验继续保留。血脂、身体指标可记录已有项目，至少有一项有效测量值，不能因为没有完整四项血脂就填零补齐。

### 8.2 BMI 计算和呈现

**明确需求：**根据长辈身高与体重自动计算，在“身体数据 → 身体指标”板块显示，不让长辈自行填写 BMI。

公式：`BMI = 体重（kg）÷ [身高（cm）÷ 100]²`，单位为 `kg/m²`。例如身高 160 cm、体重 60 kg，BMI 为 23.4375，初版建议显示 `23.4`，内部保留计算精度。

| 情况 | 处理 |
| --- | --- |
| 同次记录含有效身高、体重 | 自动计算并展示“BMI · 自动计算” |
| 只有体重、档案中有旧身高 | 初版建议引用该体重测量时点之前最近一条有效身高，显示身高来源日期；是否采用及有效期见 D08 |
| 缺少身高或体重 | 显示“补充身高和体重后自动计算”，不输出 0、NaN、Infinity 或伪造结果 |
| 零、负值、非数字、单位不明确 | 明确指出问题，阻止使用无效输入计算；不静默把斤当 kg、米当 cm |
| 历史体重与后来新增身高 | 不将未来测量的身高静默用于过去 BMI；引用或更正规则要可追溯 |

BMI 随对应数据更新，可出现在子女看板摘要中。v2 只计算并显示数值，不新增胖瘦分级、疾病诊断、减重或调药建议。老年人 BMI 的临床解释受体成分等因素影响，本版本不承担该解释。

## 9. APP 形态与三类真实手机通知

### 9.1 APP 方向与 iPhone 呈现

**明确需求：**希望做成 APP，当前先呈现苹果手机的样子，核心理由是 APP 更直接联动系统通知。iOS、HarmonyOS、Android 都需要考虑适配，提醒功能必须实现。

外观初版建议采用居中的 iPhone 风格预览框和安全区；手机实际使用时页面铺满可用屏幕，避免再套一层缩小的手机外壳。手机样式只代表视觉展示，不能标记为已生成可安装 iOS APP。

**实现建议：**先完成 iPhone 风格原型，再选择真实 APP 技术栈与首发平台。可评估原生或跨平台方案，但用户未选择 Flutter、React Native、WebView 容器等技术；本文不替用户锁定框架。网页加外壳本身不能解决后台任务、跨设备授权与系统通知。

### 9.2 平台差异与最小实现条件

| 平台 | 本机到点提醒 | 跨设备远程提醒 | 验证重点 |
| --- | --- | --- | --- |
| iOS | 通过系统 User Notifications 安排本地通知，申请并检查授权 | 服务端与 APNs、设备 token 及账号绑定配合 | 系统授权、声音/横幅/锁屏设置、专注模式、后台/离线/重装与计划更新；不能承诺绕过系统限制 [S1][S2] |
| Android | 系统通知与定时机制；根据系统版本评估闹钟权限与后台限制 | 使用适合发行地区和机型的推送通道，不能假设所有手机都有可用 GMS | Android 13+ 通知运行时权限、适用版本的精确闹钟权限、厂商省电设置、重启恢复和通道开关 [S3][S4] |
| HarmonyOS | 评估目标版本 Notification Kit 与后台提醒能力 | 评估对应版本 Push Kit、设备 token 和云侧服务 | 区分 HarmonyOS 原生应用与兼容 Android 的设备环境；逐版本核对权限、后台提醒和推送能力 [S5] |

HarmonyOS 官方入口已定位，但部分正文需动态加载；本文件没有验证具体 API 签名、配额或所有版本支持范围，实施时必须针对选定 SDK 再核对。不得把鸿蒙设备统一视为 Android，也不得假设一个插件自动覆盖三套系统。

具体支持的系统最低版本、设备范围与首发平台为 D09。各系统分别验收，iPhone 风格界面不能代替其他平台实测。

### 9.3 三类提醒：不可遗漏

| 编号 | 触发条件与发起者 | 接收端 | 必须体现的内容与动作 |
| --- | --- | --- | --- |
| N1 到点用药提醒 | 已确认计划中已选时段到达用户设定的提醒时间；默认或调整后时间均可 | 长辈手机 | 时段、药名、计划单次剂量与单位；点击进入对应任务 |
| N2 超时未打卡提醒 | 达到统一超时条件后，服务端仍未收到该次打卡记录 | 已获授权的子女手机 | 长辈对象、日期/时段、药名、剂量、尚未收到记录的事实；进入相应看板或历史任务 |
| N3 子女主动远程提醒 | 子女发现未打卡，点击“提醒 TA”或对应按钮 | 长辈手机 | 来自谁的提醒、哪天哪时段、哪种药、计划几片/几粒/几包等；进入该次记录核实页 |

每个通知关联明确的任务 ID、档案和日期，不能只是“记得吃药”的泛化消息。多位长辈、多位子女的接收授权和防重复提醒规则需配置清楚。

用户原始设想要求 N3 显示“什么时段的什么药物没有按时服用规定的剂量”。**保留时段、药名和明确剂量，但根据已知事实调整表述**：尚未打卡时只能说“尚未记录”，不能断言已知没吃。

通知详情的建议示例：`小李提醒您核对：今天午餐时段的示例药 A，计划每次 2 包，12:30 提醒，目前尚未记录。若已服用，请按实际情况补记。` 这里的药名和剂量均为演示占位，不提供具体用药建议。

若已有本人明确“未服用”记录，详情可以据实显示该状态；不把后续提醒写成“现在必须补吃”。用户稍后补记时，通知打开后的页面必须读取最新状态，避免旧通知诱发重复记录或重复服药。

### 9.4 通知设置与隐私

**明确需求：**用户开启消息通知后，按其对本应用的通知设置在设定时间提醒长辈。应用应提供系统授权状态、应用内提醒开关及进入系统设置的入口。

声音、震动、横幅、锁屏预览等服从操作系统与用户配置；通知许可并不意味着 APP 能强制开启声音、绕过勿扰/专注模式或突破后台限制。不把应用内开关显示“开”当作系统已授权。

**隐私约束：**药名、剂量和健康信息可能暴露隐私。初版建议锁屏默认显示简洁提示，如“您有一条用药记录提醒”，解锁进入后显示完整时段、药名与剂量；用户可自主选择详细通知预览。此设置不能删除 APP 内必须显示的完整内容。云推送载荷尽量只带不暴露健康详情的事件引用，由已授权客户端取得正文。

拒绝通知后仍能查看与手动记录，界面说明“系统通知未开启”并允许稍后启用。不强迫用户开通知来换取基本记录能力或提高奖励。

### 9.5 本地通知与服务端协同

**建议架构：**N1 优先交给手机操作系统安排本地通知；N2 由后端依据共享任务和记录状态判断；N3 由子女端发起，经后端鉴权，再由系统推送通道到长辈端。云端必须有真实账号、家庭授权、设备绑定、任务调度与推送凭据管理。

不依赖网页 `setTimeout`、前台轮询或要求 APP 一直亮屏来完成真实通知。服务端不能因为长辈手机网络断开，就确定长辈没吃药；离线时文案应表述为“暂未收到记录”，并考虑最后同步时间。

计划修改、停用、任务已记录、撤销或重装后的提醒安排要更新，过期本地通知与远程消息需要取消、抑制或在打开时纠正。同一任务的多个通道应去重，不能本地、远程、重试各弹一遍。

### 9.6 可靠性与实机验收

至少区分：已创建提醒任务、推送服务已受理、设备送达（仅在通道确有回执时）、用户打开、用户完成记录、发送失败/过期/状态未知。“发送成功”不代表设备收到，更不代表老人服药；APNs 官方资料也明确远程通知送达不保证 [S2]。

真实 APP 必须验证：前台、后台、锁屏、断网恢复、关通知、重启、系统限制、重复点击提醒、重复投递、设备 token 变化、计划修改、解绑家庭、切换设备和跨日。针对用户/系统终止进程的行为按平台实际限制记录，不能在没有结果时笼统宣称支持。

N1 应在受支持且权限正常的设备上按配置触发；N2/N3 必须由两个独立账号、不同真实设备完成端到端验证。延迟目标、测量样本与失败容忍范围见 D10，须在发布前量化，不在本文件虚构“100%准时”。

失败时保留提醒记录和可解释状态；适用时有限重试，超过时效停止发送。没有设备回执的通道显示“结果未知”，不伪造“已送达”。这三类提醒不是紧急救援系统，本次范围不新增电话、短信或 SOS 联动。

## 10. 卡通存钱罐、家庭奖励与 PRO

### 10.1 陪伴形象与位置

**明确需求：**形象主要出现在长辈端，有鼻子、有脸、可爱喜庆，希望长辈喜欢打开 APP。四个候选完整保留：

1. 抱着元宝的小孩。
2. 可爱的小狗。
3. 可爱的小猫。
4. 财神爷形象。

先做静态形象，未来再考虑动态。具体形象和位置用户未定，不能擅自宣布四选一已经完成。

**初版建议：**先用一只抱小元宝的喜庆小猫作为静态评审方向，或用“待选择形象”的占位；放在长辈首页“今日已记录”摘要的右侧小区域，搭配“我的存钱罐”。大字号空间不足时放在摘要下面一条紧凑横卡。它与问候、快捷卡片协调，不遮盖药品、不占用语音助手右下角位置。

点击形象进入存钱罐页面或弹层，直接看见因为打卡已积攒多少钱。形象本身是有名称的可点击控件，并提供文字入口，不能仅靠用户猜测卡通可点。原型使用原创或有明确授权的素材。

### 10.2 用户明确给定的奖励规则

| 项目 | 必须保留的要求 |
| --- | --- |
| 触发 | 长辈每完成一整天的全部打卡，获得一次随机金额奖励；不是每吃一种药就奖励一次 |
| 每日金额 | 随机 `0.1～1 元`，即 `0.10～1.00 元`；不能擅自改成积分或固定金额 |
| 提现门槛 | 累计攒够 `10 元`可提现 |
| 月末规则 | 每月底不清零，已积攒金额可以跨月继续累计 |
| 主要资金来源 | 子女端充值提供家庭奖励资金 |
| 目标 | 让长辈愿意持续使用，有轻量陪伴和家庭关怀感 |

随机分布、金额精度、奖励发放时点与每日完成口径未指定。不能把“随机”未经说明解释为 0.10 到 1.00 每分钱等概率，也不能擅自承诺平均每天收入。

### 10.3 存钱罐与子女充值页面

**初版建议：**存钱罐显示累计获得、当前可提现余额、待入账/提现中金额（存在时）、距 10 元门槛还差多少，以及逐笔奖励和提现记录。各余额必须有明确含义，累计获得不等于可提现余额。

子女端可从所选长辈的家庭奖励入口充值，并清楚看见为谁充值、金额、可用奖励资金及流水。充值目标是家庭资金池还是某位长辈、多个子女如何共同出资，列为 D11。

奖励资金、长辈已获得权益、提现申请和会员费用分账展示。不能把购买 PRO 的金额自动当作给长辈的奖励资金；会员到期不应把已获得的奖励清零。

### 10.4 资金、奖励与提现的实现条件

**原型要求：**用模拟余额、模拟充值、模拟提现演示流程，显眼标注“演示金额，不可真实提现”。原型不能收款、扣款或显示已经打款到真实账户。

**真实资金上线条件：**先确定运营主体、发行地区、实际资金流、支付/发放服务商及所需资质；通过银行或具备对应资质的机构处理适用的真实资金环节。需评估平台是否涉及代收代付、储值、资金归集等业务，不能默认接入持牌通道后其他环节就都合规。

| 问题 | 需要明确的规则 |
| --- | --- |
| 重复领奖 | 同一长辈同一日期只有一次有效奖励；刷新、切账号、换设备、重复请求不能重抽或再发 |
| 原始记录更正 | 点星可以重算，已发奖励如何处理必须有规则与审计，不能悄悄扣已到账现金 |
| 资金不足 | 明确由谁补足、是否待入账；不能显示已赚到但实际上没有资金支持的可提现余额 |
| 提现 | 到 10 元可申请；去向、身份核验、到账时限、失败处理、手续费及是否全额提取均须事先明示 |
| 充值退款 | 明确未发奖励资金的退回规则、原路退回条件、绑定解除及账号注销处理 |
| 随机机制 | 明示金额范围、抽取规则与用户权益；实际账本使用整数分并由可信服务端结算 |
| 资金对账 | 充值、奖励占用/入账、提现冻结、成功/失败、退款相互可追溯，不只保存可修改的余额数字 |
| 月末与会员到期 | 按用户要求月末不清零；不能用会员到期绕过该要求清除余额 |

随机现金、付费资格与可提现权益的组合，需要按实际模式评估促销、抽奖、赌博风险、支付规则、税务和消费者权益；本文不作“已合法”或“必然属于赌博”的判断。不得增加花钱重抽、用会员等级提高药物相关奖金、推荐多吃药赚更多或夸大保收益等机制。

奖励不能成为诱导补服、多服、隐瞒未服药或篡改记录的动力。基础记录、是否无需服用及对事实的更正，不应被设计成财务惩罚；具体结算政策仍需产品与合规一起确定。

### 10.5 PRO 与“我的”页面

**明确需求：**PRO 主要承载 AI 语音助手能力，药品录入中的“AI语音创建”显示 PRO 属性。老人和子女端的“我的”都预留会员开通区域，呈现位置和基础样式一致。

用户的经营倾向是按月/按季/按年，盈利模式尚未定稿。初版可展示 `月度 / 季度 / 年度`方案占位，不能编造售价、折扣、自动续费规则或宣布所有套餐已可售。

| 内容 | 初版建议与待定边界 |
| --- | --- |
| 会员卡片 | “AI语音助手 PRO”、简短权益、会员状态和查看方案入口 |
| 核心权益 | AI语音创建及全局 AI 语音引导；是否覆盖所有身体数据语音及家庭成员需确认 |
| 基础功能 | 建议手动录入、打卡、查历史及三类基础提醒不受会员限制；会员具体边界见 D12 |
| 支付 | 真实 iOS 数字功能订阅按适用 App Store 内购等规则设计；地区例外、其他平台支付方式须分别核对 [S6] |
| 自动续费 | 若采用，需要明示周期、价格、续费方式、取消入口和恢复购买；不默认悄悄开通 |
| 资金区分 | AI 会员订阅与子女给长辈充值是不同交易，不混用账本或把订阅余额兑换现金 |

“两端相同”指双方都有会员入口与一致的基本呈现，不代表已决定同一家庭买一次就所有人享有 PRO；权益归属、子女代购及家庭共享仍为待确认。

## 11. 视觉修复与适老可访问性

### 11.1 加号相关图标形变与错位

**用户反馈：**目前点开任何一个加号，用药信息和身体数据的图标会畸变、形状不正常或位置不正确。v2 要统一修复，不能只修某一个页面的截图。

**实现检查建议：**检查 SVG 的 `viewBox`、宽高比、容器宽高、居中方式、flex 压缩和继承样式；主体图标、尾部箭头、复选框和按钮分别管理尺寸。现有 SVG symbol 与 `icon()` helper 应继续统一复用，不通过扭曲图标填满容器。

检查范围包括：两端各页 `+`、长辈首页快捷卡片、用药表单三种模式、身体数据类型选择、核对页及原型中仍保留的快捷添加组件。即使跨类别二选一入口被取消，也要检查迁移后的图标，没有使用的旧入口不必为演示而继续暴露。

### 11.2 未按时打卡提示红点

**明确需求：**未按时打卡提醒不再用 `⚠️` 警告三角形，改为类似红绿灯红灯的小圆红点，可以闪烁。

红点与“未按时打卡提醒（N）”文字共同展示；点是圆形，大小克制。折叠/展开时只有独立箭头可以旋转，红点和标题不旋转、不翻转、不变形。

**可访问性约束：**闪烁不是唯一提示，也不是强制动画。初版建议慢速轻微明暗变化，开启“减少动态效果”时保持静态；避免快速闪烁，不使用每秒三次或更快的闪动。正常不播放刺耳声音。红点表达待核实记录，不代表健康风险诊断。

### 11.3 整体版式

| 场景 | 要求 |
| --- | --- |
| 主配色 | 沿用现有健康绿、白色卡片、柔和背景；黄色星星、紫色 PRO、珊瑚红错误提示各有明确用途 |
| 圆角 | 快捷卡片柔和、餐时分段按钮稍克制、状态标签紧凑，不把所有控件做成同一种大胶囊 |
| 图标 | 固定比例，居中，无压扁/拉长；不能被长文字挤歪 |
| 时段四列表 | 保留四列对应关系；范围可换行，时间和餐时可点击；大字号时增加行高，不靠缩小文字硬塞 |
| 日历 | 日期与星星都可辨认，七列不溢出；非完成日无日期底色 |
| 悬浮按钮 | 语音入口与底部导航、安全区不重叠，不遮住最下方药物操作 |
| 卡通形象 | 小范围、静态先行，与主操作保持层级，不让首页拥挤 |
| 验证视口 | 至少窄屏移动视口、常见 iPhone 尺寸及桌面预览；每种都检查标准字号和适老大字号 |

**技术可访问性要求：**使用语义按钮、输入、标签与列表；保留键盘焦点、弹窗关闭后焦点恢复、`aria-label`、`aria-live` 与背景 `inert` 行为。必填、错误、完成、PRO 和当前餐时均需文字或可访问状态，不能只依赖颜色。

## 12. 数据、权限与迁移要求

### 12.1 新字段必须与业务含义对应

以下是满足需求的概念字段建议，不是已确定的数据库建表或本轮代码改动。

| 对象 | v2 需要表达的信息 |
| --- | --- |
| 药品计划 | 所属长辈、药名、单次量/单位、开始与结束/周期、来源、备注；每个所选时段的时段键、提醒时刻、可空餐时 |
| 当次任务 | 计划关联、长辈、所属业务日期、原时段与截止、当时的药名/剂量/餐时快照、计划版本、有效性 |
| 服药记录 | 关联任务、本人声明状态、实际服药量/单位、实际服药日期时间或明确不详、记录时间、操作者、输入方式、补记和更正痕迹 |
| 延后提醒 | 原任务、原时刻、新时刻、发起人、创建时间、当前有效性；不修改实际服药事实 |
| 身体数据 | 指标名、数值、单位、测量时间、备注、来源；BMI 所用身高与体重记录引用 |
| 语音草稿 | 当前意图、已采集字段、待补字段、用户核对状态；原始音频按最小化策略处理 |
| 设备与通知 | 账号/设备绑定、平台、有效 token、授权状态、通知类别、事件引用、发送尝试、结果与到期时间 |
| 每日完成 | 日期、长辈、应处理任务集合、使用的完成规则及版本、判定结果；不能只存永久星星而丢失依据 |
| 奖励流水 | 日期与资格依据、金额（整数分）、唯一奖励标识、资金来源、待入账/已入账/冲正记录 |
| 充值与提现 | 订单、交易状态、余额冻结、支付渠道关联、退款/失败记录与对账信息 |
| 会员 | 权益主体、方案、有效期、平台订单/权益验证状态，与奖励账本分离 |

### 12.2 权限保持与家属协助

| 行为 | 长辈 | 子女 |
| --- | --- | --- |
| 查看和记录自己的数据 | 可以 | 不适用 |
| 查看长辈档案与协助添加计划/测量 | 自己的档案 | 仅已授权长辈，保存前确认对象 |
| 声明已服用、未服用或本次无需服用 | 由长辈本人确认 | 延续 v1，不开放远程代打 |
| 语音更改打卡事实 | 与本人手动记录相同权限 | 不能通过语音绕过子女权限 |
| 发起家庭远程提醒 | 接收与处理自己的提醒 | 仅对有权限的长辈和任务发起 |
| 子女拿长辈手机协助录入 | 长辈同意后可以协助添加 | 不因持有手机就获得代声明服药事实的产品授权 |

长辈手机上的实际操作者不可能仅靠语音或账号自动准确识别。若需要标记“家属协助”，应让用户明确选择并记录来源，不假称系统已判断说话者身份。用户本次允许的协助添加行为不能被写成子女可远程代打卡。

### 12.3 原型数据兼容与真实服务迁移

当前原型继续走 `commit(change)`、`validData()` 与统一转义；扩展字段时同步默认数据、创建表单、核对页、列表、历史和恢复演示数据。用户输入的药名、单位、备注、识别文字等进入 HTML 前均要转义。

需为 v1 存档增加有版本的迁移或明确的降级策略。旧“早餐后/午餐后/晚餐后”与新“早餐/午餐/晚餐 + 可选餐时”不是单纯同名字段；不得在缺乏原始证据时把旧系统默认名称迁移成用户已经确认“餐后服用”。初版建议标记旧来源、提示核对餐时，未核对前不新增医学含义。

固定演示日不变；可提供本月历史种子数据用于星星演示，但不得称为真实历史。无效存档、空数据、恢复初始数据、旧枚举显示和重复任务都应验证。

真实 APP 的跨设备状态与资金必须由服务端鉴权和校验，不能以本地存储权限或余额为准。离线写入要有唯一请求标识、可见同步状态和冲突处理；绑定撤销后停止新数据访问及相关通知。已发生的历史更正、通知和资金记录按合法留存规则审计，不通过删除历史来掩盖差异。

## 13. 医疗、隐私、AI 与运营边界

### 13.1 医疗相关边界

1. 产品记录用户既有方案与实际情况，不诊断、不开药、不推荐药物、不调整剂量、不建议停药或补服。时段、提醒和餐时由用户依据既有处方/安排确认。
2. OCR 与语音只能辅助录入；低置信、缺失、歧义或与人工输入冲突时必须澄清，不用模型常识补药名、剂量或疗程。
3. “未按时打卡”是记录问题，不等于漏服；“本次无需服用”是用户声明，不等于系统停药意见；BMI 是计算值，不是诊断。
4. 身体数据、通知或奖励不能自动触发处方变化。v2 不引入真实设备监测、疾病预警、紧急救援或医疗专业服务。

原型保留可见说明：`仅供演示，不提供医疗建议，不用于真实用药决策。` 真实产品也须说明适用范围与责任，并根据功能实际用途完成相应合规评估；一条免责声明不能代替必要的医疗安全设计。

### 13.2 健康数据和家庭共享

依据《个人信息保护法》，医疗健康、金融账户属于敏感个人信息；处理应有特定目的、充分必要性及严格措施，并依法取得单独同意、告知必要性与权益影响。处理敏感个人信息等场景应事前开展个人信息保护影响评估 [S7]。

需要分别说明：保存哪些健康资料、哪些子女可看、是否会发含详情的通知、是否使用云端语音/OCR、由哪些服务方处理、保存多久、如何撤回和删除。家庭绑定、麦克风权限、通知权限和会员购买不是相互替代的授权。

长辈能查看和撤销家庭共享；撤销后停止后续访问和推送。提供更正、导出、删除/注销和投诉渠道，依法需继续保存的交易或审计数据说明理由与范围。不能承诺“注销后立即删除所有法定必须留存资料”。

### 13.3 语音、照片与 AI

明确录音开始和结束，不后台持续监听。优先只保存用户核对后的结构化信息，原始音频不默认长期保存；确需保留时说明用途、期限和删除方式。涉及可识别个人的录音也属于个人信息，不能因它是语音输入就排除保护。

原型图片仍只本地预览，不上传，也不把临时图片 URL 写入持久存储。真实 OCR/语音服务若上传资料，须另行告知并满足相应授权、委托处理和安全要求；不默认允许供应商将健康数据用于模型训练。若涉及数据出境，另行评估适用条件，而不是凭用户点麦克风许可视为同意出境。

不以声音推断健康状况、年龄、身份或金融资格；不新增声纹认证。AI 输出的药品计划和测量值都能回看、更正，保留人工确认步骤。

### 13.4 金额、订阅与用户权益

充值奖励、随机现金和会员订阅分别明确规则与责任，展示原型金额时标记模拟，真实开通前完成第 10 节条件。会员定价、自动续费、退款、奖励资金不足及提现失败不能藏在难以阅读的长条款里。

不能将开通 PRO、充值或购买抽奖机会设置为提高药物相关现金回报的必要条件。不得以“快去多吃一片就能领钱”等语言引导。卡通形象不冒充医护人员，不以财神或元宝暗示保证收益。

真实发布前应按服务地区、平台及实际功能检查应用备案/上架、隐私标签或数据安全申报、数字订阅、支付资金业务及可能适用的 AI 服务要求。本文提供设计边界与核查事项，不宣称已完成法律意见或资质审核。

## 14. 验收清单与可重复场景

以下全部是**后续实现的待验收项**，本轮只验收文档完整性。勾选必须有实际验证依据，不能因为文字已经写入就视为功能完成。

### 14.1 页面与用药录入

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A01 | 切换张阿姨与小李账号 | 长辈首项为服药打卡，子女为首页看板；同一长辈数据一致 |
| A02 | 查看本月含完成、未完成、未来日期的日历 | 只有符合规则的完成日期叠加黄色星星；日期清楚；未完成日期无背景、无星星 |
| A03 | 子女切换长辈并看今日用药 | 对象、任务、剂量、时段、餐时与状态一起更新；不能代打 |
| A04 | 今日血压、旧血脂、缺失身体指标混合 | 每类显示自己的实际测量时间；今日/最近一次不混淆；空值不填零 |
| A05 | 长辈首页标准/大字号 | 两张圆角卡片在问候与今日已记录之间左右分布，原文案完整、均衡可点 |
| A06 | 从长辈手机协助添加计划 | 默认对象为本人，最终核对后创建；不自动变成已服用 |
| A07 | 点账号切换左侧时钟 | 直接进入用药信息的打卡记录区域，保持对象，不出现演示时间弹窗 |
| A08 | 分别点用药信息、身体数据加号 | 直接进入对应表单，没有用药/身体二选一；数据内部类型选择仍可用 |
| A09 | 查看药名、单次量、单位 | 药名沿用；标签为“单次用量（请输入数字）”；单位支持直接输入“包”等自定义值 |
| A10 | 查看时段表四列四行 | 复选框、时段、提醒推送、餐时对齐；四个范围与 08:00/12:30/18:30/21:00 默认值正确 |
| A11 | 调整提醒的小时与分钟 | 可点对应区域上下滑动滚轮，确认回显，取消保留原值；未调整使用默认时间 |
| A12 | 选择/取消餐前和餐后 | 单行互斥且可都不选；深色选中、浅色未选；标签在首页和用药信息对应药物时段后显示 |
| A13 | 手动/拍照/AI语音创建切换 | 三种平行入口、同一草稿和校验；PRO 标记克制可辨认，不丢已填内容 |
| A14 | 语音逐步创建一条多时段计划 | 有明显按住说话与“说什么”提示；收齐全部适用字段，次数/时段一致，最后到核对页 |
| A15 | 拍照内容不含剂量或提醒信息 | 缺项提示补充，不凭药品常识自动造数据；模拟模式明确标注 |
| A16 | 任意模式缺必填后点核对 | 灰色星号初始可见；提交失败后主动弹出醒目错误提示层，同时显示错误星号、红框及就地错误说明；关闭提示或点“去完善”后聚焦首错，保留草稿 |
| A17 | 修正错误并最终确认 | 红框按项解除；一个核对页展示完整计划；返回可改；仅明确确认后创建 |
| A18 | 不同日程/重复点击/起止日期 | 合法任务按配置生成，不重复保存；不支持的分时剂量等情况明确处理而非错误合并 |

### 14.2 语音、记录与 BMI

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A19 | 从长辈首页右下角进入助手 | AI 先引导操作意图，能选择加药、记录服药、稍后提醒、身体数据；不遮底部控件 |
| A20 | 说“今天八点吃了某药一片” | 核对匹配的当次任务、实际时间、实际数量和单位；确认后两端更新 |
| A21 | 给超时任务设稍后提醒，含跨午夜 | 原任务与原时段不变，未按时标记保留，新提醒时间清楚且仍关联原任务 |
| A22 | 午后补记早上已经服药 | 实际服药时间与记录时间分开；从未按时卡更新原任务，不重复计数 |
| A23 | 记录一项后仍有逾期未记录 | 成功后出现“你有未打卡的记录”，按任务逐个让用户选择三种回复，不强制代选 |
| A24 | 回答“没有按时服用” | 澄清仍未服用或后来服用；前者记未服用，后者补记，不能混淆 |
| A25 | 选择本次无需服用及查看旧记录 | 当前按钮、确认、历史等全局新文案一致；当次状态改变，后续计划不被停用 |
| A26 | 语音记录血压/血脂/身体指标 | 收缩压/高压、舒张压/低压可识别；日期时间、数值单位及医生交代备注核对后保存；失败可手动继续 |
| A27 | 160 cm 与 60 kg | 身体指标自动显示建议精度下的 BMI 23.4，用户无 BMI 输入项 |
| A28 | 身高缺失/无效/与体重不同日期 | 不输出错误值；跨记录引用符合确定规则并标明来源日期；不出医学评级 |

### 14.3 通知、奖励、样式与回归

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A29 | 桌面与手机查看 APP 样式 | 桌面有 iPhone 预览感，手机不二次缩小；样式不被宣称为真实安装包 |
| A30 | N1 默认时间和自定义时间到点 | 原型清楚模拟；真实 APP 在目标设备与合法授权下收到对应通知，打开正确任务 |
| A31 | N2 长辈超过期限无记录 | 子女另一台真实设备收到对应长辈任务通知；离线未知不说成确定漏服 |
| A32 | N3 子女主动提醒 | 长辈另一台设备收到并能查看时段、药名及明确几片/几包剂量；不改变服药状态 |
| A33 | 权限关闭、后台、离线、重启、重复消息等 | 按平台测试记录限制和结果，状态不伪造；重复抑制、过期处理、解绑失效及锁屏隐私正确 |
| A34 | 长辈首页查看与点卡通 | 有面部、喜庆、静态先行、不拥挤；点击可看存钱罐；最终形象选择有记录 |
| A35 | 完整完成日与未完成日 | 按已确认口径判定；符合时每日一次随机 0.10～1.00 元；未全完成不发；不把每次药物打卡都发奖 |
| A36 | 余额跨月、达到门槛 | 月底不清零；9.99 元未达 10 元，10.00 元达到门槛；提现过程金额和状态可核对 |
| A37 | 子女充值及资金不足 | 明确目标与账本；模拟/真实不混淆；不足不伪造可提现余额；退款和提现失败有处理 |
| A38 | 重复点击、换设备、撤销、更正 | 同日不重复领、不反复随机；账本可追溯，更正按明示规则处理 |
| A39 | 两端进入我的/会员 | 都有一致的会员区域，AI 功能体现 PRO；月/季/年为占位或已批准方案，价格和共享权限不杜撰 |
| A40 | 检查全部添加入口和模式图标 | 用药、身体图标比例正确、位置正确、不被文字挤压 |
| A41 | 展开/折叠未按时卡 | 小圆红点替代警告三角；红点不随折叠箭头旋转；减少动态效果时静态可辨 |
| A42 | 窄屏/桌面 × 标准/大字号 | 卡片、日历、四列表、滚轮、核对页、导航和悬浮入口无明显溢出遮挡；键盘与读屏可用 |
| A43 | 旧存档、无效存档、空数据、恢复初始数据 | v1兼容或合理降级；旧餐后来源不误作确认；全部新字段受校验并可恢复演示数据 |
| A44 | 多长辈权限、子女语音代打尝试、解绑 | 数据不串档；语音与点击权限相同；解绑停止后续访问与通知 |
| A45 | 真实健康、音频、图片及交易数据处理 | 必要授权、最小化、保存期限、撤回/删除机制与第三方处理安排落实；无未声明上传 |
| A46 | 星星/奖励特殊情形组合 | 无任务、无需服用、未服用、晚补、量不一致、计划变动均按 D01/D04/D07 等已确认规则运行 |
| A47 | 对外展示与真实上线门槛 | 演示能力有标识；真实三类通知有跨设备证据；真钱和订阅有实际合规、支付与对账验证 |

### 14.4 典型验收数据组合

原型建议准备：同一家庭的张阿姨、小李及另一位可切换长辈；至少一个全完成历史日、一个仍有未打卡日、一个明确未服用日、一个本次无需服用日；四个时段任务；一个已延后但仍逾期任务；一条实际服药早而记录晚的补记；今天血压与旧血脂；有效和缺失身高体重；9.99 元与 10.00 元门槛样例。全为演示数据。

完整流程应至少覆盖：子女看板查看 → 为选中长辈创建多时段计划 → 长辈收到或模拟收到提醒 → 本人记录 → 双端更新 → 超时任务追问/延后 → 子女远程提醒 → 最新身体数据与 BMI → 完成日星星 → 奖励到账记录。UI 原型演练与真实设备通知验证分别记录，不互相替代。

## 15. 实施顺序与待确认事项

### 15.1 按依赖推进，不删减目标

| 阶段 | 主要交付 | 完成门槛 |
| --- | --- | --- |
| 文档阶段（本轮） | 完整需求、建议、未决规则、验收和来源 | 九项需求无遗漏，可追踪，不声称已实现 |
| 交互原型 | 首页、路由、表单、语音模拟、状态、BMI、星星、卡通与模拟账本 | 可完整演示，模拟项明确；关键规则有记录 |
| 真实 APP 与数据 | 选定客户端、身份、家庭授权、任务与事件同步、真实语音接入 | 权限正确、跨设备一致、误识别有确认和回退 |
| 三类真实通知 | N1、N2、N3与对应系统能力和后端 | 逐平台实机测试通过；保持用户指定必做目标 |
| 会员与真实奖励资金 | 会员方案、支付/充值、结算、提现与账本 | 完成口径、运营和平台规则确认，交易与对账验证完成 |

可以并行进行 UI 评审和基础设施选型，但不能在关键规则未确定时进行真实资金结算。未解决的规则不阻止本轮需求文档交付；会阻止对应生产功能被宣布完成。

### 15.2 必须显式确认的规则

| 编号 | 问题 | 初版建议/候选 | 影响 |
| --- | --- | --- | --- |
| D01 | 全天完成含什么；星星与奖励是否同口径 | 优先讨论“如实完成所有应处理记录”；明确未服用、无需服用、无任务、身体数据和补打的处理 | 月历、奖励、公平性与医疗行为激励 |
| D02 | 端点、00:00～05:00、逾期与按时定义 | 四时段左闭右开；按时段结束判断未按时；是否保留60分钟为另一提醒阈值待定 | 首页当前任务、历史状态、子女提醒 |
| D03 | 每行提醒能否超出所属时段；滚轮步长 | 建议限制在该时段、分钟步长1；越界需求需显式支持 | 表单与通知调度 |
| D04 | 实际量不同、部分服用、实际时间不详 | 如实保存，不假装全部按量完成；是否新增状态及奖励处理待定 | 语音记录、状态与奖励 |
| D05 | 无计划药物的临时实际服药记录 | 人工核对或建立适用计划后关联；是否新增临时记录模型待定 | 全局助手的“记录新药”分支 |
| D06 | 延后次数、最长时间、跨日、子女提醒 | 延后只改通知；不清原逾期、不默认取消子女提醒 | 通知骚扰控制与事实一致性 |
| D07 | 可补记多久、历史只读例外及新建/停用生效 | 当天超时补打必做；跨日补记与任务生效需定规则 | 历史、日历、奖励与旧数据 |
| D08 | BMI 能否引用旧身高及多久有效 | 建议使用体重测量时点之前最近有效身高并标时间 | BMI准确呈现 |
| D09 | 首发平台、系统最低版本、APP技术栈 | 先iPhone风格评审；安装包和多平台适配单独选型 | SDK、工期、实机测试矩阵 |
| D10 | 通知延迟目标、频控、多子女接收与回执 | 定义可实测指标、接收授权、重试和节流；不承诺无条件准点 | 三类通知真实验收 |
| D11 | 资金归属、随机分布、结算/充值/退款/提现 | 保留0.1～1元、10元门槛、月末不清零；其他规则逐项确定 | 支付、账本与运营合规 |
| D12 | PRO定价、套餐、个人/家庭共享和代购 | AI为核心；月/季/年为倾向；基础提醒建议免费 | 会员权益与平台计费 |
| D13 | 子女新增前是否每次强制选对象 | 建议带入当前对象并显眼核对；v1强制选择器可保留 | 跨档案误录风险与步骤数 |
| D14 | 看板模块顺序、历史月份、子女首页加号 | 按第2节先出一版；本月必做，历史月与入口形态待定 | 首页密度与导航 |
| D15 | 卡通四选一、最终位置与后续动画 | 小猫为评审建议，不代表定稿；先静态 | 视觉设计与素材授权 |
| D16 | 语音供应商、方言范围、资料保存与云处理 | 先中文引导、人工核对与手动回退；服务与数据规则再选 | 识别质量、费用、隐私与可用性 |

### 15.3 本轮文档交付检查

文档应完成双向检查：从原文能找到对应规格和验收；从新增规则能辨认其属于用户明确要求、建议、实现约束还是待确认。尤其核对四组时间、两条快捷卡片文案、三个语音回复、金额区间、提现门槛和“月底不清零”。

本轮未修改交互、数据或样式代码，因此无需将浏览器功能测试当作文档验收；不得将 A01–A47 标成已通过。后续每次实现改动仍按项目要求执行浏览器渲染、控制台、双端切换、打卡/提醒或记录、刷新持久化、权限及多视口/双字号检查。

## 16. 官方资料与证据范围

以下资料用于约束真实 APP 的技术与隐私表述，不替代发布时对目标版本、发行地区和具体业务的复核。本文件没有引用旧 PRD 中未经本轮验证的调研比例或医疗效果承诺。

| 编号 | 来源 | 本文使用范围 |
| --- | --- | --- |
| S1 | [Apple User Notifications](https://developer.apple.com/documentation/usernotifications)；[Local and Remote Notifications Overview](https://developer.apple.com/library/archive/documentation/NetworkingInternet/Conceptual/RemoteNotificationsPG/) | 本地与远程通知的职责。第二项为归档概念资料；实现 API 以目标 SDK 为准 |
| S2 | [Apple：Creating the Remote Notification Payload](https://developer.apple.com/library/archive/documentation/NetworkingInternet/Conceptual/RemoteNotificationsPG/CreatingtheNotificationPayload.html) | 远程通知不保证送达、避免载荷中放敏感数据。归档资料不用于断言当前载荷大小或接口限制 |
| S3 | [Android：Notification runtime permission](https://developer.android.com/develop/ui/views/notifications/notification-permission) | Android 13+ 的通知运行时权限 |
| S4 | [Android：Schedule alarms](https://developer.android.com/develop/background-work/services/alarms/schedule) | 定时精度、精确闹钟适用权限及系统限制 |
| S5 | [HarmonyOS：Notification Kit](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/notification-overview)；[Push Kit](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/push-kit-introduction)；[代理提醒](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/agent-powered-reminder) | 定位通知、推送和后台提醒的官方能力入口；动态正文未完整提取，具体 API、支持版本和配额待实施时核实 |
| S6 | [Apple App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/) | 数字功能内购/订阅、业务说明、隐私与审核要求；具体现金奖励模式需要单独评估，不因本文引用而自动获准 |
| S7 | [国家网信办：中华人民共和国个人信息保护法](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm) | 第28–30条敏感个人信息处理，第47条删除，第51条保护措施，第55–56条个人信息保护影响评估 |

## 附录 A. 用户九项需求原文

以下保留用户表达用于需求溯源。正文中的术语规范、合规修正和待确认标记不改写原文，也不能把原文中的设想当成已经完成的功能。

### 原始第 1 项

新增子女首页看板，也就是说在子女端“服药打卡”这个页面改为首页看板。需要增加一个日历。就是本月的日历，每一天打卡如果全部完成了，当天的日期。上面就叠加一层黄色的星星图标来标志这一天已经完成全部打卡。未完全完成打卡的日子则没有背景，只是显示日期的数字。看板还需要一个模块能够直接看到老人今天的用药情况，还需要一个模块展示今天的身体数据或最新一次的身体数据。这一块的呈现我还没有想好，你可以先按照你的理解做一版出来让我看看。

### 原始第 2 项

老人端的首页依旧保持名称为服药打卡，但是这个页面稍微有一些复杂了，右下角的加号，我希望它移动到整个屏幕的中上方，就在今日已记录和问候语之间左右分布两个圆角方框，左边方框里呈现“点击添加用药打卡”，右边方框里呈现“点击添加身体数据”。记得要保持整个页面的视觉效果是均衡美观的，这两个按钮不需要太大，但也要方便点击。一般来说，这是为了让老人能够更加快捷地添加自己要打卡的药物。如果是子女帮忙添加，也可以用老人的手机从此页面进行添加。这个页面还有一个功能，我觉得需要小小的优化，就是右上角切换账号的那个图标左边的时钟，我不知道它的功能到底是什么，但是我希望它是一个查看历史打卡信息的入口，我希望它通向用药信息页面的那个打卡记录。

### 原始第 3 项

用药信息页面右下角的加号点开以后，不要进行二选一了，直接就是添加药品的页面，身体数据这个页面也是一样，点开加号以后，直接就是添加身体数据的页面。

### 原始第 4 项

从首页的“服药打卡”或“用药信息”页面，点击“+”号，进入的“添加药品打卡计划”页面现在需要增加一些新的功能，完善一些旧的功能。首先，药品名称不用变。我希望你在“单次用量”这四个字后面增加一对括号（），内容就写请输入数字，意思是每顿吃几粒或几包。然后用量单位这里，我希望可以增加直接输入的功能，不要只是下拉。服用时段这块问题最大，我需要它呈现的是一个列表的形式：

第一列是复选框。第二列是时段。第三列是提醒推送。第四列是餐前/后。我希望有四个时段，分别是：

1. 早餐：5 点到 11 点
2. 午餐：11 点到 16 点
3. 晚餐 16 点到 20 点
4. 睡前 20 点到 24 点

以此来辅助老人或者是子女选择其计划用药的时段。第三列的提醒推送每一个时段都会有一个默认时间：

1. 早餐时段：默认 8:00
2. 午餐时段：默认 12:30
3. 晚餐时段：默认 18:30
4. 睡前时段：默认 21:00

每一行的提醒推送时间都显示为可以调整的形式。也就是说，在设定的时候，你可以选择不去调整，按默认的来，也可以通过点开hour或minute部分，上下滑动屏幕来选择你想要设定的时间。我希望能够有一个推送功能：只要用户开启了消息通知，就可以按照用户对此应用消息通知的设定，在其设定的时间给老人端手机推送消息提醒其用药。第 4 列的内容是一个按钮，你可以选也可以不选，只要勾选了就会以一个带颜色的圆角标签显示在“服药打卡”页面和“用药信息”页面每个药品的后面。这个按钮分成左右两部分，左边叫餐前，右边叫餐后。所以这一栏的名称，我觉得你可以自己想一个更加简洁合适的名字，不一定就叫餐前或餐后，这也太长了。我希望这个按钮也是有弧度的轮廓，但是不要太过圆润，它的设计应该是简洁的。你选了“餐前”，这一半儿就会显示为深色（主色调），另一半则保持浅色。另一半也是同理。以上是对已有功能的优化，然后我需要在这个页面多一个平行于手动输入、拍照识别的模块，它名字叫“AI语音创建”。这个模块我打算把它设置为一个 PRO 的功能，所以我希望它显示为一个不同的颜色，或者有不同的标志，或者右上角有一个 PRO 的标志。总之不要太夸张，但是也能显得它是一个 PRO 的功能。不管从语音创建还是拍照识别，都要填写手动输入栏目下的所有内容。这个语音创建的模块一点开，我希望它有一个很明显的按钮让我可以按住就说话。这个功能要是想好用，我希望它是有提示的。我希望它按照手动输入模块里必须要输入的内容，给用户以明确的“你要说什么”的提示。或者我希望他能够在用户进入这个页面的时候，直接让 AI 引导用户比如说先引导他说出药品的名称然后他用语音回答，再引导他说出每次要吃多少粒或者多少包，一天要吃几次，然后你再引导他选择服用的时段，采用交互的方式完成所有信息的填写。最后，不管哪个模块，你都需要在一个页面让用户核对清楚用药的计划，最后再完成添加。我希望这个核对用药在点击之后，如果用户有没有填写的必填项，你要弹出非常明确醒目的提示，要求完成标红的框。所以我希望还有一个功能：把所有的必填项都加上星号。星号原来是现在的这个灰色的原色，但是当用户没有完全填完，就提交“核对用药计划”时，要把它显示为红色的框。我希望这个红色不要太丑，它和我们这个界面的配色要视觉和谐。

### 原始第 5 项

我刚才忘说了，在老人端的服药打卡页面（也就是首页），右下角还要有语音助手功能。这个语音助手功能跟添加用药信息里面的AI语音创建比较类似，但是它算是一个全局的语音引导入口。如果是从这个页面点进去，我希望 AI 直接引导用户选择当前的点击意图，比如说，很多老人或者是子女想通过直接点击语音助手添加用药，那么就和刚才跟你说的是一样的。如果他的点击意图是想记录，那么你就需要引导他说出今天什么时间，吃了什么药，吃了几片或几包。如果他点开的意图是想让你帮忙“过一会儿再提醒他”，因为他这会儿还吃不了药，那么你就需要帮他重新设定一个提醒时间，此时如果已经过了吃这个药的时段，依旧显示为未按时打卡。如果他是想进来打卡，但是已经过了时间了，那么你也可以帮助他在“未按时打卡”卡片里帮他补打卡。如果他是已经忘记本次服用了，点进来之后如果他开始记录新的药物打卡，你需要在完成该记录之后提醒他：“你有未打卡的记录”，并让他自行回复：

1. 已经服用
2. 没有按时服用
3. 本次无需服用。如果是没有按时服用，那你就要帮他在未按时打卡的提醒里点击未服用（总之记录该药物当天、当次属于未服用状态）。同理如果是本次无需服用，就记录为跳过本次。哦，对了，我希望把全局的“跳过本次”，都改为“本次无需服用”。因为跳过本次的意思不是很明确。如果他点击语音助手的意图是记录身体数据，那你要引导他说出自己的收缩压或者说高压，舒张压或者说低压，什么日子，什么时间测的，大夫有没有说什么？这部分可以记录到备注里。你也可以引导他说出自己的血脂信息和血压是同理的。身体数据也是一样的。

### 原始第 6 项

身体数据这块，我希望你能根据老人的身高体重显示一个 BMI 值。但是无需让老人自己填，只是自动计算并显示在身体数据的身体指标板块。

### 原始第 7 项

我想把它做成一个 APP，暂时给它长成苹果手机的样子，我做成 APP 的原因是，APP 的提醒功能更加直截了当，它就是联动手机的通知，但是我暂时不知道怎么去做，不同的手机，iOS 系统、鸿蒙系统、安卓系统是不是不一样的？总之这个功能一定要实现。提醒功能有三方面：

1. 到点提醒老人端的手机；
2. 超时未打卡提醒子女端的手机。
3. 子女因为发现老人未打卡而发起的远程提醒，由子女点击发起，老人端显示什么时段的什么药物没有按时服用规定的剂量（直接显示为几片几包）

### 原始第 8 项

还有一个功能，就是我希望能够让老人喜欢上这个 APP 老人现在都喜欢小孩或者猫猫狗狗，我希望有一个卡通的形象，它可以是：

1. 一个抱着元宝的小孩
2. 一只可爱的小狗
3. 一只可爱的小猫
4. 也可以是财神爷形象

总之就是他要有鼻子有脸，然后看着很喜庆。这个形象，我希望先是静态的，到后面可以给它做成动态的，这个形象我暂时没想好把它放在哪里，希望不要让页面变得非常拥挤。

这个形象主要是出现在老人端，老人点开它就可以看到当前因为打卡已经攒了多少钱。所以，这个形象其实是一个存钱罐，老人每打完一整天的卡，就能随机获得一些钱（0.1～1元），攒够10元可提现，每月底不清零。然后这个钱主要是从子女端的充值得来的，盈利模式我暂时没想好，但是我觉得按月/按季/按年的形式更好。pro功能主要是AI语音助手功能。开会员这个可以放在“我的”页面做，老人和子女端相同。可以先预留出来空间放这个部分。

### 原始第 9 项

UI方面的呈现需要优化。现在点开任何一个加号，你会发现用药信息、身体数据的图标都会有畸变，或者说它不显示为正常的形状，不在正确的位置。未按时打卡提醒不需要⚠️这样的表示，只需要显示为一个有点像红绿灯的红灯一样的红点，它可以闪烁，是圆形的一个小点，这样可以避免你打开折叠卡片之后，这个注意的图标就翻转了。

### 原始交付要求

以上是我能想到的第二版MVP的所有改动、优化、新增项。帮我把这些所有需求不丢失任何信息的、有条有理的、详细的、合规的记录到第二版MVP文档里
