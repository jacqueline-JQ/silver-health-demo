# 银龄健康管家：双端家庭协同 MVP 文档 v2

> 文档性质：第二版产品需求规格与验收依据。在原始九组需求、此前十项业务规则确认的基础上，纳入最新确认的比赛 Demo 交付方式、三系统视觉、通知联动与两个 AI 助手场景。本文记录待实现的目标行为，不代表功能已经开发、上线或获得监管认可。
>
> 本轮文档工作：将已确认内容写回本文件并同步规则、范围、实施顺序和验收，保留现有《其他待实现功能》清单及原始附录。本轮没有修改应用代码或交付新页面。
>
> 下一轮开发交付：纯前端、可亲手操作、带实际界面动效的比赛 Demo；真实 APP 安装包、系统推送、跨设备服务、真实语音识别与模型 API 接入移至后续阶段。本文中的“本次／首轮 Demo”均指该比赛交付；“未来真实 APP”单独标明，不作为比赛 Demo 的通过门槛。

## 0. 版本目标与阅读约定

### 0.1 第二版目标

在第一版“子女协助建计划 → 长辈打卡 → 双端查看 → 子女提醒”的基础上，补充子女首页看板、长辈快捷入口、精细用药计划、两个可交互 AI 助手场景、BMI、三系统手机桌面与模拟通知、聚焦打卡全屏交互。

用户强调的三个方向是：长辈操作更直接；子女能集中看见长辈的记录；比赛现场能从模拟手机桌面完整体验“收到通知—进入应用—完成记录”。产品演示以子女协助长辈使用的家庭场景为主，iOS 是默认且重点打磨的视觉版本，鸿蒙、安卓分别呈现其风格。用户提到的长辈常用鸿蒙、子女常用苹果是本轮设计出发点，不在此扩写为经过验证的市场比例。

**最新交付范围已确认：**首轮用纯前端实现三类通知的可交互模拟，不接真实手机推送，也不以两台真实设备联通作为验收条件。交付包含真实编写的 UI 动画和可改变结果的交互，不是录制视频、静态截图或只能按固定顺序播放的动画。真实通知的技术约束完整保留为后续 APP 阶段要求。

**保留的十项业务决策（最新演示范围另见 0.5，不改变以下服药规则）：**

- 第 7 章是打卡事实、时间、延后、星星、计划生效与历史更正的统一依据，其他章节和验收引用同一规则。
- 未记录任务到达自然时段结束且不在延后保护期内，才显示“未按时打卡”；普通提醒时间只决定何时提醒。
- 普通、逾期和历史打卡均让长辈本人选择“已服用／未服用／本次无需服用”，不强制核对实际时间或数量；补记详情只说明记录时间。保留 5 秒撤销和本人更正，不自动判断“延时服用”。
- 有任务且全部为“已服用／本次无需服用”才有星星；未服用、未记录、无任务均不点星；当天补记可恢复资格，跨日补记与延迟同步分别处理。
- 当前待处理任务已到提醒点时可自动聚焦；可关闭、逐药处理、保护草稿，延后期间不反复弹出。
- 新计划只生成生效后适用的当前及未来任务；编辑／停用只影响未开始时段，不抹掉已经开始的任务。
- 添加计划仅保留“手动输入”和“AI语音输入”，均须核对计划；两种账号的首页对话助手均保留，所有保留功能无会员付费墙。AI语音输入支持文字与点选混合，首轮语音识别按第 6.11 节模拟。
- 每个独立当次任务仅可延后 1 次，选择 5 或 30 分钟；保护期内抑制逾期红点、N2 和 N3；家属共用每任务 5 分钟 N3 冷却。
- 本次取消拍照／扫码识药、健康报告导出、陪伴形象、换装和气泡的入口及实现要求；形象类想法转入独立后续清单。存钱罐、资金奖励及 PRO 会员继续取消。
- 业务日期统一按北京时间；跨午夜延后仍属于原日任务。首轮保留 iOS／鸿蒙／安卓完整演示主题与三类模拟通知；三类真实通知目标移至后续 APP 阶段。

### 0.2 需求标记

| 标记 | 含义 | 开发使用方式 |
| --- | --- | --- |
| 明确需求 | 用户已经明确提出的内容、数字、文案或位置 | 必须保留；不能因实现复杂而静默删除 |
| 初版建议 | 为用户尚未定稿的呈现提供一版可讨论方案 | 可用于原型评审，不等于用户已经确认 |
| 待确认 | 原始要求未给出口径，或多种要求之间需要选择 | 记录候选与影响；在相关生产功能启用前确定 |
| 实现约束 | 为数据真实性、权限、可靠性和可访问性补充的工程规则 | 不应冒充用户原话；实现时必须有对应验证 |
| 上线条件 | 真实服务所需的基础设施、平台审核、隐私或资金条件 | 原型演示不能代替这些条件 |

本文件中的“必须”可能来自明确需求，也可能来自注明的实现约束或上线条件，必须连同所属交付阶段阅读。未决事项集中在第 15 节，避免开发者从示例中猜测产品决策。最新明确取消或修改的内容优先于 v1、v2.1 过渡稿及附录历史原话；其中“真实服务后置、首轮交付可操作模拟”的决定覆盖此前本轮必须接通真实通知的表述。附录 A 保留原话，不恢复已替换的范围或规则。

### 0.3 与现有文件的关系

| 文件或范围 | v2 的处理 |
| --- | --- |
| `老年人智慧健康管理_MVP文档_v1.md` | 保留第一版历史。本文明确变更的部分以 v2 为目标；未变更的家庭权限、人工确认、历史留存等继续适用 |
| `老年人智慧健康管理PRD.md` | 远期方向参考。医生端、诊断、处方建议、真实硬件、SOS 等没有因此进入本次范围 |
| `AGENTS.md` | 描述当前静态演示项目的实际边界。v2 是待实现需求，不能据此把当前模拟服务描述成真实能力 |
| `老年人智慧健康管理_MVP文档_v2.1_优化设计与确认.md` | 已归档过渡稿，不再作为实现基准；其中不同的星星、录入和形象规则以本文最新确认版为准 |
| `老年人智慧健康管理_其他待实现功能.md` | 仅保存形象、换装、气泡等后续构想，不构成本次入口、数据字段或验收承诺 |
| 当前 HTML 原型 | 仍是原生 HTML/CSS/JavaScript、本地演示账号与本地存储；没有因本文新增真实服务 |
| 拟新增 `demo.html` | 下一轮开发的独立比赛演示入口，嵌入原 `index.html` 并联动；不是已经存在的新应用，也不复制另一份业务数据 |
| 演示日期 | 原型以 `2026-09-13` 为固定基准，跨日验证使用受控日期偏移；真实 APP 使用实际日期，两端业务日期均按 `Asia/Shanghai`（北京时间）计算 |

v2 明确替换的 v1 规则包括：子女首个导航名称、长辈首页添加入口、加号的分页面路由、顶部时钟用途、四时段及提醒配置、餐前餐后独立标记、手动／语音两种录入模式、全局语音入口、“跳过本次”文案和 BMI。v1 的“提醒时间 + 60 分钟”及历史只读、停用隐藏所有待处理任务等被第 7 章的最新规则替换。

**范围保持：**未被最新十项取消的首页看板、日历、快捷添加、身体数据、BMI、三类提醒及适老界面需求继续保留。

### 0.4 交付层级

| 层级 | 可以交付什么 | 不能据此宣称什么 |
| --- | --- | --- |
| 本轮文档 | 本文件的完整需求、阶段边界、实现建议与待执行验收 | 文档写入即表示代码已完成 |
| 首轮比赛 Demo（下一轮开发） | `demo.html` 联动原应用；三系统桌面、通知与应用主题；真实 UI 动效；文字与示例语音对话；表单、查询、核对、本地保存、日历与聚焦打卡 | 已在真实手机后台推送、已识别真实声音、已跨手机同步、已生成三个安装包 |
| 未来真实 APP（后续阶段） | 另行选型后接入安装包、真实身份与家庭授权、跨设备数据、真实语音和通知，按独立上线条件验收 | 所有手机在任意条件下必然准时送达、已发送就代表老人已经看见或服药；不能提前写成首轮成果 |
| 资金与会员 | 本版本不交付 | 任何付费、奖励、充值、提现或 PRO 会员宣称 |
| 本次移除能力 | 拍照／扫码识药、健康报告导出、陪伴形象、换装、气泡均不交付，不设占位入口 | 已提供相关功能，或后续清单已经承诺实施 |

<a id="confirmed-demo-scope"></a>

### 0.5 本轮讨论确认表与阅读入口

以下是本次写回的确认内容，不取代第 7 章业务规则。正文给出实施细节，第 14 章给出可观察的通过条件；全部仍待开发验证。

| 编号 | 已确认内容 | 详细依据 | 对应验收 |
| --- | --- | --- | --- |
| C01 | 纯前端比赛 Demo；能亲手操作、实际播放 UI 动效、真实保存本地演示结果；真实服务后置 | 0.1／0.4、10.4、15.1 | A61、A80、A84 |
| C02 | 独立 `demo.html` 直接嵌入现有 `index.html`；复用业务，增加消息接口和内部主题适配 | [9.1／9.2](#demo-stage)、12.4 | A61、A62、A70 |
| C03 | 控制区切换三系统；iOS 默认且重点打磨，鸿蒙／安卓风格可辨；系统与账号角色独立 | 9.1／9.4、[11.4](#platform-visuals) | A62–A66 |
| C04 | 桌面、图标、通知、应用控件和动效统一适配；切系统、返回桌面不重载应用、不丢草稿 | 9.2、11.4／11.5 | A63–A65、A83 |
| C05 | N1／N2／N3 三类提醒均可在单浏览器内模拟、触发并查看接收方效果 | 9.4／9.5／9.7 | A30–A32、A68 |
| C06 | 桌面→横幅→提醒页→关闭→对应时段药物列表→状态打卡；关闭不算记录、不立即重弹 | 9.3／9.4 | A37、A52、A67、A69 |
| C07 | 首页点击／长按进入同一有问候、有建议的聊天页；两种角色可用；增加已有计划查询 | [6.1](#assistant-home)、6.9 | A71–A74、A81 |
| C08 | 加药入口直接收集信息，回显已知、只追问缺项；四时段及餐时可点选；最后完整核对 | [5.6](#assistant-plan)、5.7 | A75–A77、A80 |
| C09 | 两入口共用聊天组件、上下文草稿与业务操作；文字、示例语音、表单可混合并可纠正 | 6.10／6.11、12.4 | A73、A78、A82 |
| C10 | 首轮模拟语音识别，有限文字规则推进；明示示例，不冒充监听或任意口语理解；ASR／模型后置 | [6.11／6.12](#speech-scope)、13.3 | A79、A84；未来 P05 |
| C11 | 查询读已有计划，核对后创建／保存；打卡走原有入口，结果联动首页、历史、子女视图 | 5.7、6.3、6.9、12.2 | A20、A44、A74、A80／A81 |
| C12 | 原有看板、星星、BMI、时段、延后、补记、更正、权限、适老字号及取消范围继续保留 | 2–8、10–13 | A01–A60 中有效项 |

## 1. 九组需求追踪总表

| 原始编号 | 对应需求 | 主体章节 | 验收编号 |
| --- | --- | --- | --- |
| R1 | 子女首个页面改首页看板；本月日历；完成日黄色星星叠加日期；未完成日只显示数字；今日用药；今日或最新身体数据；先提出一版呈现 | 2 | A01–A04 |
| R2 | 长辈首个页面仍叫服药打卡；中上方两张左右圆角添加卡片；尺寸适中；支持子女借长辈手机协助添加；时钟直达用药信息的打卡记录 | 3 | A05–A07 |
| R3 | 用药信息加号直接加药；身体数据加号直接添加身体数据；取消跨类别二选一 | 4 | A08 |
| R4 | 药名、用量、自定义单位、四列时段、提醒滚轮、餐时与错误提示保留；**手动／AI语音两种模式**共用核对。加药助手进入即引导、回显已知、补齐缺项，支持文字／示例语音／点选卡；拍照／扫码及 PRO 取消 | 5、6.10–6.12、7.5 | A09–A18、A53、A60、A75–A80 |
| R5 | 两端首页点击／长按进入有主动问候的通用助手；加药、当次声明、延后、补记、遗漏追问、身体数据及已有用药计划查询；按角色授权执行，服药不必问实际时间／数量 | 6、7 | A19–A26、A49–A51、A71–A74、A78–A82 |
| R6 | 根据身高体重自动计算 BMI，在身体数据的身体指标中显示，不要求用户填写 BMI | 8 | A27–A28 |
| R7 | 首轮三系统高保真前端 Demo，iOS 优先；独立桌面入口联动原应用，完整通知打开／关闭／打卡流程；N1／N2／N3 模拟、聚焦卡、延后和频控均保留；真实通知后置 | 7、9、11.4／11.5 | A29–A33、A37–A39、A48、A52、A54–A56、A61–A70、A83／A84；未来 P01–P04 |
| R8 | **最新决定本次移除形象、换装和气泡**，转入独立后续清单；存钱罐、红包及会员继续取消，首页语音助手保留 | 10、独立后续清单 | 原 A34／A35 标为取消；范围清理见 A36、A57 |
| R9 | 修复所有加号相关用药/身体图标的形变和错位；小圆红点替代未按时提醒警告图标；可闪烁；展开折叠不使红点翻转 | 11 | A40–A42 |

跨模块验收见 A43–A47，此前十项业务规则补充见 A48–A60；本轮演示与对话确认补充见 A61–A84，真实服务另见 P01–P06。附录 A 原样保留九项原始需求，供反向核对，不作为已取消能力或本轮真实服务接入的开发依据。

## 2. 子女端：首页看板

### 2.1 名称、对象与入口

**明确需求：**子女端原“服药打卡”页面改为“首页看板”，首个底部导航和页面标题同步修改。长辈端的同一导航位置仍叫“服药打卡”。

继续在显眼位置显示当前长辈及切换器。月历、今日用药、身体摘要、提醒按钮均属于同一个选中档案，切换长辈时整体更新。子女只能查看已绑定且已授权的档案，不能代替长辈声明服药事实。

### 2.2 本月打卡日历

**明确需求：**显示本月完整日历。某一天全部完成打卡时，在当天日期上叠加黄色星星图标。未完全完成的日子没有日期背景，只显示日期数字。

| 元素 | 呈现与行为 |
| --- | --- |
| 月份 | 清楚显示年月；原型默认 `2026 年 9 月`，真实 APP 默认北京时间本月 |
| 日期 | 七列月历，日期字号可辨认；月首、月末补位留空或弱化，不计入当月数据 |
| 完成日 | 黄色星星叠加日期区域，日期仍可阅读；不能只在日历下方另列星星 |
| 未完成日 | 无填充底色、无星星，保留数字；不能改成灰底、红底等另一套完成标记 |
| 今天 | 初版建议用月份区域“今天”说明或日期下的小线条定位，避免给未完成日添加背景 |
| 未来日期 | 不显示完成星星 |
| 说明 | “当天有用药任务，且每项均记录为已服用或本次无需服用时显示星星。”不将星星解释成经核实的按时、按量服药 |
| 点日期 | 初版建议跳转“用药信息 → 打卡记录”，携带该日期与长辈对象，便于查看星星依据 |

日历依据该日实际应处理任务集合及第 7.4 节规则计算，不能用今天的药物计划反推历史。有任意“未服用”或未记录任务均不点星，无任务也不点星；当天补记为“已服用／本次无需服用”后，全部满足条件即可点星。身体数据不影响星星。跨日补记、延迟同步及历史更正按第 7.4、7.6、7.7 节处理。

初版范围为“本月”；是否支持切换历史月份列为可选待定项，不以扩展月历功能阻碍本月展示。

### 2.3 今日用药模块

**明确需求：**在看板直接看见长辈今天的用药情况。

**初版建议：**模块标题为“今日用药”，先显示“已记录 X/Y、待记录 N、未按时打卡 N”，下方按早餐、午餐、晚餐、睡前分组展示简洁列表。计数中的“已记录”包括不同用户声明，不能直接命名为“已服药”。

每条至少显示：药品名称、单次计划剂量（例如 `1 片`、`2 包`）、所属时段、提醒时刻、餐前/餐后标签（如有）、当前打卡状态。详情显示记录时间、补记／更正信息；仅在本人主动提供时展示实际服药细节，不把计划剂量或记录时间当成实际服用量／时间。“已记录”包含未服用，因此不等于星星资格。

对允许提醒的未记录任务提供“提醒 TA”；真实 APP 发起第 9 节远程提醒，原型明确显示“模拟提醒”。模块末尾提供“查看全部用药/打卡记录”。没有任务时显示“今天暂无用药打卡计划”，不将其描述为“全部完成”。

### 2.4 今日或最新身体数据模块：第一版呈现建议

用户尚未确定布局，先采用一张“身体数据”摘要卡，按类型分三行，避免把看板做成密集图表。

| 行 | 摘要内容 | 日期规则 | 入口 |
| --- | --- | --- | --- |
| 血压 | 收缩压/舒张压，单位 mmHg；脉搏有记录时附带显示 | 优先展示今天最近一次；否则明确标“最近一次”并显示实际日期时间 | 查看血压详情 |
| 血脂 | TC、TG、HDL-C、LDL-C 中已有项目及单位；空间不足时显示前两项和“查看全部” | 使用本次检验记录的日期，不因今天录入就标为今天测量 | 查看血脂详情 |
| 身体指标 | 体重、身高、可计算时的 BMI；体脂率有值时在详情显示 | 标注测量时间；跨次引用身高时显示来源时间 | 查看身体指标 |

各类型独立选取最新记录。不能因为血压是今天的，就把整张卡中的旧血脂也标成“今日”。无数据则显示“暂无记录”和“添加身体数据”，不显示零或虚构正常值。

只展示数值、单位、日期与来源，不引入红绿诊断色块、达标评价或自动医疗建议。首页可保留简短摘要，原有血压趋势继续放在详情页。

**初版建议的页面顺序：**当前长辈与日期 → 今日用药 → 本月日历 → 身体数据。存在未按时记录时，在今日用药区域突出提示。模块顺序可在视觉评审中调整，三个模块均必须保留。

## 3. 长辈端：简化“服药打卡”首页

### 3.1 两张快捷添加卡片

**明确需求：**首页名称保持“服药打卡”。原右下角添加加号迁移为页面中上方的两张左右分布圆角方框，位于“今日已记录”和问候语之间。

| 位置 | 固定文案 | 点击结果 |
| --- | --- | --- |
| 左侧 | 点击添加用药打卡 | 进入“添加药品打卡计划”，默认对象为当前长辈本人 |
| 右侧 | 点击添加身体数据 | 进入“添加身体数据”，默认对象为当前长辈本人 |

目的：长辈能快速添加自己需要打卡的药物；子女在得到长辈同意后，也可以拿长辈手机从这里协助录入计划和身体数据。

**初版建议：**问候语 → 左右快捷卡片 → 今日已记录摘要 → 当前任务与未按时提醒。该顺序是对“位于两者之间”的具体呈现建议，保留两张卡片在同一行且视觉均衡的要求。

两张卡片等宽、等高、间距一致，圆角适中，文字允许自然换行。图标只作辅助，文字是主要识别方式。标准字号点击区域至少 44 px 高，适老大字号至少 56 px 高；不要为显得“小”而缩小实际可点击范围。在窄屏和大字号下仍需清楚可点，必要时增加卡片高度。

“添加用药打卡”在此指创建未来需要打卡的药品计划；不表示点击后就已经服药。实际服药记录通过药物卡片或语音助手完成。

### 3.2 顶部时钟改为历史入口

用户所指图标是右上角“切换账号”图标左侧的时钟。当前代码中的用途是调整演示时间；v2 将其改成“查看历史打卡信息”。

点击后直接进入“用药信息 → 打卡记录”区域，不停留在“正在服用”，也不再弹出演示时间设置。保持当前长辈对象；初版建议默认定位今天，允许继续选择历史日期。为图标补充可访问名称“查看打卡记录”。

演示时间调节仍可作为原型验证工具保留，初版建议移至“我的 → 演示设置”。不能因此把原型固定日期改为系统日期。

### 3.3 当前任务与悬浮入口

当前时段仍是首页主体，每个药物单独成卡，显示计划剂量、提醒时间及可选餐前/餐后标签。未按时任务保持折叠提示，样式按第 11 节调整。

首页右下角保留第 6 节“AI 助手”对话按钮，支持点击／长按进入同一聊天子页面。原添加加号不与它重复堆叠。入口必须避开底部导航、安全区与当前任务按钮；本次不提供陪伴形象或形象气泡。子女首页同样提供该助手，按角色显示可用动作；对话中的普通消息气泡不属于已取消的陪伴形象气泡。

## 4. 全局导航与加号路由

### 4.1 底部导航

| 顺序 | 长辈端 | 子女端 |
| ---: | --- | --- |
| 1 | 服药打卡 | 首页看板 |
| 2 | 用药信息 | 用药信息 |
| 3 | 身体数据 | 身体数据 |
| 4 | 我的 | 我的 |

角色可以改变首页名称和内容；字号模式只改变呈现尺度，不改变权限与业务含义。

### 4.2 添加入口映射

| 页面与入口 | 目标页面 | 跨类别二选一 |
| --- | --- | --- |
| 长辈首页左侧快捷卡片 | 添加药品打卡计划 | 不出现 |
| 长辈首页右侧快捷卡片 | 添加身体数据 | 不出现 |
| 用药信息右下角 `+` | 添加药品打卡计划 | 不出现 |
| 身体数据右下角 `+` | 添加身体数据 | 不出现 |
| 首页保留的加药 `+` 或加药动作 | 同一个添加药品打卡计划页面 | 不另造简化表单 |
| 语音助手中的“添加用药计划” | 同一个药品草稿及最终确认流程 | 先确定意图，再补齐字段 |
| 两端首页右下角对话按钮：点击／长按 | 同一个通用助手聊天子页面，点击默认文字模式、长按定位语音模式 | 自动问候并提供可点击意图建议，允许直接输入 |
| 添加药品打卡计划 → AI语音输入 | 带“创建计划”上下文的聊天界面 | 意图已明确，直接询问药品信息，不再询问想做什么 |

用户在第 4 项概括了“从首页或用药信息点击 + 进入计划”，与第 2 项长辈首页加号迁移一起理解：长辈首页用快捷卡片承接原加药功能。最新已确认两端首页右下角均用于助手；子女看板若保留直接加药动作，初版建议放在今日用药模块内，具体落位仍见 D14，不与助手在同一角落堆叠。身体数据通过摘要卡内入口添加。

身体数据内部选择“血压/血脂/身体指标”仍然需要保留；这不属于被取消的“用药信息/身体数据”二选一。

对象规则沿用 v1：长辈固定为自己；子女只可给已授权的长辈添加，提交前清楚确认对象。初版建议带入看板当前长辈，在表单顶部允许重新选择；是否每次强制弹出对象选择器见待确认项 D13。无绑定对象时提供绑定入口，不创建悬空数据。

## 5. 添加药品打卡计划

### 5.1 两种平行录入模式

页面统一命名“添加药品打卡计划”，只包含两个平行入口：`手动输入`、`AI语音输入`。

| 模式 | 用户输入 | 结果与边界 |
| --- | --- | --- |
| 手动输入 | 逐项填写药品计划 | 标准表单草稿 |
| AI语音输入 | 聊天式引导；首轮可输入文字、使用明确标记的示例语音、点击设置卡，可一次提供多个字段 | 同一份可编辑草稿；回显已知信息，追问缺项，最终核对。未来接真实语音时复用该流程 |

**最新明确需求：**两种模式面向所有用户开放，风格一致，无 PRO 角标或付费墙。本次移除拍照识别及扫码，不保留图片选择、模拟识药按钮或相关占位模块。

两种方式共同使用一份字段定义和校验规则，切换模式保留已填写内容，不能悄悄覆盖用户修改。语音需要补齐计划表单的所有适用必填字段；选填项可留空，不虚构备注或餐时要求。这里的计划核对与第 6、7 章的一键服药打卡是两个流程，不能因后者简化而取消建计划的必填校验。

### 5.2 字段与必填规则

| 字段/字段组 | 标识与输入 | 校验或保留规则 |
| --- | --- | --- |
| 记录对象 | 必须明确；长辈端固定，子女端选择已授权长辈 | 属于合法家庭权限范围 |
| 药品名称 | `药品名称 *`；沿用现有字段 | 去除首尾空格后非空；不改名为药品搜索或其他概念 |
| 单次用量 | `单次用量（请输入数字） *` | 必填数字且大于 0；表示每顿吃几片、几粒或几包，不把单位输入此框 |
| 用量单位 | `用量单位 *`；可直接输入，也提供常用候选 | 必填；不能只允许下拉，也不能必须先选“其他”才能输入 |
| 服用时段 | `服用时段 *`；见四列表 | 至少勾选一个时段 |
| 每个已选时段的提醒时间 | 所选行必须有合法小时与分钟，默认值可满足必填 | 必须位于所选自然时段，分钟步长 1；未勾选的行不生效，不要求填写餐时或提醒 |
| 餐时 | 选填；餐前/餐后两段按钮 | 每行最多选一项，也允许都不选 |
| 开始日期 | `开始日期 *`；保留 v1 | 原型默认演示日；真实 APP 默认今天，可修改 |
| 服用周期 | `服用周期 *`；保留 v1 | 长期服用，或截至某日期 |
| 结束日期 | 选择“截至某日期”时出现 `结束日期 *` | 条件必填，不早于开始日期 |
| 服用备注 | 选填 | 由用户或实际处方信息提供；不由 AI 生成医疗建议 |
| 录入方式和操作来源 | 来源由系统记录，家属协助由用户明确选择 | 区分手动、语音、模拟及家属协助；本次不要求图片，不伪造语音识别能力 |

“单次用量”允许合法小数，例如实际方案中的半片，不擅自改成正整数限制。单位候选可以沿用片、粒、袋、支、毫升、滴、喷、瓶，并补充“包”；用户直接输入的“包”不能未经确认替换成“袋”或自动换算剂量。

“每天几次”继续根据所选时段数自动计算。语音可以询问“一天几次”作为一致性核对；若回答每天两次却选择三个时段，必须澄清，不能生成互相矛盾的计划。

v1 的同一计划各时段相同剂量继续作为范围限制。用户若说“早一片、晚两片”，系统必须提示当前模型不能直接表达，保留信息并进入人工处理；不能静默合并成相同剂量。分时不同剂量、按需、隔日、每周服用仍需另行确认，不自动生成错误的每日计划。

### 5.3 四列表时段设置

**明确需求：**以列表形式呈现四列：第一列复选框，第二列时段，第三列提醒推送，第四列餐前/餐后选项。第四列表头初版建议使用简洁的“餐时”。

| 选择 | 时段及范围 | 提醒推送：默认时间，可调整 | 餐时：选填 |
| --- | --- | --- | --- |
| 复选框 | 早餐：05:00～11:00 | 08:00 | 餐前 / 餐后 |
| 复选框 | 午餐：11:00～16:00 | 12:30 | 餐前 / 餐后 |
| 复选框 | 晚餐：16:00～20:00 | 18:30 | 餐前 / 餐后 |
| 复选框 | 睡前：20:00～24:00 | 21:00 | 餐前 / 餐后 |

这些时段辅助长辈和子女录入其既有用药安排，不是系统推荐的医学服药窗口。不能由默认时间推断药品应餐前还是餐后。

**已确认的设置约束：**普通提醒必须落在所选时段内，分别可选 05:00–10:59、11:00–15:59、16:00–19:59、20:00–23:59。00:00～05:00 不新增固定服药时段。左闭右开边界、超时、延后保护和跨午夜归属统一见第 7.3、7.7 节；这里的滚轮仅决定通知时刻，不决定实际服药事实或逾期截止。

### 5.4 小时/分钟调整

每行显示可编辑的小时和分钟，不让用户误以为默认时间固定不可修改。点击小时或分钟区域后，打开时间选择器，上下滑动对应滚轮选择时、分，确认后写回该行；取消则保留原值。

**明确需求：**不调整时使用默认时间，调整时使用用户选择的时间。iPhone 风格原型需能清楚演示滚轮交互；真实 APP 按目标系统提供等效体验。

**实现约束：**支持触摸、键盘、屏幕阅读器和桌面操作；提供可点击或可输入的等效操作，不能只依赖滑动手势。分钟步长已确认为 1 分钟；越出所属时段的值不能保存，应就地说明范围，不静默换到其他时段。调整后回显明确的 `HH:mm`，不因滚轮外观隐藏最终数值。

通知开关与这些时间的关系见第 9 节：首轮在前端保存提醒时刻，由演示时钟和触发器呈现模拟通知；未来真实 APP 的提示声音、震动、横幅和锁屏展示服从用户的系统通知设置。

### 5.5 餐前/餐后分段按钮和药品标签

按钮有略带弧度但不过分圆润的轮廓，左右两半分别是“餐前”“餐后”。未选择时均为浅色；选择左半时左半用深色主色调、右半保持浅色，选择右半则相反。

每个时段独立选择。初版建议再次点击已选项即可取消，让用户回到不选；也可在可访问操作中提供“清除餐时”。不要让“选填”在选过一次后变成无法取消。

选中后，在“服药打卡”和“用药信息”对应药品后方显示带颜色的小圆角文字标签。若同一药物不同的时段餐时不同，标签必须跟随时段行，不能在药名旁显示互相冲突而无归属的两个标签。子女今日用药模块复用相同规则。

未选择时不显示空标签，也不自动补成餐后。标签须同时包含文字，不能只靠深浅颜色辨别。

<a id="assistant-plan"></a>

### 5.6 AI语音输入：意图明确的建计划对话

**最新明确需求：**长辈或子女从“用药信息 → 添加 → AI语音输入”进入时，助手已经知道用户要创建药品计划，立即显示引导消息，不再问“您想做什么”，也不能先出现空白聊天页。标题保留“添加药品打卡计划”，顶部明确当前记录对象，首轮同时显示“模拟语音”说明。

**首次消息示例：**“我来帮您整理用药计划。请先告诉我药品名称、每次用量和单位。如果已经知道服用时段、提醒时间，也可以一起说。后面也能直接点击选项填写。”

保留明显的按住说话操作区，首轮按第 6.11 节明确标为模拟体验；同一输入栏可切换文字输入。逐步问答与一次输入多个字段都要支持，文字、示例语音与点击卡片共同更新一份草稿。输入文字不增加第三个计划录入模块。

#### 5.6.1 收集顺序与已知信息

以下是收集顺序参考，不是每次必须逐题执行的固定脚本。一次已经给出的信息先回显并记入草稿；每轮只补充尚未知道、未确认或互相冲突的字段，选填内容允许跳过。

| 步骤 | 引导示例 | 写入与校验 |
| --- | --- | --- |
| 1 | “请说出药品名称，也可以照着药盒念。” | 药名；首轮输入不明确时重新输入或手动修改，未来真实语音听不清时可重说；不猜测相似药名 |
| 2 | “每次吃多少？单位是片、粒、包，还是其他？” | 单次用量和单位，分别回显 |
| 3 | “按照您已有的用药安排，一天几次？” | 暂存用户描述，用于核对所选时段 |
| 4 | “请选择早餐、午餐、晚餐或睡前，可以选多个。” | 展示四列表，语音选择或点击选择 |
| 5 | “早餐提醒默认是早上八点，需要调整吗？” | 按已选时段逐一确认提醒时刻，可保留默认 |
| 6 | “有餐前或餐后的要求吗？没有可以不选。” | 各时段可选餐时，不擅自给建议 |
| 7 | “从哪天开始？长期记录，还是到某一天结束？” | 开始日期、周期、必要时的结束日期 |
| 8 | “还有需要写下的备注吗？” | 选填；允许直接跳过这一步询问 |
| 9 | “信息已经整理好，请核对下面的用药计划。” | 进入所有模式共用的核对页 |

界面同时呈现消息往返、当前问题、已知摘要、原始输入文字、可编辑字段及“重新输入／改为手动”。用户提出修改时，明确回显受影响字段，不清空其他内容；不凭默认值假称用户说过某项信息。若提供可选语音播报，允许暂停、重复和关闭，所有提示仍有文字；播报不是语音识别，也不是首轮必须接通的服务。

#### 5.6.2 信息不全时的完整交互示例

| 轮次 | 用户／助手行为 | 草稿及下一步 |
| --- | --- | --- |
| 进入 | 助手主动发出上方首次消息，显示当前长辈 | 意图为创建计划；药名、剂量等仍空，不提前保存 |
| 用户输入 | “那个，示例药，每次一片，早晚吃。” | 提取候选药名“示例药”、计划量 1、单位片；“早晚”保留为待确认时段描述 |
| 助手回复 | “已记下：示例药，每次 1 片。您说的早晚，是指早餐和晚餐吗？可以直接点下面的选项确认。” | 保留已知字段；显示四时段设置卡，不重新询问药名和用量 |
| 用户点选 | 勾选早餐、晚餐；保留 08:00／18:30 或修改；分别点选餐前／餐后，或明确不设置 | 时段、提醒及可选餐时写回同一草稿；自动显示每天 2 次 |
| 助手继续 | “时段已确认。从哪天开始？长期记录，还是到某一天结束？” | 显示可编辑日期与周期卡，已有合法信息不重复问；备注可留空 |
| 用户改口 | “不是一片，是半片。” | 将计划量候选改为 0.5 片，回显变化并保留原文；不误解为已经服用半片 |
| 最终核对 | 用户检查完整计划与生效范围，点击“确认创建” | 才调用原应用保存逻辑；生成适用任务，并在聊天中显示创建结果和查看入口 |

示例中的药名只是演示占位，不能依药名猜剂量、餐时或疗程。对于不能可靠处理的改口，明确询问或让用户点开字段修改，不用固定脚本覆盖输入。

#### 5.6.3 对话内可点击的配置卡

复杂选择不要求只用语音完成。对话中插入可编辑卡片；需要较大空间时可展开为面板，关闭面板回到原对话及草稿。配置卡必须包含：

| 配置部分 | 用户可操作内容 | 与原表单一致的规则 |
| --- | --- | --- |
| 四时段 | 早餐 05:00–11:00、午餐 11:00–16:00、晚餐 16:00–20:00、睡前 20:00–次日00:00；每行可勾选 | 至少选一项；精确左闭右开边界依第 7.3 节 |
| 提醒时间 | 默认 08:00、12:30、18:30、21:00；每个选中时段可点小时／分钟滚轮，也有可输入的等效操作 | 分钟步长 1，只能保存在该自然时段内；不把提醒点当成逾期截止 |
| 餐时 | 每个时段独立选择“餐前／餐后”，可以都不选或清除 | 选填、单行互斥；缺失不默认补成餐后 |
| 起止与周期 | 开始日期、长期服用／截至某日期、条件必填结束日期 | 与第 5.2 节校验相同；默认值显示出来供核对 |
| 摘要与修改 | 药名、计划量、单位、每天次数、已选时段及来源可回看 | 点击修改与继续对话双向更新；不产生两份互相矛盾的草稿 |

用户只需理解时段范围和提醒设置，逾期与延后保护由系统按第 7 章计算，不额外要求用户填写“逾期规则”。必填缺项、次数矛盾、时刻越界仍按第 5.8 节提示；完成后统一进入第 5.7 节核对页。首轮输入状态和未来录音失败回退分别见第 6.8、6.11、6.12 节。

### 5.7 所有入口统一核对与保存

共同流程为：填写或识别草稿 → 点击“核对用药计划” → 必填与格式校验 → 单一核对页 → 返回修改或确认创建 → 计划生效并生成适用任务。

核对页在一处完整展示：记录对象、药名、每次剂量和单位、每天次数、每个时段的时间范围/提醒时刻/餐时、开始日期、周期/结束日期、备注与来源。不能只核对药名而隐藏后续设置。

**明确需求：**手动和语音最后都必须到这一页让用户看清，再完成添加。采用明显的“确认创建”按钮；语音可以读出摘要，但未经明确确认不能保存。核对页须展示第 7.5 节的生效范围：当前及未来哪些时段会生成任务，提醒点已过的当前任务会在保存后提醒一次，已结束时段不会自动追造任务。第 6 节的一键服药声明不进入这个计划核对页。

### 5.8 必填星号与醒目错误反馈

1. 所有实际必填项及条件必填项都加 `*`；初始星号保持界面现有灰色。预填的开始日期、周期、已选行提醒时间也不能漏掉必填标识。
2. 用户点击“核对用药计划”时，一次性检查所有适用字段。若不完整，不进入核对页，主动弹出醒目的错误提示层：`还有必填信息未完成，请填写标红的框后再核对用药计划。` 同时显示字段红框与就地错误说明，不能只显示普通行内提示来替代弹出反馈。
3. 缺失或无效字段显示与现有配色协调的红色边框；对应星号变红，并有就地文字说明，例如“请填写药品名称”“至少选择一个服用时段”。不把本来选填的餐时、备注标红。
4. 错误提示层应可被屏幕阅读器读出；用户关闭提示或点击“去完善”后，自动滚动并聚焦第一处错误，保留其他输入。提示层不能使焦点无法返回表单。
5. 修正后移除对应红框和错误文字；再次提交仍有遗漏时继续明确指出，全部通过后才进入核对。

初版建议复用现有 `--danger: #d85e65` 与 `--danger-soft: #fff0f0` 形成柔和珊瑚红提示；实际文字和边框需检查对比度，可调整深浅。错误提示不能只是一闪而过的 Toast，也不能仅由浏览器默认气泡代替。

<a id="assistant-home"></a>

## 6. 双端首页 AI 助手与共用对话能力

### 6.1 入口、定位与意图选择

**最新明确需求：**长辈“服药打卡”和子女“首页看板”的右下角均提供对话按钮。点击或长按进入同一个聊天子页面；点击默认文字输入，长按定位语音模式。此处“全局”表示能引导多类业务，不表示每页都放悬浮按钮。首页助手独立于已取消的陪伴形象和形象气泡。

**进入行为与页面结构：**

| 部位／操作 | 必须呈现的行为 |
| --- | --- |
| 顶部 | 显示“AI 助手”、当前账号／长辈对象、返回入口和首轮模拟能力说明，避免不知在为谁操作 |
| 消息区 | 新会话立即出现助手问候与“可以说什么”的提示；随后保留用户消息、助手回复、任务选择卡、可编辑表单卡和保存结果，不能只做欢迎页加跳转按钮 |
| 点击入口 | 打开聊天子页面并显示可输入文字的底部输入栏，无需先发送一句话才得到引导 |
| 长按入口 | 打开同一页面并定位“按住说话”模式，问候仍自动显示；一次长按只触发一次导航，松手不再触发额外点击，不冒充已经开始真实录音 |
| 输入栏 | 同一栏内切换“文字／按住说话”模式；语音模式提供明确可按压区域，文字模式保留编辑与选择文本，避免把长按选字误判为语音 |
| 辅助操作 | 提供发送、示例内容、重新输入、修改卡片及手动替代；持续按压困难时有点按等效操作；标准／大字号均可用 |
| 再次进入 | 有未完成会话时先显示已有消息、草稿和简短继续提示，不重复插入欢迎消息或覆盖草稿；新会话才生成新的首次问候 |

**主动消息示例：**

- 长辈：“张阿姨，您好。您可以告诉我已经吃过哪次药，也可以添加药品、记录身体数据。想查看今天的用药安排，也可以直接问我。”
- 子女：“小李，您好。当前正在查看张阿姨的记录。您可以查询今天的用药安排、添加药品计划或身体数据，也可以查看哪些任务符合提醒条件。”

同时提供文字可点建议：长辈可选“记录服药／添加药品／添加身体数据／今天吃什么药”，并可进入“稍后提醒／补记用药”；子女显示查询、协助添加和符合条件的“提醒 TA”，不显示可提交本人服药声明的入口。用户可以直接输入意图，不强制先读或选择菜单。

问候和建议读取当前账号、授权长辈、演示日期及实际任务。存在当前待办时可优先提示“要记录这次用药吗？”，无待办不虚构任务；推测意图只用于排列建议，不能自动选择药物、提交声明或保存计划。切换长辈后重新校验对象，对旧会话隔离处理，见第 6.10 节。

AI 帮助查询已有计划、找到页面、采集信息和生成待核对草稿，不自主决定药品、剂量、是否应服药或停药。所有按钮、文字、示例语音与通知入口执行同一权限检查；以下第 6.3–6.6 节的服药声明、延后及本人补记只向长辈本人开放。**两端助手无 PRO 限制、无付费墙**，基础手动能力始终可用。

### 6.2 意图一：添加用药计划

进入第 5.6 节逐步创建流程，采集药名、单次量和单位、每天次数与时段、每行提醒时刻、可选餐时、起止与周期、可选备注。不能因为从首页语音入口进入，就跳过手动表单的必填项或最终用药计划核对页。

### 6.3 意图二：简单记录当次是否服用

**最新明确需求：**只需识别老人想记录哪次任务及是否吃过，不强制询问实际吃药时间、数量或单位，不增加“时间／用量不同”入口。

| 环节 | 行为 |
| --- | --- |
| 对应任务 | 匹配本人的药品、任务日期与时段；同药多次或同名多计划时，只澄清具体哪一次，不能猜测或批量打卡 |
| 本人声明 | 当前目标已明确时，点击“已服用”或明确说“这次已经吃过了”即可提交；“未服用”“本次无需服用”按第 7.6 节处理 |
| 不确定表达 | “好像”“可能”或“没有按时服用”等含混回答，只澄清是否吃过，不追问时间或数量；未明确前不保存服药事实 |
| 结果 | 更新首页、历史、子女看板及星星资格；逾期／历史补记显示“补记＋记录日期时间”，不自动判断“延时服用” |

语音中老人主动说出的实际时间、数量等可以作为自述附加信息保留；缺失时留空，不用当前时刻或计划剂量补齐。数量与计划不同则如实标注差异，不自动补足，不生成剩余剂量任务，不建议再吃多少。本次不新增部分服用状态，声明为“已服用”时按第 7.4 节参与星星判定；星星不代表已核实按量服药。

无对应计划／任务时说明“还没有找到对应任务”，提供选择已有任务或进入建计划流程的入口。不能伪造任务，也不能自动将本次服药变成长服计划。新计划只能按第 7.5 节生成适用任务，不能为了补记追造已结束时段；无计划临时记录模型仍见 D05。

### 6.4 意图三：过一会儿再提醒（延后提醒）

**明确需求：**用户现在还吃不了药，希望语音助手重新设定提醒时间。

流程：选择具体当次任务 → 检查第 7.3 节的延后资格 → 询问“5 分钟还是 30 分钟后提醒？” → 读出明确日期时间 → 本人确认后设置。点击卡片上的 5／30 分钟按钮本身就是明确设置；语音说 10 分钟、其他钟点等不支持值时，请其在两个选项中选择，不静默取近似值。

**必须保持：**延后的是通知，不是原计划的应处理时段，也不是实际服药记录。延后不能把任务改成已完成，也不能改写实际服药事实。

**统一依据：**次数、端点、已逾期任务的资格及保护期统一按第 7.3 节执行；跨午夜归属见第 7.7 节，N2／N3 抑制和频控见第 9.5 节。机会用完显示 `* 延后机会已用完，请按实际情况记录`。刷新、重新进入、撤销打卡或更正记录均不能恢复已使用的延后机会。

若真实 APP 离线，先明确显示“本机稍后提醒已设置，家属侧待同步”。只有服务端确认收到后，才能表示家属侧 N2 已受保护期抑制；不声称离线操作已经立即改变远端状态。

提示文案初版建议：“已设置稍后提醒。本次原来的打卡时段不变，请按实际情况记录。”不使用“已为您调整用药方案”。

### 6.5 意图四：已过时间，补打卡

**明确需求：**用户想打卡但已过时间时，语音助手能帮助其在“未按时打卡”卡片内补记对应药物，而不是只允许查看。

列出本人可访问、仍保留的未记录任务，展示药物、原日期与时段。老人只需回忆是否吃过并选择“已服用／未服用／本次无需服用”；不要求核对实际时间或数量。提交更新原任务，不重复新建已完成事件。

**打开页面不等于记录：**13:00 打开早餐逾期卡，仍显示“未按时打卡”；本人选择后才显示所选声明，详情标注例如 `补记 · 9月13日 13:00记录`。这个时间仅是补打卡时间，不是实际服药时间。处于延后保护期的任务按第 7.3 节显示“延后中”。

当天补记、跨日补记、历史更正与星星的关系统一见第 7.4、7.6、7.7 节。不再以 v1 历史只读或计划已停用为由阻止本人处理仍保留的历史任务；数据留存期限属于真实服务的待定事项，不要求老人填写一套新的时间／数量表单。

### 6.6 新记录后提醒仍未打卡的任务

**明确需求：**若用户忘了本次服用，进入助手后先记录其他新的药物打卡，助手在该记录成功后还要提示：`你有未打卡的记录`，并让用户自行回复。

只列出按第 7.3 节已经超时且不在延后保护期内的未记录任务，不拿尚未到时或正在延后的任务制造催促。一次问一个任务，展示药名、原时段、计划剂量和日期。中止追问不撤销刚保存的新记录，也不替用户选择状态。

| 用户要求的回复选项 | 目标操作 | 防止误记的约束 |
| --- | --- | --- |
| 已经服用 | 补记该任务为已服用 | 目标任务明确、本人回答确定即可记录，不再追问实际时间或数量 |
| 没有按时服用 | 用户明确希望映射到“未服用” | 这句话也可能表示“晚些时候已吃”，先澄清是否至今仍未服用，不能仅按字面直接误记 |
| 本次无需服用 | 记录对应药物当天当次为本次无需服用 | 确认是用户的实际安排，仅作用于当次，不停用整条计划 |

“没有按时服用”的简短追问：`这一次到现在还没有服用，还是后来已经服用了？` 前者记录“未服用”，后者记录“已服用”并标记补记，不再追问何时吃、吃多少。屏幕上的选择直接写“已服用／未服用／本次无需服用”。

用户暂不回答时，保留“未按时打卡/事实未确认”。“本次无需服用”只能由用户声明；AI 不根据药名或身体数值选择它。

### 6.7 意图五：记录身体数据

先问记录血压、血脂还是身体指标，然后复用相应表单与“核对测量记录”页。

| 类型 | 引导内容 | 应形成的记录 |
| --- | --- | --- |
| 血压 | “高压，也就是收缩压是多少？”“低压，也就是舒张压是多少？”“哪天、几点测的？”“大夫有没有交代，或有其他备注吗？”；脉搏有则补充 | 收缩压、舒张压、测量日期时间、可选脉搏、用户转述的医生交代或其他备注 |
| 血脂 | “报告上有哪些指标？”依次收集总胆固醇、甘油三酯、HDL-C、LDL-C 等已有项目；问每项单位、检测日期时间与可选备注 | 原始指标名、数值、单位、检测时间；没有提供的项目不填零、不推测 |
| 身体指标 | “要记录身高、体重，还是体脂率？”继续问数值、单位、测量日期时间与备注 | 对应测量记录；身高体重有效时自动计算 BMI |

“高压/低压”是允许的口语别称，界面同时回显规范名称。医生交代只记录用户转述，可标“用户转述”，不改写成 AI 的医生建议或验证过的处方。

支持分次回答及一次说出多项；遇到数值、单位、日期不清或自相矛盾时追问。语音输入也必须经过字段校验、用户核对和确认保存；不能仅因是语音识别就放宽血压必填等规则。

### 6.8 语音通用状态与可访问性

首轮至少覆盖：已进入／主动问候、等待输入、文字处理中、示例语音体验中、模拟处理中、正在询问、待核对、已保存、已取消、无权限、无法理解。模拟状态明确带“示例／模拟”字样，不显示仿佛真实拾音的麦克风状态；预设声波若使用，也须标明模拟，不能称为用户音量。

未来接通真实语音后，再增加“正在聆听／识别中／识别失败”等真实状态。真实录音松手结束，提供取消；麦克风被拒、网络不可用或识别失败时说明具体情况，保留草稿并允许手动继续，不常驻后台监听。未请求真实麦克风时不能显示已获得录音许可。

按住说话之外提供点按等效方式；所有问题都有文字，原始输入和提取字段都可回看与修改。返回、取消、表单弹层关闭后恢复焦点。朗读若提供则可暂停、重复或关闭，不自动与输入重叠。动画和消息状态应适配减少动态效果，不用持续闪动打断阅读。**语音服务不可用时不丢草稿，不阻断基础手动打卡与查看历史。**

### 6.9 意图六：查询已有用药安排

**最新明确需求：**支持“今天吃什么药”“今天晚上吃什么”“几点吃”“吃多少”等查询，先从已有计划及当次任务读取信息，不把查询变成建计划或打卡。

| 场景 | 回复与操作 |
| --- | --- |
| 今日／某时段查询 | 在当前演示日期和授权长辈范围内显示药物卡：药名、计划单次量／单位、时段、提醒时刻、已设置的餐时、当前记录状态；明确写“按已保存的计划” |
| 有上下文的追问 | 上一轮已经明确某药某次时，“几点”“多少”沿用该任务；出现多个可能对象时先提供任务选择，不猜测 |
| 未设置餐时／备注 | 如实显示未设置或不显示相应标签，不默认回答餐后或补充医学要求 |
| 没有计划／无该时段任务 | 回答“当前没有找到对应的用药计划／任务”，提供查看用药信息或添加计划入口，不生成药名或剂量 |
| 查询后想记录 | 长辈可明确选择对应任务再声明；子女只可查看或按条件提醒，不因对话上下文而获得代打权限 |
| 超出范围的医学问题 | 不提供新处方、加减药、补吃剂量或药物推荐；说明助手能够整理和查询用户已有安排，提供返回当前计划的入口 |

未来任务只供查询，不能提前声明服用。回复内容必须随计划及记录的实际变化更新，不使用与本地数据无关的固定“今日用药”台词；刚创建的计划也要能被下一轮查询到。

### 6.10 两入口共用的对话与草稿机制

**已确认实现方向：**首页通用助手和加药助手共享聊天界面组件、输入处理、草稿、校验与业务操作；用入口上下文区分初始状态，不复制两套建计划逻辑，也不把现有原生前端改成新的框架项目。

| 要素 | 实现约束 |
| --- | --- |
| 入口上下文 | 记录来自首页还是添加计划；首页从意图引导开始，加药直接进入字段收集；从首页转加药保留已说内容与当前对象 |
| 会话绑定 | 当前账号、授权长辈、业务日期、明确意图、选中的任务／计划与会话标识；对象不明先澄清，不能跨档案复用未核对字段 |
| 工作草稿 | 区分已知、缺失、待确认和冲突字段，保留原始文字、提取候选、人工修改以及字段来源；合法默认值也展示供核对 |
| 往返更新 | 文字、示例语音、配置卡与完整手动表单双向更新；新回复只改有证据的字段，不覆盖其他人工修改；选填项可空 |
| 意图切换 | 用户可改问其他内容；先保留未完成草稿，再进入新的明确意图。返回时可继续，不把上一意图的用量误用为当前服药事实 |
| 系统／桌面切换 | 换 iOS／鸿蒙／安卓、返回模拟桌面再打开，不丢消息、输入和草稿；不重复初始化会话或重播首次问候 |
| 账号／长辈切换 | 草稿按账号和对象隔离，原草稿保留待返回；新身份看不到旧身份无权访问的消息，保存与继续会话前再次鉴权，不静默把旧草稿改成新对象 |
| 消息与结果 | 用户发言、助手消息、字段摘要、选择卡和保存结果连贯呈现；重复点击不得重复保存。失败说明原因并保留可修正内容 |
| 落库边界 | 会话推测和未确认草稿不属于已创建计划或服药事实。建计划／测量经完整核对确认；明确的本人当次声明依第 7.6 节直接记录并支持撤销 |

共享实现采用“输入文字 → 识别支持的意图／字段 → 更新草稿 → 返回下一问题或选择卡 → 校验／执行已有业务动作 → 刷新结果”的有限流程。首轮只有明确支持的表达和示例可靠进入自动处理；陌生表达、同名药歧义、冲突或不支持的方案须澄清／转表单，不声称任意自然语言都能理解。

<a id="speech-scope"></a>

### 6.11 首轮模拟语音：如何操作、如何保证结果可变

**首轮明确范围：**实际实现聊天页面、文字输入、字段提取规则、补充问答、点击卡片、查询和本地保存；语音识别使用可选择的示例文本模拟，不调用麦克风、语音识别 API 或模型 API。模拟只限制输入和理解能力，不把业务流程降为静态展示。

**示例驱动流程：**选择示例语音内容 → 按住“体验语音输入（模拟）”或使用点按等效按钮 → 显示“模拟处理中” → 回显可编辑、带示例标识的文字 → 用户明确发送 → 进入与文字输入相同的对话流程。选示例和体验动画本身都不提交业务；没有选示例时引导选择或改用文字，不随机生成一段仿佛刚刚识别的话。真实键入的文字与编辑后的示例都能改变后续草稿和结果，不能始终回复同一套脚本。

| 必备示例／操作 | 应验证的能力 |
| --- | --- |
| 一次给出完整且受支持的计划信息 | 回显全部已知内容，可修改后直接核对，不逐项重复盘问 |
| 只给药名与用量／缺少用量 | 先承认已知，保留草稿，只追问适用必填项 |
| “早晚吃”，未说明餐前餐后 | 提供明确时段选择；餐时选填，不猜测；用户可点击补全 |
| “不是一片，是半片”／“饭前改饭后” | 处理明确改口或要求用户点选确认，不丢单位、数量和更正方向 |
| “这次吃过了”但有多个任务 | 澄清对应药物、日期与时段，不批量打卡 |
| “还没吃”／“没有按时服用” | 保留否定；第二句先澄清是否后来吃过，不直接误记已服用 |
| 实际输入／修改文字，或混合点击设置卡 | 已知摘要、追问、核对和保存结果跟着变化，草稿与手动表单一致 |
| 无法处理的自由表达 | 说明未能确定的部分，保留原文，提供任务选择／字段卡／手动填写，不伪造识别成功 |

“嗯、那个”等无业务含义的过渡词可按有限规则忽略，但目标是可靠提取字段，不是机械删除词语。必须保留否定、尚未、半片、小数、单位、餐前／餐后和改口含义；原始文字与可编辑提取结果并列可回看，不能用润色后的通顺句子掩盖含义变化。第一轮不承诺完整方言识别或任意口语纠错。

模拟源记为示例输入，用户键入记为文字输入；不得标成真实麦克风识别。确认后调用原 `app.js` 的统一业务和保存路径，真正更新首页、任务、历史、子女看板及星星；查询读取这份最新本地数据。刷新后已确认记录依本地存储恢复，未提交会话是否跨刷新恢复按第 12.4 节的明确策略执行。

### 6.12 后续真实语音与 AI 接入边界

网页可以接真实语音，但“录到声音”“声音转文字”“理解用药信息”是不同环节。下表是后续接入方向，不是首轮需要配置的服务：

| 环节 | 首轮比赛 Demo | 未来接入方式与限制 |
| --- | --- | --- |
| 声音转文字（ASR） | 明确标记的示例文本 | 部分浏览器提供语音识别能力，但支持和网络依赖不一致，不能保证离线或作为比赛唯一入口；稳定服务需另选语音识别供应商 |
| 文字理解、字段提取与对话 | 有限意图／规则、已知摘要、缺项追问、卡片回退 | 复杂自由口语和上下文可接语言模型或其他解析服务；ASR 本身不自动提供业务理解，模型输出仍须校验、澄清与核对 |
| 查询、核对与保存 | 原前端及本地演示数据实际执行 | 复用同一业务流程；未来跨设备持久化另接服务端，不能让模型直接改服药事实 |
| 提示播报 | 文字必须完整，可选朗读不作为识别能力 | 若另接语音合成，应可暂停／关闭，与录音分清状态 |

商业 ASR／模型 API 通常需要服务账号、网络、凭据及服务端或云函数中转；密钥不得写入可下载的前端文件。真实麦克风权限、识别失败、供应商与方言范围、原始音频和云端资料处理见 D16 与第 13.3 节。接入时替换输入／理解层，保留两入口、聊天卡片、草稿、校验和最终确认，无须重做整套交互。

## 7. 打卡事实、时段、历史与完成口径

### 7.1 全局用语替换

**明确需求：**将全局面向用户的 `跳过本次` 改为 `本次无需服用`。覆盖：首页按钮、未按时卡片、语音问题与回复、确认弹窗、历史、状态徽标、通知落地页、帮助文案及新版本示例。

已保存为内部 `skipped` 的旧数据可以保留兼容键，但显示为“本次无需服用”。旧事件原含义和操作来源仍要保留，不能把历史“跳过”追溯解释成医生已经确认无需服药。v1 历史文档和本文件原文附录保留旧词用于追踪，不属于面向用户的旧文案遗漏。

“本次无需服用”的简短确认提示沿用：“仅记录您本次的实际安排，不代表系统建议停药。”选项只影响当次，不删除药物、不停用后续计划。不增加实际时间／数量核对。

### 7.2 各类时间与服药事实分开保存

| 时间 | 含义 | 哪些动作可改变 |
| --- | --- | --- |
| 计划时段、原提醒与截止 | 原本安排在哪次任务、何时通知、何时截止 | 第 7.5 节只改变未开始时段；已开始任务保留快照 |
| 实际服药时间与数量（可空） | 本人主动提供的吃药细节 | 不强制询问；不能从记录时间、通知送达或计划剂量推断 |
| 记录提交时间 | 本人在本机确认这条声明的时间 | 系统生成，不提供倒填入口；更正新增版本及更正时间，不覆盖旧痕迹 |
| 服务端接收时间 | 真实服务收到本地记录的时间 | 由服务端记录，不能替换本机确认时间 |
| 稍后提醒时间 | 对原任务再次提醒的完整日期时间 | 本人明确设置；不修改原任务归属、截止或服药事实 |

### 7.3 统一状态、自然时段与延后规则

**本节是首页、历史、红点、语音、聚焦卡和 N2 的唯一时间判定依据。**所有比较使用包含业务日期和 `Asia/Shanghai` 时区的完整时刻，不能仅比较 HH:mm。

| 时段 | 左闭右开范围 | 默认提醒点 | 无记录、无保护时的截止 |
| --- | --- | --- | --- |
| 早餐 | `[05:00,11:00)` | `08:00` | 到达当天 `11:00` |
| 午餐 | `[11:00,16:00)` | `12:30` | 到达当天 `16:00` |
| 晚餐 | `[16:00,20:00)` | `18:30` | 到达当天 `20:00` |
| 睡前 | `[20:00,次日00:00)` | `21:00` | 到达次日 `00:00` |

```text
未按时打卡 = 任务仍适用且没有明确记录
             且 当前时间 >= 原时段截止
             且 当前不在有效延后保护期内

延后保护期 = 从本人确认延后开始，到延后到期时刻之前
             当前时间 == 延后到期时刻时，保护已结束
```

因此 11:00 属于午餐，同时无保护的早餐未记录任务已超时；不留一分钟空档。原 v1“提醒后 60 分钟”规则不再使用。

**服药声明优先显示；没有声明时才计算提醒状态：**

| 卡片状态或标记 | 含义 |
| --- | --- |
| 待打卡 | 无明确记录，尚未达到原截止；未来任务附“尚未到此时段”，进入所属时段后可主动记录，不必等提醒点 |
| 延后中 | 无明确记录且处于保护期；显示再次提醒时间，暂不显示逾期红点 |
| 未按时打卡 | 满足上方统一条件，仅表示没有记录，不能自动写成“未服用”或漏服 |
| 已服用 | 本人确认吃过该次药；不默认确认现在吃、吃了计划数量或按时服用 |
| 未服用 | 本人明确声明这一次未服用；是一条已处理记录，但不符合星星条件 |
| 本次无需服用 | 本人声明该次无需服用；不影响后续计划，也不等于系统建议停药 |
| 补记 | 对原时段截止时刻及之后提交的声明附加的详情标记，包括保护期内跨时段记录；配记录日期时间，不是独立的服药事实状态 |

仅打开任务不改变声明；提交之后才显示本人所选的三种结果。详情中例如“补记 · 9月13日 13:00记录”，只说明何时记录；本次不自动分类或展示“延时服用”。

**一次延后规则：**

1. 仅长辈本人可对已到原提醒点、仍适用且没有明确记录的任务设置延后。已逾期但机会未用过的任务也可设置；未到提醒点、已取消或已有声明的任务不可设置。
2. 每个唯一当次任务（档案＋计划＋业务日期＋时段）限 1 次，选择 5 或 30 分钟。同名药、次日相同时段不是同一次任务；刷新、重启、切换账号、撤销或更正声明不能补回次数。
3. 有效保护期间，当前红点与 N2 暂停，N3 不可发起；曾经超时、已发送的通知及原截止仍保留，不追溯擦除。保护期内允许本人主动记录，记录后取消尚未触发的该次提醒。
4. 保护结束仍无记录时重新套用统一公式。08:10 延后 5 分钟，08:15 只再次提醒，早餐到 11:00 才超时；15:50 延后 30 分钟，16:19 受保护，16:20 仍无记录则超时。
5. 延后机会用完后按钮禁用，显示 `* 延后机会已用完，请按实际情况记录`。具体 N2／N3 轮次、频控和服务端同步见第 9.5、9.7 节。

### 7.4 全天完成、日历星星

**最新已确认，替换此前“全部有明确记录即可点星”的规则：**当天应处理任务数大于 0，且每一项均为本人声明的“已服用”或“本次无需服用”，才显示星星。

| 场景 | 星星与统计 |
| --- | --- |
| 所有任务均已服用，或已服用／本次无需服用混合，或全部本次无需服用 | 有任务且全部满足条件，显示星星 |
| 任意一项为未服用 | 不显示星星；未服用仍计入“已记录”，不计入“已服用” |
| 任意一项未记录，包括未按时打卡或延后中 | 不显示星星 |
| 当天曾经逾期，当天补为已服用／本次无需服用 | 全部满足条件后可点星，曾经晚打卡不永久取消资格 |
| 无任务，或全部为按第 7.5 节取消的未来任务 | 不显示星星 |
| 身体数据未录入 | 不影响星星，不纳入用药任务分母 |

“任务已处理”“本人声明已服用”“星星资格”分别计算。星星依据本人声明，不证明已核实按时、按量服药，也不与金钱奖励关联。

当天未结束时，根据适用任务集合和最新声明动态计算：新增当前／未来任务、取消未来任务、撤销或更正记录均重新计算。更正为“未服用”会移除当天星星，更正回符合条件的声明可恢复。

日界以北京时间次日 00:00 为准。历史星星保存当日截止前的任务集合、记录版本与规则版本依据；次日才首次补记或更正，不重写当日截止时的历史结果，详情明确显示后续补记／更正及时间。确实在当天本机确认、随后才同步的记录按第 7.7 节核对后恢复应有的历史结果，不当作次日补记。不能只保存一个永久星星而丢失依据，也不能把后来更正后的事实伪装成当日原记录。

### 7.5 新建、编辑与停用计划的生效规则

**最新已确认：**药品计划向后生效；当次任务一旦所属时段开始，保留其身份与计划快照。起止日期仍限定适用日期，结束日期包含当天。

| 操作／场景 | 任务与通知行为 |
| --- | --- |
| 新建计划 | 从确认生效时刻生成适用的当前及未来任务，不追造已结束时段；开始日期在未来则等到对应日期生效 |
| 当前时段仍未结束，且为已选时段 | 生成该时段一个任务；原提醒点已过或恰好到达时，保存后提醒一次，与该时点普通 N1 去重；逾期仍按原自然时段截止 |
| 已过时段或过去开始日期 | 不自动回填历史未记录任务，也不立即发送针对过去时段的 N2；无计划临时记录仍属 D05 未决范围 |
| 编辑计划 | 已开始、已逾期、已记录任务的药名／计划剂量／时段／提醒快照不变；更新未开始时段的任务，明确取消旧版本通知，避免同时出现新旧任务 |
| 停用计划 | 取消未开始时段及未来日期的任务与通知；这些未来取消任务退出分母，但已开始任务继续保留并按原规则处理 |
| 停用后补记 | 本人仍可记录或更正保留的当次／历史任务，不能仅因父计划已停用而拒绝；它们仍属于原日分母 |

示例：18:00 新建早餐＋晚餐计划，只生成仍适用的晚餐及未来任务，不补出今天早餐；18:40 新建晚餐计划且提醒为 18:30，保存后提醒一次，到 20:00 才按规则超时。16:00 编辑计划时，晚餐时段已开始，因此编辑只影响之后尚未开始的时段。

子女在授权范围内可协助编辑／停用后续计划，保存前展示对象与生效影响；不能借此删除已到期未记录任务或改写本人声明。当前这次实际是否服用仍由长辈本人选择。正式编辑计划也要经过第 5 章的字段校验和核对。

### 7.6 一键声明、撤销与本人更正

普通、逾期和历史任务均只需老人回忆这一次是否吃过。点击“已服用”直接记录，含义是“本人确认这次吃过”，不附加“现在按上述剂量服用”的隐含确认。点击“未服用”直接记录；“本次无需服用”保留第 7.1 节既有的简短确认，不增加实际时间／数量核对。屏幕应有三种事实选项，不能仅在语音里提供“未服用”。

每次声明成功后提供 5 秒撤销，恢复本次操作前的声明；过期后仍可从本人历史详情进入“更正记录”，选择正确声明并确认。已记录“未服用”而后来吃过时，可更正同一任务为“已服用”。保留原值、新值、操作者、提交及更正时间，不重复创建一次服药任务。

撤销／更正立即重算显示、当日统计和提醒安排，历史星星按第 7.4 节保留结算依据。已发送消息不伪装成未发送，未触发的提醒取消或按最新条件重排；延后已使用次数、N3 冷却和同一轮通知去重信息不回滚。已取消的未来任务不开放打卡，未开始时段不能预先声明吃过；仍保留的过去任务允许本人补记、更正。

语音中主动提供的时间／数量可以保留，不作为提交必填项。本次不提供独立“时间／用量不同”入口，不推断实际剂量或实际服药时间。子女无论通过按钮、语音、旧通知还是更正入口，都不能声明或改写长辈的服药事实。

### 7.7 跨午夜、离线同步与演示时钟

业务日期统一使用 `Asia/Shanghai`（北京时间），不随两台手机各自的系统时区改变。00:00～05:00 不新增固定服药时段，但允许继续提醒、查看或记录前一天任务；不得按“最早时段”把它重新归到早餐。

| 场景 | 处理 |
| --- | --- |
| 9月13日 23:50 延后 30 分钟 | 新提醒为 9月14日 00:20，仍关联 9月13日睡前任务；00:20 保护结束仍无记录时按第 7.3 节超时 |
| 9月14日 00:10 首次补记前一天任务 | 详情显示跨日补记；不补亮 9月13日星星，即使此前有跨午夜保护 |
| 9月13日 23:55 已在本机确认，9月14日 00:10 才同步 | 保留本机确认时间与服务端接收时间，按真实的当日记录及规则核对并恢复原日应有结果；不归为次日补记 |

真实服务应有唯一操作标识、本机确认记录及同步状态，避免重复应用。修改实际服药自述时间不能改变提交日期；时钟异常或无法确认提交时点的冲突应显示待核对，不凭后来填写的时间直接补星。离线延后在服务端确认前不能承诺 N2 已被抑制。

原型保持 `2026-09-13` 固定基准日及可控演示时刻。跨日验证可在演示设置中使用受控日期偏移或专门场景，不改成系统当天；23:50→次日00:20 必须按完整日期计算，不能表现为同日倒退。恢复初始数据时一并恢复日期偏移、延后和通知演示状态。

## 8. 身体数据与自动 BMI

### 8.1 延续的记录能力

保留血压、血脂、身体指标分类、手动录入、实际测量时间、备注、历史和血压最近 7 次趋势。加号直达身体数据录入，语音与手动共用校验和确认。

血压至少需要收缩压、舒张压及测量时间，脉搏选填；原有数值格式校验继续保留。血脂、身体指标可记录已有项目，至少有一项有效测量值，不能因为没有完整四项血脂就填零补齐。

### 8.2 BMI 计算和呈现

**明确需求：**根据长辈身高与体重自动计算，在“身体数据 → 身体指标”板块显示，不让长辈自行填写 BMI。

公式：`BMI = 体重（kg）÷ [身高（cm）÷ 100]²`，单位为 `kg/m²`。例如身高 160 cm、体重 60 kg，BMI 为 23.4375，初版建议显示 `23.4`，内部保留计算精度。

| 情况 | 处理 |
| --- | --- |
| 同次记录含有效身高、体重 | 自动计算并展示“BMI · 自动计算” |
| 只有体重、档案中有旧身高 | 初版建议引用该体重测量时点之前最近一条有效身高，显示身高来源日期；是否采用及有效期见 D08 |
| 缺少身高或体重 | 显示“补充身高和体重后自动计算”，不输出 0、NaN、Infinity 或伪造结果 |
| 零、负值、非数字、单位不明确 | 明确指出问题，阻止使用无效输入计算；不静默把斤当 kg、米当 cm |
| 历史体重与后来新增身高 | 不将未来测量的身高静默用于过去 BMI；引用或更正规则要可追溯 |

BMI 随对应数据更新，可出现在子女看板摘要中。v2 只计算并显示数值，不新增胖瘦分级、疾病诊断、减重或调药建议。老年人 BMI 的临床解释受体成分等因素影响，本版本不承担该解释。

<a id="demo-stage"></a>

## 9. 三系统手机演示、应用联动与通知流程

### 9.1 独立 Demo 入口与交付形态

**最新已确认：**新增独立的 `demo.html` 作为比赛演示舞台，直接联动现有 `index.html`。它是网页中的模拟手机，不是真的退出浏览器、进入操作系统桌面或安装 APP。桌面、横幅、应用打开／返回和应用内操作均可点击，并实际执行界面动画；评委能随时改变操作顺序，不用等待预录视频或固定播放脚本。

首轮提供 iOS、HarmonyOS、Android 三套演示主题，共用一套业务逻辑和数据。iOS 默认且优先精细打磨；鸿蒙和安卓做到整体风格一致、可辨认。手机物理外壳可以共用，但桌面壁纸、系统应用图标、Dock、状态栏、通知和应用内控件／动效不能只靠改颜色区分。具体视觉清单见第 11.4 节。

| 文件／层次 | 下一轮实现职责 | 与现有应用的关系 |
| --- | --- | --- |
| 新增 `demo.html` | 手机舞台骨架、模拟桌面、内嵌应用区域、手机框外的演示控制区 | 直接嵌入现有页面；不是第二套健康应用 |
| 新增 `demo.css` | 外壳、三系统桌面／图标／状态栏／通知样式、应用打开与返回桌面的外层动效 | 只管理外层；不会自动改变 iframe 内部的 CSS |
| 新增 `demo.js` | 系统切换、桌面／应用显示、横幅呈现、场景触发、与内层交换消息 | 不直接拼装药物事实，不另存一份家庭数据，不模拟点击内层 DOM 按钮 |
| 现有 `index.html` | 继续作为应用入口，加载应用脚本和样式 | 在同源 iframe 中加载；单独打开仍可使用；嵌入时避免重复手机外壳／状态栏 |
| 现有 `app.js` | 页面、任务、表单、聊天、权限、本地保存；增加演示消息接口和主题接收逻辑 | 所有数据修改仍走原有业务动作与 `commit(change)`，新增状态按 `validData()` 校验 |
| 现有 `styles.css` | 应用内部三系统主题、导航、对话、表单卡、提醒页与动效；继续支持两种字号 | 根据内层系统标记适配，保留产品健康绿、中文内容和适老可读性 |
| `seed-data.js` 与本地状态 | 默认演示家庭、任务与可重复场景数据；运行时结果保存在原应用 | 不将运行结果回写种子，不因演示壳新增第二套打卡数据 |

桌面预览居中显示手机舞台及外部控制；真实手机浏览网页时使用可用屏幕，控制区自适应放置，避免在手机中再套一个过小手机框。辅助系统图标用于建立桌面视觉语境，主交互是“银龄健康管家”；本轮不要求实现电话、相机、设置等整套手机系统功能，也不把它们包装为已接通服务。

**运行方式：**在项目根目录通过 `python3 -m http.server 8000` 启动本地静态服务，打开 `http://localhost:8000/demo.html`；应用嵌入地址为同源相对路径，例如 `./index.html?demo=1`。端口占用时可更换，但内外层使用同一来源。该服务只提供静态文件，无业务后端；比赛不依赖 `file://` 下不一致的 iframe 通信与存储行为。直接打开原 `index.html` 的独立预览能力继续保留。

### 9.2 同一应用的嵌入、主题同步与消息接口

**已确认架构：**同源 iframe 只在演示舞台初始化时加载；“返回桌面”隐藏应用并保留实例，再打开时恢复显示；换系统只更新外观与动效配置，不重设 iframe 的 `src`、不重载应用、不重新载入种子数据。隐藏应用时暂停自动聚焦并禁止隐藏内容接收键盘焦点，仍允许按演示时钟检查通知资格。

#### 9.2.1 内外层如何使用同一系统风格

控制区选择 iOS → 外层更新 iOS 桌面和通知 → 向应用发送 `ios` 系统标识 → `app.js` 在内部设置如 `data-os="ios"` 的主题标记 → 应用导航、控件和转场使用对应配置。

父页面样式不能穿透 iframe，因此外层和内层都必须显式应用同一系统标识。`demo=1` 只表示嵌入呈现，不给予额外角色权限。内层初次加载后先与外层确认当前主题，再显示应用画面；切换过程中也须协调更新，避免短暂出现“iOS 桌面＋安卓应用”的旧主题闪烁。

外层负责桌面到应用的容器展开／收回，内层负责页面导航、弹窗、底部面板和按钮状态；避免同一次进入叠加两个互相冲突的全屏转场。iframe 内的毛玻璃不能直接采样外层壁纸：桌面 Dock、横幅在外层绘制，应用内导航材质采样应用自己的内容。

#### 9.2.2 拟采用的消息约定

通过浏览器 `postMessage` 交换有限指令，消息名是实现建议，不表示当前代码已存在这些接口。每次请求带唯一请求标识，涉及任务时携带任务 ID、档案和业务日期；内层以当前实际状态为准。

| 方向／消息意图 | 主要内容 | 应用行为 |
| --- | --- | --- |
| 内→外：应用已就绪 | 协议版本、当前视图／账号与可公开的演示状态 | 外层在就绪前不发送会丢失的打开任务指令；显示简短准备状态，失败时提供重试或独立打开应用 |
| 外→内：应用主题；内→外：主题已应用 | `ios`／`harmonyos`／`android` 及请求标识 | 只改变主题与动效配置，回复完成后展示一致画面，不改账号、药物数据或草稿 |
| 外→内：进入桌面／恢复应用 | 舞台可见状态与本轮访问标记 | 控制自动聚焦、焦点及恢复位置；隐藏不等于用户已处理任务，也不清空草稿 |
| 外→内：获取通知场景；内→外：可用任务／不可用原因 | N1／N2／N3 类别、候选任务、当前演示时刻 | 内层验证任务、权限、延后和冷却，返回药名、计划剂量、日期时段、接收账号及状态；外层不自行造药物内容 |
| 外→内：打开关联通知 | 通知标识、任务引用和当前接收方会话 | 再次鉴权并读取最新任务；长辈进提醒／结果，子女 N2 进对应看板／历史；保留未保存输入 |
| 内→外：数据／视图已变化 | 当前版本、受影响任务／档案及最新可显示摘要 | 原应用保存成功后刷新外层横幅和控制反馈，停止展示过期的可操作状态 |
| 外→内：切换演示账号或请求恢复初始数据 | 明确的控制动作 | 复用原应用账号切换／恢复确认流程，不直接改身份字段或存储；草稿隔离与恢复规则见第 12.4 节 |

双方核对消息来源 origin、发送窗口 source、协议版本、允许的消息类型与参数；限定目标来源，不接收任意代码或任意存储写入。重复消息不能重复保存或推送同一逻辑通知。打开任务和执行动作时仍由内层判断权限，主题切换、URL 参数和演示控制区都不能给予子女代打权限。

#### 9.2.3 状态保持与角色组合

系统选择与账号角色相互独立；可演示“子女＋iOS”“长辈＋鸿蒙”，也可在任一系统下切换两种账号。切换系统不自动切换身份；选择接收方演示账号必须显式进行，角色展示始终清楚。

例：iOS 下确认早餐已服用，再切鸿蒙或安卓，仍显示同一任务已记录；填写一半计划时返回桌面、换系统再打开，原文字、卡片选择和草稿都在。切换账号／长辈则按对象隔离会话，不能把上一位长辈的草稿带入新对象。首轮用同一浏览器切换账号演示家庭共享，不承诺两台实体手机或两个浏览器已经同步，也不要求新增双手机并排业务实例。

### 9.3 长辈端聚焦打卡全屏卡片

**最新明确需求：**长辈进入首页时，对当前自然时段内已到提醒点、尚未记录、仍适用且不在保护期的任务自动呈现聚焦卡。未到提醒点可在任务列表主动记录，未来时段不能预打卡。延后到期可定位原任务，包括跨时段／跨日任务；普通逾期任务继续由折叠卡、语音或对应通知进入，不因首页存在任意未记录任务而反复强制弹出。

**出现、排队与退出规则：**

| 场景 | 行为 |
| --- | --- |
| 主动关闭／稍后查看 | 提供始终可见、有可访问名称的关闭按钮；不写服药事实，不消耗延后机会；通知进入后关闭须定位对应日期／时段的药物列表，原任务状态保持，用户可继续逐药打卡 |
| 本次前台访问内重新渲染或切换页面 | 同任务同一轮被关闭的自动展示不马上重开；下一次前台访问可重新判断，用户主动点通知／任务仍可打开 |
| 多种药同时需要处理 | 按原提醒时刻、稳定任务顺序逐项展示“第 X／Y 项”；单项动作只影响一项，关闭可退出整个队列，不能批量代打 |
| 记录成功 | 显示声明结果及 5 秒撤销；当前任务移出队列，有下一项则继续显示，否则关闭；各页面共享数据一致更新 |
| 设置延后 | 当前任务移出本轮队列，保护期内不自动再弹；有其他任务则继续队列，没有则关闭；到期产生新提醒轮次 |
| 正在填表、对话输入、模拟语音或有其他弹窗 | 自动提醒排队或仅显示非阻塞提示；即使主动点击通知也保留消息、未发送文字和草稿，完成／保留退出当前流程后可处理通知，不覆盖未保存输入；未来真实录音同样处理 |
| 模拟桌面／未来 APP 后台或锁屏 | 首轮由外层模拟桌面显示通知，应用保持隐藏，不由内层抢回画面；未来后台／锁屏由操作系统展示通知，打开 APP 后才显示应用内聚焦卡，不承诺强行唤起全屏 |
| 旧通知／重复通知 | 重新鉴权并读取最新状态；已记录显示结果，已取消显示失效说明，无权限不展示健康详情，不恢复旧的可打卡按钮 |

以下为到点提醒的布局示意；逾期补记标题改为“这次用药还没有记录”，状态按第 7.3 节显示。示例餐时只在计划已有该设置时出现。

**最新必做路径：**第 9.4 节“先关闭提醒页，再在对应时段列表打卡”必须可完整操作。下方聚焦卡中直接声明和延后的已有能力仍保留，不能用它替代关闭后返回列表的路径。同一前台访问内刚关闭的同轮任务不因重渲染立即重开；跨日通知关闭后定位原日列表／历史任务，不把它错误归到今天早餐。所有出口保留清楚的返回和手动操作。

```
+-------------------------------------------------------+
|   第 1 / 2 项                         [关闭/稍后查看] |
|                       [ 闹钟动画 ⏰ ]                 |
|                                                       |
|                    该吃药啦！ (28px 粗体)              |
|              08:00 · 饭后服用 (16px 灰色副标)          |
|                                                       |
|   +-----------------------------------------------+   |
|   |                                               |   |
|   |         💊  降压药 (络活喜) (32px 粗黑)        |   |
|   |                                               |   |
|   |            计划 1 片 · 餐后 (20px)             |   |
|   +-----------------------------------------------+   |
|                                                       |
|          本次任务仅有 1 次延后机会                     |
|                                                       |
|   +-----------------------------------------------+   |
|   |               [  ✓  已 服 用  ]               |   |  <- 60px高 翠绿大按钮
|   +-----------------------------------------------+   |
|                                                       |
|   +-----------------------+ +---------------------+   |
|   |     延后 5 分钟       | |     延后 30 分钟    |   |  <- 48px高 暖橙色次级按钮
|   +-----------------------+ +---------------------+   |
|                                                       |
|   +-----------------------------------------------+   |
|   |        未服用         /      本次无需服用      |   |
|   +-----------------------------------------------+   |
+-------------------------------------------------------+
```

**核心元素与适老化布局：**
1. **顶部视觉锚点**：关闭入口、队列进度、闹钟 SVG 及约 `28px` 大标题；到点显示“该吃药啦！”，逾期补记显示“这次用药还没有记录”。闹钟轻微摇摆，减少动态效果时静止。
2. **中心药物大字看板**：
   - 药品名称：`30px~32px` 特大字号，深色高对比度居中。
   - 计划剂量与要求：`18px~20px`，显示原日期／时段、计划单次量、提醒时刻及可选餐时；未设置餐时或备注则不自行补“餐后／温水送服”等要求。
3. **关键约束文案**：
   - 展示 `* 本次任务仅有 1 次延后机会`；机会用完时禁用按钮，提示 `* 延后机会已用完，请按实际情况记录`。未记录状态按第 7.3 节计算，打开页面不自动打卡。
4. **动作按钮梯队**：
   - **第一主行动（已服用）**：满宽绿色按钮（高 `56px~60px`、字号 `22px` 粗体），点击直接提交本人声明，不再核对实际时间或数量；显示反馈和 5 秒撤销，再按队列规则继续／关闭。
   - **第二行动组（延后提醒）**：并排 `延后 5 分钟`、`延后 30 分钟`，按第 7.3 节资格启用，成功后当前任务退出队列；到期通知与前台卡片分别按运行状态处理。
   - **其他事实操作**：清楚可点的 `未服用`、`本次无需服用`；后者沿用第 7.1 节简短确认。不能把未服用只藏在语音或复杂菜单中。

标准字号下延后与其他事实按钮至少 `48px` 高，大字号时适当增高；关闭按钮也保持足够点击面积。长药名和大字号可滚动，不让操作被底部安全区遮挡。保存后的详情依第 7.6 节显示补记／更正信息。

### 9.4 手机顶部模拟横幅、演示控制与完整操作链

**明确需求：**小型演示控制区放在手机外壳外，首轮直接控制 `demo.html` 的三系统主题、模拟桌面和通知场景，不把开发控制按钮混入长辈的日常操作区。

| 控制 | 作用与边界 |
| --- | --- |
| 系统：iOS／鸿蒙／安卓 | iOS 默认；同步切换桌面、系统图标、通知、应用内主题与动效；不刷新应用、不清空数据 |
| 演示账号／当前长辈 | 显示或切换现有演示账号与授权对象，复用原有权限；与系统选项独立，子女只能选择已授权长辈 |
| 返回桌面／打开应用 | 隐藏／恢复同一应用实例；桌面“银龄健康管家”图标也可打开应用，动效响应实际点击 |
| 触发 N1／N2／N3 场景 | 从原应用取得当前有效任务和可用原因，明确显示模拟接收端。N3 的正常发起仍由子女点击“提醒 TA”，不能靠控制区绕过角色、保护期或冷却 |
| 演示时刻／场景准备 | 使用固定演示日与受控时刻准备早餐到点、超时等场景；显示所选时间并先校验条件，不要求现场等待真实钟点，不凭触发按钮伪造逾期 |
| 重播横幅 | 对已有模拟通知重播外观与转场，不另发一条逻辑提醒、不重置冷却；点击后仍读最新任务，已记录则进结果页 |
| 恢复初始演示数据 | 复用现有明确确认动作，恢复任务、主题默认值及演示状态；这是重置记录，不能与仅回桌面／切主题混为一谈 |

外部控制区持续保留简洁“比赛演示／模拟系统通知”标识；手机内通知仍保持高保真。原型模拟的桌面是网页状态，不以浏览器真正退到后台作为触发前提，不请求浏览器系统通知权限。

- **通知形态**：高度仿真的手机顶部下拉 Push Banner，包含应用图标、应用名“银龄健康管家”、时间标签“刚刚”、标题与详细药名及剂量。
- **三系统样式切换**：
  - **iOS 风格**：毛玻璃圆角卡片，SF Pro 字体质感，右上角时间，微阴影。
  - **鸿蒙 (HarmonyOS) 风格**：微拟物胶囊通知条，纯白高亮卡片，圆角特征。
  - **安卓 (Android / Material You) 风格**：左侧单色圆圈图标，大标题+展开式二级内容。
- **点击响应**：长辈端按第 9.3 节定位关联任务，保护已有草稿并读取最新状态，已记录任务打开结果页。子女收到 N2 则进入对应长辈看板或历史，不能打开代打卡操作。

上方三套横幅是各自视觉方向，最终须与第 11.4 节选定的同一系统参考版本一致；不能只有横幅换皮、桌面与应用内仍完全相同。苹果字体使用可用的系统字体及合理回退，不能假设在所有演示电脑都已安装专有字体。

**评委可亲手完成的长辈端主流程：**

| 步骤 | 画面与动作 | 数据与恢复行为 |
| --- | --- | --- |
| 1 模拟桌面 | 显示选定系统的壁纸、图标、Dock／状态栏，应用图标可点 | 内嵌应用已初始化并完成主题同步，保留原会话，尚未记录任何服药事实 |
| 2 出现横幅 | 演示者准备有效场景后触发，顶部横幅滑入 | 药名、剂量、时段、对象来自实际演示任务；写入模拟通知状态，不改变服药记录 |
| 3 点击通知 | 从桌面切入应用，打开该次任务提醒页 | 当前态再校验；已记录显示结果、已取消显示失效、无权限不展示健康详情；草稿先保护 |
| 4 关闭提醒 | 点击清楚可见的关闭按钮，回到该日期／时段药物列表并定位关联药物 | 只改变界面位置，不算打卡、不消耗延后次数；同轮不立即自动重开 |
| 5 逐药记录 | 在列表点击“已服用／未服用／本次无需服用” | 走原有本人声明逻辑，保留无需服用简短确认与 5 秒撤销；一项动作只改变一项任务 |
| 6 查看结果 | 药物状态、当日统计、历史及子女视图更新；可返回桌面或换系统 | 实际保存本地演示数据；刷新后已确认结果保留，换系统后仍是同一记录 |

N2 的接收者是子女，点开应进入对应长辈看板／历史；上述步骤 4–5 的本人打卡只在长辈账号执行。N3 可先在子女 iOS 看板点击“提醒 TA”，再显式切换长辈账号及鸿蒙主题查看模拟接收结果；这展示家庭协同流程，不是跨手机传输。

通知进入／退出、应用展开／收回、提醒页关闭和按钮反馈是实际编写的交互动效。评委可以中途关闭、重新打开、查询其他任务或切换系统；本次不新增必须观看的自动播放影片或独立动画文件作为交付替代。

### 9.5 三类提醒：不可遗漏

**阶段说明：**以下类别、内容、资格和频控均是首轮必须演示的业务规则；接收“手机”在首轮表示 `demo.html` 中对应演示账号的模拟手机画面。N2／N3 在原应用本地状态中模拟生成和接收，未来才由服务端与真实手机推送通道执行。同浏览器常规演示读同一份已保存数据；“本机已确认、远端暂未收到”仅在专门标记的离线同步模拟场景中出现。

| 编号 | 触发条件与发起者 | 接收端 | 必须体现的内容与动作 |
| --- | --- | --- | --- |
| N1 到点用药提醒 | 仍适用且无记录的任务到达已设置提醒时刻；当前新建任务首次提醒按第 7.5 节；保护期内不重复发原提醒 | 长辈手机 | 时段、药名、计划单次剂量与单位；点击按第 9.3 节打开关联任务 |
| N2 超时未打卡提醒 | 达到统一超时条件后仍无记录；首轮由本地模拟调度判断，未来由服务端按尚未收到记录判断 | 已获授权的子女模拟手机／未来真实手机 | 长辈对象、日期/时段、药名、剂量、尚未记录／暂未收到记录的事实；进入相应看板或历史任务 |
| N3 子女主动远程提醒 | 已授权子女对已到提醒点、仍适用且未记录的任务发起；不在保护期且通过共享冷却检查 | 长辈手机 | 来自谁、原日期／时段、药名、计划剂量、尚未记录的事实；点击按第 9.3 节打开任务 |

每个通知关联明确的任务 ID、档案和业务日期，不能只是“记得吃药”的泛化消息。多位子女的具体接收人配置仍见 D10，去重和冷却已经确认如下：

- **N2：**首轮本地模拟调度、未来服务端均按第 7.3 节统一公式检查。每个任务每一轮超时只生成一次逻辑提醒；传输重试或重播横幅不能变成新一轮。先超时后使用延后时，暂停当前催促但保留历史；延后到期后若再次满足超时条件，可产生新一轮 N2。保护到期仍处于原自然时段时，不提前发 N2。
- **N3：**未到原提醒点、正在延后、有任意明确声明（含未服用）、已取消或无权限时均不可发起。按钮就地说明“尚未到提醒时间／正在延后／已有记录”等原因，不能让子女绕过保护或要求已声明未服用的人补吃。
- **共享冷却：**所有家属对同一任务共用 5 分钟冷却。首轮本地受理一次有效模拟 N3 后起算，未来由服务端受理时起算；冷却内显示剩余时间并拒绝新提醒，不按家属账号各算一份。重复请求使用同一操作标识，不因重试或重播横幅重复推送；切主题、切账号和刷新不能清零冷却。
- **状态联动：**本人提交声明后取消／抑制尚未触发的该次提醒；撤销或更正时重算资格，但不清零冷却、延后次数和已有发送痕迹。

用户原始设想要求 N3 显示“什么时段的什么药物没有按时服用规定的剂量”。**保留时段、药名和明确剂量，但根据已知事实调整表述**：尚未打卡时只能说“尚未记录”，不能断言已知没吃。

通知详情的建议示例：`小李提醒您核对：今天午餐时段的示例药 A，计划每次 2 包，12:30 提醒，目前尚未记录。若已服用，请按实际情况补记。` 这里的药名和剂量均为演示占位，不提供具体用药建议。

若旧通知对应任务后来已有“未服用”等声明，打开时据实显示最新记录，N3 不再可用；不能恢复旧催促按钮或写成“现在必须补吃”。任务取消、解绑或其他权限变化也按最新状态处理。

### 9.6 通知设置与隐私

**首轮 Demo：**提供明确标记的模拟通知开关和场景状态，可演示关闭通知时不出现新横幅、打开后按有效任务触发。开关不代表浏览器或真实操作系统已授权；系统设置说明使用标记为演示的面板，不伪装已经打开本机真实设置。普通桌面横幅可展示演示药名与剂量；如果演示锁屏隐私模式，按下方规则切换简洁预览，应用内完整详情不缺失。

**未来真实 APP：**用户开启消息通知后，按其对本应用的通知设置在设定时间提醒长辈。应用应提供真实系统授权状态、应用内提醒开关及进入系统设置的入口。以下系统授权和云推送条款均属于后续阶段。

声音、震动、横幅、锁屏预览等服从操作系统与用户配置；通知许可并不意味着 APP 能强制开启声音、绕过勿扰/专注模式或突破后台限制。不把应用内开关显示“开”当作系统已授权。

**隐私约束：**药名、剂量和健康信息可能暴露隐私。初版建议锁屏默认显示简洁提示，如“您有一条用药记录提醒”，解锁进入后显示完整时段、药名与剂量；用户可自主选择详细通知预览。此设置不能删除 APP 内必须显示的完整内容。云推送载荷尽量只带不暴露健康详情的事件引用，由已授权客户端取得正文。

拒绝通知后仍能查看与手动记录，界面说明“系统通知未开启”并允许稍后启用。不强迫用户开通知来换取基本记录能力。

### 9.7 首轮本地模拟与未来服务端协同

**首轮架构：**业务资格和持久化放在现有 `app.js` 内，外层只请求场景并展示横幅。N1 用演示时钟判断到点，N2 用同一超时公式，N3 从授权子女动作产生；通知类别、接收方、任务引用、轮次、去重和冷却可本地记录。需要展示接收端时显式切换现有演示账号，保留同一应用和同一份家庭数据。

可以用前端定时器驱动可见页面中的动画或检查受控演示时刻，但现场同时提供人工触发，不依赖真实后台调度或等待实际时间。所有模拟发送／接收反馈清楚标记；实际记录一旦更新，外层通过消息同步最新摘要。模拟断网、待同步和迟到接收是明确的场景状态，不能让用户误以为当前有运行中的远端服务。

**未来真实 APP 建议架构：**N1 优先交给手机操作系统安排本地通知；N2 由后端依据共享任务和记录状态判断；N3 由子女端发起，经后端鉴权，再由系统推送通道到长辈端。云端必须有真实账号、家庭授权、设备绑定、任务调度与推送凭据管理。以下真实网络与消息可靠性约束不要求在首轮架设后端。

不依赖网页 `setTimeout`、前台轮询或要求 APP 一直亮屏来完成真实通知。服务端不能因为长辈手机网络断开，就确定长辈没吃药；离线时文案应表述为“暂未收到记录”，并考虑最后同步时间。

计划修改、停用、任务已记录、撤销或重装后的提醒安排要更新，过期本地通知与远程消息需要取消、抑制或在打开时纠正。同一任务的多个通道应去重，不能本地、远程、重试各弹一遍。

离线记录和延后先在本机保存，明确显示待同步；服务端尚未收到时可能仍发出“暂未收到记录”的 N2，不能保证已同步抑制。恢复网络后用唯一操作标识合并记录、保护期和通知状态，取消可取消的待发消息，已送达消息在打开时纠正。跨午夜星星按第 7.7 节区分本机确认时间与接收时间。

### 9.8 首轮验证与未来实机验收的分界

**首轮通过条件：**在本地静态服务和目标演示浏览器中验证内外主题一致、三类模拟通知、通知关闭后列表打卡、状态与持久化、旧消息、重复触发、权限、草稿保护、两种字号与多视口。可以用实际手机浏览器检查触摸和布局，但不要求安装 APP 或真实系统收到通知。验收 A01–A84 中有效项，均不能用已播放一段视频代替交互验证。

**以下保留为未来真实 APP 的可靠性／实机要求，另按 P01–P06 验收：**

至少区分：已创建提醒任务、推送服务已受理、设备送达（仅在通道确有回执时）、用户打开、用户完成记录、发送失败/过期/状态未知。“发送成功”不代表设备收到，更不代表老人服药；APNs 官方资料也明确远程通知送达不保证 [S2]。

真实 APP 必须验证：前台、后台、锁屏、断网恢复、关通知、重启、系统限制、重复点击提醒、重复投递、设备 token 变化、计划修改、解绑家庭、切换设备和跨日。针对用户/系统终止进程的行为按平台实际限制记录，不能在没有结果时笼统宣称支持。

N1 应在受支持且权限正常的设备上按配置触发；N2/N3 必须由两个独立账号、不同真实设备完成端到端验证。延迟目标、测量样本与失败容忍范围见 D10，须在发布前量化，不在本文件虚构“100%准时”。

失败时保留提醒记录和可解释状态；适用时有限重试，超过时效停止发送。没有设备回执的通道显示“结果未知”，不伪造“已送达”。这三类提醒不是紧急救援系统，本次范围不新增电话、短信或 SOS 联动。

### 9.9 未来真实 APP：平台差异与最小接入条件

本节保留先前版本的真实通知技术信息，用于后续 APP 选型；不进入首轮前端开发依赖、演示通过率或工期承诺。用户尚未选择 Flutter、React Native、WebView 容器或原生框架，网页外壳也不能自动解决后台通知和跨设备授权。

| 平台 | 本机到点提醒 | 跨设备远程提醒 | 验证重点 |
| --- | --- | --- | --- |
| iOS | 通过系统 User Notifications 安排本地通知，申请并检查授权 | 服务端与 APNs、设备 token 及账号绑定配合 | 系统授权、声音/横幅/锁屏设置、专注模式、后台/离线/重装与计划更新；不能承诺绕过系统限制 [S1][S2] |
| Android | 系统通知与定时机制；根据系统版本评估闹钟权限与后台限制 | 使用适合发行地区和机型的推送通道，不能假设所有手机都有可用 GMS | Android 13+ 通知运行时权限、适用版本的精确闹钟权限、厂商省电设置、重启恢复和通道开关 [S3][S4] |
| HarmonyOS | 评估目标版本 Notification Kit 与后台提醒能力 | 评估对应版本 Push Kit、设备 token 和云侧服务 | 区分 HarmonyOS 原生应用与兼容 Android 的设备环境；逐版本核对权限、后台提醒和推送能力 [S5] |

HarmonyOS 官方入口已定位，但部分正文需动态加载；本文件没有验证具体 API 签名、配额或所有版本支持范围，实施时必须针对选定 SDK 再核对。不得把鸿蒙设备统一视为 Android，也不得假设一个插件自动覆盖三套系统。

具体支持的系统最低版本、设备范围与首发平台为 D09。未来各系统分别实机验收，iPhone 风格页面不代替其他平台测试；首轮仅以三套演示主题的视觉及操作效果验收。

## 10. 本次功能范围与无付费门槛

> **最新明确决定：**本次保留第 0.5 节列明的完整比赛 Demo 范围，包括手动输入、AI语音输入及双端首页助手。取消的模块不因旧文档曾列为“全员开放”而继续开发；真实服务后置不等于删除其未来技术要求。

### 10.1 本次移除与后续清单

| 能力 | 当前处理 |
| --- | --- |
| 拍照识别、扫码识药 | 从添加计划流程中移除，无按钮、图片选择或模拟识药占位 |
| 健康报告导出 | 本次不做报告生成、打印或下载入口；不将其继续列为基础功能 |
| 陪伴形象、换装、气泡引导 | 本次全部移除，无素材、入口或当前数据字段要求；构想见独立的[《其他待实现功能》](老年人智慧健康管理_其他待实现功能.md) |
| 存钱罐、红包、充值提现、PRO 会员 | 继续取消，不因保留后续形象构想而恢复资金／会员机制 |

形象与气泡移除后，两端首页右下角助手仍由第 6 章独立实现。这里取消的是陪伴形象的气泡引导，不是聊天页面的消息气泡。原始附录、归档文档及历史来源说明中的旧词只用于溯源，不形成当前功能入口。

### 10.2 保留功能平铺开放

手动输入、AI语音输入、首页语音助手、授权多长辈管理等均无 PRO 或会员门槛；权限仍由账号与家庭授权决定，“全员开放”不等于子女可代打卡。

子女在 AI语音输入流程中提供整段既有医嘱时，可沿用同一草稿整理与人工核对，文本修正也在该流程内完成，不增加第三个模块。首轮仅自动处理明确支持的表达；复杂医嘱保留原文并引导手动填写，不承诺任意处方文本解析。同一药早中晚服用形成一条含三个时段的计划；三个时段均适用的完整生效日生成三次任务，新建当天仍按第 7.5 节排除已经结束的时段。不同药品分别核对，不静默批量保存。不支持的不同剂量、非每日方案仍按第 5.2 节保留原描述并转人工处理，模拟解析必须有标识。

**“我的”页面调整：**
- 移除会员开通区域、PRO 权益卡片、订阅方案占位。
- 保留：账号与家庭绑定、演示设置、隐私设置、通知设置、字号模式、关于/帮助、意见反馈。
- 不展示任何付费入口、会员状态、自动续费相关内容。

### 10.3 后续实现清理要求

后续代码实施时移除 `isPro`、`proEnabled`、`subscriptionStatus` 等会员判断，以及本次取消能力的运行时入口、占位 UI 和无用数据要求。界面不展示会员、资金奖励、形象换装或报告下载服务。清理不能破坏保留的手动／语音流程、家庭权限、历史来源和用户记录；本轮仅修订文档，不声称现有代码已经完成清理。

### 10.4 首轮做完整的部分与后置部分

| 层面 | 首轮比赛 Demo 必做 | 后续阶段再做 |
| --- | --- | --- |
| 交付入口 | 独立手机演示舞台＋原应用联动；原入口仍可使用 | 原生／跨平台客户端安装包、分发与上架 |
| 视觉与交互 | 三系统主题；iOS 重点打磨；可实际触发和打断的 UI 动效 | 全面复刻整个操作系统、原生系统级手势及设备触觉能力不属首轮承诺 |
| 通知 | 三类通知的任务资格、控制触发、横幅、打开与关闭、打卡联动、去重和冷却 | 系统授权、本机通知调度、真实远程推送、设备送达回执及实机可靠性 |
| 助手 | 两个聊天入口、主动引导、文字交互、示例语音、有限解析、缺项追问、配置卡和原计划查询 | 真实麦克风识别、ASR 供应商、复杂自由对话模型、方言支持和云处理 |
| 数据 | 本地创建／确认／声明／更正、历史与看板实际更新，跨主题和账号读取同一家庭数据 | 真实身份、跨设备同步、服务端权限及冲突处理 |
| 基础产品 | 看板、星星日历、BMI、自然时段、延后、历史补记、适老字号及原权限 | 不因后置服务而削减这些当前业务规则 |

首轮实现顺序见第 15.1 节；“模拟语音／通知”不能成为只交静态截图、固定录屏或不保存结果的理由。也不因目标包含 AI、三系统就引入 React、Expo、Swift 或新的依赖管理体系。

## 11. 视觉修复与适老可访问性

### 11.1 加号相关图标形变与错位

**用户反馈：**目前点开任何一个加号，用药信息和身体数据的图标会畸变、形状不正常或位置不正确。v2 要统一修复，不能只修某一个页面的截图。

**实现检查建议：**检查 SVG 的 `viewBox`、宽高比、容器宽高、居中方式、flex 压缩和继承样式；主体图标、尾部箭头、复选框和按钮分别管理尺寸。现有 SVG symbol 与 `icon()` helper 应继续统一复用，不通过扭曲图标填满容器。

检查范围包括：两端各页 `+`、长辈首页快捷卡片、用药表单两种模式、身体数据类型选择、核对页及原型中仍保留的快捷添加组件。即使跨类别二选一入口被取消，也要检查迁移后的图标，没有使用的旧入口不必为演示而继续暴露。

### 11.2 未按时打卡提示红点

**明确需求：**未按时打卡提醒不再用 `⚠️` 警告三角形，改为类似红绿灯红灯的小圆红点，可以闪烁。

红点与“未按时打卡提醒（N）”文字共同展示；点是圆形，大小克制。折叠/展开时只有独立箭头可以旋转，红点和标题不旋转、不翻转、不变形。

**可访问性约束：**闪烁不是唯一提示，也不是强制动画。初版建议慢速轻微明暗变化，开启“减少动态效果”时保持静态；避免快速闪烁，不使用每秒三次或更快的闪动。正常不播放刺耳声音。红点表达待核实记录，不代表健康风险诊断。

### 11.3 整体版式

| 场景 | 要求 |
| --- | --- |
| 主配色 | 沿用现有健康绿、白色卡片、柔和背景；黄色星星、珊瑚红错误提示各有明确用途 |
| 圆角 | 快捷卡片柔和、餐时分段按钮稍克制、状态标签紧凑，不把所有控件做成同一种大胶囊 |
| 图标 | 固定比例，居中，无压扁/拉长；不能被长文字挤歪 |
| 时段四列表 | 保留四列对应关系；范围可换行，时间和餐时可点击；大字号时增加行高，不靠缩小文字硬塞 |
| 日历 | 日期与星星都可辨认，七列不溢出；非完成日无日期底色 |
| 悬浮按钮 | 语音入口与底部导航、安全区不重叠，不遮住最下方药物操作 |
| 聚焦打卡卡片 | 全屏模态、关闭入口、队列进度、三种事实操作、可减少的闹钟动效；大字号下按钮高度自适应 |
| 顶部通知横幅 | 三系统外层主题同步，毛玻璃／鸿蒙圆角卡／Material 风格一致；点击按最新任务状态进入聚焦卡或结果页；控件在手机框外 |
| 验证视口 | 至少窄屏移动视口、常见 iPhone 尺寸及桌面预览；每种都检查标准字号和适老大字号 |

**技术可访问性要求：**使用语义按钮、输入、标签与列表；保留键盘焦点、弹窗关闭后焦点恢复、`aria-label`、`aria-live` 与背景 `inert` 行为。必填、错误、完成、当前餐时均需文字或可访问状态，不能只依赖颜色。聚焦打卡卡片的红色提示文案需可被屏幕阅读器读出；延后按钮置灰时需有 `aria-disabled` 与文字说明。

<a id="platform-visuals"></a>

### 11.4 三系统视觉：iOS 优先，桌面到应用连续一致

**已确认优先级：**首轮主要以 iOS 演示，希望通过苹果界面的材质、层级和操作感提升产品质感；鸿蒙与安卓各自做到风格清晰、整体协调。三者都能完成同一业务流程，但不要求以同等投入复刻所有系统细节。主题与角色独立，重点场景可以使用“子女 iOS、长辈鸿蒙”。

开发开始时选定一致的 iOS 参考版本，以及一套鸿蒙参考和一套安卓 Material 参考，记录所用视觉参考；不混用各代设计，也不把某一种安卓风格称为所有厂商的统一外观。具体参考版本属于 D17 的视觉实施选择，不改变已确认的 iOS 优先级。

| 层次 | iOS 重点打磨内容 | 鸿蒙方向 | 安卓方向 | 共同约束 |
| --- | --- | --- | --- | --- |
| 桌面壁纸 | 有空间和光感层次，能衬托图标与半透明材质 | 匹配选定鸿蒙参考的壁纸色彩和桌面秩序 | 匹配所选 Material 桌面参考的配色与留白 | 本地素材可用，无需现场加载远程图片；不在阅读任务时持续移动背景 |
| 系统应用图标 | 电话、信息、相机、设置等有苹果风格的图形、轮廓、间距与文字标签；健康管家图标清楚可点 | 独立的系统图标素材、轮廓和排列，不能直接沿用苹果图标组 | 所选参考的图标形状、排列和强调色，不能只换壁纸 | 使用合适授权／自制的本地素材；辅助图标建立语境，不扩展为完整系统应用功能 |
| Dock 与状态栏 | 半透明 Dock、时间／信号／电池及底部指示条的比例、留白和对齐 | 鸿蒙对应的状态栏与底部区域特征 | 安卓参考对应的状态栏与导航区域 | 内外层不重复绘制两套状态栏，不让安全区遮挡内容 |
| 通知 | 毛玻璃圆角表面、背景透色、细描边／阴影、图标和文字层级，平滑滑入及收回 | 与所选鸿蒙版本一致的圆角卡片、排版和视觉重量 | Material 通知卡、图标／标题／展开内容层级 | N1／N2／N3 的业务内容相同，尺寸随长药名和字号适配 |
| 应用打开／返回 | 点击桌面图标从其位置连续展开应用，退出沿对应关系收回；点横幅则自然进入关联任务 | 与鸿蒙参考相符的展开、收回和速度感 | 与 Material 参考一致的缩放／淡入和页面过渡 | 外层动画不重建应用，不等待完整动画结束才接受用户操作 |
| 导航与表单 | 苹果风格的导航栏、底部导航、分段按钮、滚轮、弹窗与底部面板；即时按压反馈 | 对应风格的圆角、间距、控件层级及面板 | Material 风格的按钮、状态反馈、选择控件和面板 | 同一功能字段、时段规则和权限不因系统改变；健康绿、中文内容和两种字号统一保留 |
| 聊天与提醒 | 消息、卡片、输入栏和提醒页进入退出连续，毛玻璃用于适当悬浮层 | 在各自主题中保持同等可操作性 | 在各自主题中保持同等可操作性 | 不用动效隐藏核对内容；未发送文字、草稿、任务状态保持 |

毛玻璃优先用于 Dock、通知横幅和导航等悬浮层，药名、剂量、测量值及打卡按钮保持高对比、稳定的阅读背景。半透明层不层层叠加到无法辨认文字；不支持模糊或用户要求减少透明度时使用更实的底色，不能破坏信息层级。字体优先使用可用的平台系统字体并提供回退，不为模拟字体偷偷引入网络加载。

### 11.5 动效实现与体验检查

动效属于可操作页面本身：按下立即反馈、打开说明去向、关闭沿合理路径返回、状态变化清楚可辨。常用打卡动作不能为展示效果故意延迟；动画过程中仍可关闭或改变操作，连续点击不引起跳帧、重复保存或卡死。

| 交互 | 动效目标 | 实现建议与检查 |
| --- | --- | --- |
| 按钮／图标按压 | 让用户立即知道触摸已被接收 | 按下时轻微缩放或明暗反馈，松开恢复；取消按压不提交业务；键盘操作也有清楚焦点反馈 |
| 桌面图标打开应用 | 表达图标与应用之间的空间关系 | 从实际图标区域展开并结合遮罩／圆角过渡，外层容器承担；返回沿对应方向收回，不从零大小凭空弹出 |
| 横幅进入／退出 | 顶部通知自然到来、可点击、可关闭 | 保持相同来去方向，支持快速重复操作时从当前呈现继续；重播只是视觉操作 |
| 提醒页／底部面板 | 保留上下层关系与返回路径 | 转场连续且可关闭；支持触摸的部分有点击替代，不把未完成输入丢到背景 |
| 卡片配置与聊天回复 | 显示新信息及修改结果 | 短而克制的过渡，保持正在阅读的位置；模拟处理明确标识，不人为加入长等待营造假识别 |
| 打卡成功／撤销 | 状态变化、保存反馈与可纠正性清楚 | 先及时显示操作结果，保留 5 秒撤销，不以长庆祝动画遮住后续药物 |

实现优先用原生 CSS transition、Web Animations API 及必要的 Pointer Events；只为确有需要的可中断手势设计弹性，避免给每个控件添加夸张弹跳。优先动画 `transform`、`opacity` 等低成本属性，毛玻璃与大面积材质实际检查性能；不为了一个淡入安装动画库。初始时长、曲线及弹性参数参考所选技能，属于开发调优值而非用户新业务规则。

交付前在正常速度、慢放／逐帧下检查图标原点、通知进退、面板衔接、快速反向操作、文字清晰度与无闪烁；在演示浏览器及可用触摸环境检查两种字号。支持 `prefers-reduced-motion`，用更轻的淡入或即时状态变化替代大幅移动；高频状态数字和药物内容不为装饰持续跳动。减少透明度／高对比偏好下仍保持读得清、点得到。

### 11.6 用户指定的设计与动画技能参考

下列名称全部保留作为实施与复核参考。技能不改变本项目原生 HTML/CSS/JavaScript 技术栈、首轮范围、权限或无依赖原则；具体外观仍通过实际页面验证，不能仅因调用技能就宣称已还原某操作系统。

| 技能 | 本项目用途与阶段 |
| --- | --- |
| `apple-design` | 已在方案阶段阅读；指导苹果材质、空间层级、即时响应、连续进退、可中断交互和适老可读性 |
| `emil-design-eng` | 已在方案阶段阅读；指导排版、组件细节、按压反馈、动效节制与性能检查 |
| `animate` | 已在方案阶段阅读；实施时按动效目的选原生实现，补齐进入／退出、连续操作和减少动态效果 |
| `animation-vocabulary` | 实施沟通时明确“展开、回弹、毛玻璃、面板进退”等效果的术语，避免只用模糊形容词 |
| `prototype` | 需要比较具体视觉细节时，在可操作原型中对比；三系统切换本身仍按已确认方案，不新增必须先选型的审批流程 |
| `find-animation-opportunities` | 实施前识别真正需要反馈或转场的位置，避免全页面堆动画 |
| `improve-animations` | 有可运行页面后检查整套动效的一致性、性能和改进优先级 |
| `review-animations` | 实现后复核进入退出、连续触发、曲线、时长与可访问性，不把尚未运行的设计写成已通过 |
| `pick-ui-library` | 若确有组件缺口再评估；首轮优先复用现有语义控件和原生实现，不因列表中有该技能就安装库 |
| `ask-sonner` | 可参考通知反馈的组件思路；其 React 库用法不直接引入当前项目 |
| `animate-expo` | Expo／React Native 技术栈适用时再使用；不作为纯前端 Demo 的依赖 |
| `write-swift` | 后续确定原生 Swift 开发时再使用；当前不生成 Swift APP |

除明确写“已阅读”的三项外，其余在对应实施／复核阶段按用途加载；本次未声称已应用全部技能或完成视觉审查。

## 12. 数据、权限与迁移要求

### 12.1 新字段必须与业务含义对应

以下是满足需求的概念字段建议，不是已确定的数据库建表或本轮代码改动。真实服务字段与首轮本地演示字段分开；后续设备 token、真实授权和服务端接收时间不得在首轮伪造。

| 对象 | v2 需要表达的信息 |
| --- | --- |
| 药品计划 | 所属长辈、药名、计划单次量／单位、起止／周期、来源、备注；每时段提醒与可空餐时；计划版本及确认生效时刻 |
| 当次任务 | 唯一任务标识、计划关联、长辈、北京时间业务日期、原时段起止、原提醒、药名／计划剂量／餐时快照、版本、适用或未来取消状态及原因 |
| 服药记录 | 关联任务、本人三种声明、操作者、输入方式、本机确认时间、服务端接收时间、唯一操作标识、补记标记及版本；主动提供的实际时间／数量可空，不是提交必填字段 |
| 更正与撤销 | 原声明、新声明、操作者、修改时间、关联版本与撤销范围；不能连带回滚延后次数或通知冷却 |
| 延后提醒 | 原任务、原截止与提醒、新提醒完整时刻、发起人、确认时间、有效性、每任务已用次数 0／1、同步状态；不改实际服药事实 |
| 身体数据 | 指标名、数值、单位、测量时间、备注、来源；BMI 所用身高与体重记录引用 |
| 助手会话与草稿 | 两种入口上下文、账号／档案／日期、消息、原始文字、模拟示例来源、已知／缺失／冲突／待确认字段、人工修改、当前输入模式、核对状态和恢复位置；首轮无真实音频 |
| 首轮模拟通知 | 类别、接收演示账号、任务与提醒轮次引用、模拟请求标识、模拟生成／展示／打开状态、到期时间及 N3 家属共享冷却；不伪造设备送达 |
| 未来设备与通知 | 真实账号／设备绑定、平台、token、授权、发送尝试、通道回执、服务端接收与过期状态；只有接通后才有真实值 |
| 每日完成 | 日期、时区、长辈、应处理任务集合与取消依据、当日截止时记录／规则版本、当日动态结果及历史结果；迟到同步的校正依据，与次日首次补记区分 |
| 聚焦展示 | 当前前台访问、任务队列、已关闭的任务与轮次、草稿恢复位置；展示关闭不写成服药事实，不混入跨日任务身份 |
| 三系统演示状态 | 沿用 `notificationStyle`（`ios`、`harmonyos`、`android` 三选一）作为整个演示系统主题的唯一标识，扩展作用到桌面与应用；舞台桌面／应用状态、就绪／主题同步状态、通知开关、活动横幅与受控场景 |

**当前不需要的字段：**资金／会员相关的 `piggyBank`、`rewardBalance`、`transactions`、`subscriptionStatus` 等，以及形象／换装相关的 `mascot`、`mascotUpdatedAt` 等，不再作为 v2 数据要求；后续清单不是提前加字段的依据。

### 12.2 权限保持与家属协助

| 行为 | 长辈 | 子女 |
| --- | --- | --- |
| 查看和记录自己的数据 | 可以 | 不适用 |
| 查看长辈档案与协助添加计划/测量 | 自己的档案 | 仅已授权长辈，保存前确认对象 |
| 编辑／停用计划 | 可管理自己的后续安排 | 仅已授权长辈，核对生效影响；不能取消已开始任务以改写统计 |
| 声明已服用、未服用或本次无需服用 | 由长辈本人确认 | 延续 v1，不开放远程代打 |
| 5 秒撤销、历史补记和更正 | 仅本人任务；停用计划的保留任务仍可处理 | 不开放，不因具有计划编辑权而获得服药事实修改权 |
| 设置延后 | 仅本人符合资格的当次任务 | 不开放代设置，不能通过语音绕过 |
| 语音更改打卡事实 | 与本人手动记录相同权限 | 不能通过语音绕过子女权限 |
| 首页点击／长按进入对话 | 可使用本人助手全部适用意图 | 可查询授权长辈、协助添加、符合条件时提醒；不会出现可代打的操作 |
| 查询今天吃什么／几点／计划量 | 读取本人既有计划及任务 | 读取当前已授权长辈既有计划及任务；无权限不返回详情 |
| 发起家庭远程提醒 | 接收与处理自己的提醒 | 仅对有权限且符合第 9.5 节条件的任务发起，遵循家属共享冷却 |
| 子女拿长辈手机协助录入 | 长辈同意后可以协助添加 | 不因持有手机就获得代声明服药事实的产品授权 |

长辈手机上的实际操作者不可能仅靠语音或账号自动准确识别。若需要标记“家属协助”，应让用户明确选择并记录来源，不假称系统已判断说话者身份。用户本次允许的协助添加行为不能被写成子女可远程代打卡。

### 12.3 原型数据兼容与真实服务迁移

当前原型继续走 `commit(change)`、`validData()` 与统一转义；扩展字段时同步默认数据、创建表单、核对页、列表、历史和恢复演示数据。用户输入的药名、单位、备注、识别文字等进入 HTML 前均要转义。

需为 v1 存档增加有版本的迁移或明确的降级策略。旧“早餐后/午餐后/晚餐后”与新“早餐/午餐/晚餐 + 可选餐时”不是单纯同名字段；不得在缺乏原始证据时把旧系统默认名称迁移成用户已经确认“餐后服用”。初版建议标记旧来源、提示核对餐时，未核对前不新增医学含义。

固定演示日不变；可提供本月历史种子数据用于星星演示，但不得称为真实历史。无效存档、空数据、恢复初始数据、旧枚举显示和重复任务都应验证。

旧 `taken_on_time`／`taken_late` 如有本人服用声明，当前显示统一为“已服用”，保留旧枚举与来源供追溯；旧记录时间不能补成实际服药时间。旧星星按可追溯的任务和记录依据迁移到最新规则，不能保留“未服用也点星”的旧结果；缺少当日依据时提示数据不足，不凭空补亮。旧拍照来源说明可保留为历史来源，不恢复当前拍照入口或要求图片。

**最新迁移清理：**恢复初始演示数据时清理资金／会员及形象／换装废弃字段，不再默认创建形象；`notificationStyle` 默认 `"ios"`，日期偏移归零。计划生效、任务快照、可空实际细节、撤销／更正、延后次数、共享冷却和星星规则都要纳入有版本的迁移、校验及恢复；不得只换文案而继续用旧超时逻辑。

真实 APP 的跨设备状态必须由服务端鉴权和校验，不能以本地存储权限为准。离线写入要有唯一请求标识、可见同步状态和冲突处理；绑定撤销后停止新数据访问及相关通知。已发生的历史更正、通知记录按合法留存规则审计，不通过删除历史来掩盖差异。

### 12.4 首轮演示壳、应用与会话的状态边界

**单一业务来源：**药品计划、当次任务、声明、更正、身体数据和提醒资格仍由原应用管理，经 `commit(change)` 校验后保存在 `silver-health-data-v1`；`demo.html` 不自己修改该键，也不维护另一份可写药物数据。现有 `silver-health-view-v1` 继续承担当前账号、页面等视图信息，可有版本地扩展主题和演示状态；最终结构由实现确定，不能出现两个互不一致的主题标识。

主题／舞台配置与账号权限独立。外层通过消息取得摘要，存留的横幅内容只是展示快照，打开与操作时回到内层检查最新事实。所有外层接收的药名、单位、用户名、聊天文字和通知正文同样需要转义；不能把消息来源可信误当成其中所有文本可直接拼 HTML。

未提交聊天消息和工作草稿首轮可保存在当前应用实例的会话内存中；必须在切主题、返回桌面、打开通知或临时转入设置卡时保留。若切换账号／长辈，按会话所有者与目标对象隔离，不将私人消息展示给另一账号；返回原对象可继续。确认保存前再次检查目标权限、计划版本与任务状态。

**刷新策略：**已确认计划、打卡和测量记录必须从本地持久化恢复；首轮不把未提交聊天草稿的跨浏览器刷新恢复列为必做，刷新未提交草稿前按浏览器能力提示可能丢失。此限制不影响不重载 iframe 的切主题／回桌面保持承诺，不把草稿缺失写成保存成功。若后续选择持久化草稿，应另做版本、对象隔离与恢复校验，不默认长期保存真实音频。

恢复初始演示数据复用明确确认流程，清理新增的模拟通知、冷却、延后、会话草稿、关闭轮次及日期偏移，默认主题回到 `ios` 并同步内外层。与单纯返回桌面、重播横幅或切主题严格区分；这些展示动作不能重置业务历史。

## 13. 医疗、隐私、AI 与运营边界

### 13.1 医疗相关边界

1. 产品记录用户既有方案与实际情况，不诊断、不开药、不推荐药物、不调整剂量、不建议停药或补服。时段、提醒和餐时由用户依据既有处方/安排确认。
2. 助手辅助查询已有计划和整理用户输入；解析不确定、歧义或与人工输入冲突时澄清，不用模型常识补药名、剂量或疗程。首轮有限规则无法处理时回退表单，不虚构置信度。计划和测量必填项仍核对；服药声明不因缺少可选实际细节而强制追问。本次不接入 OCR 或扫码识药。
3. “未按时打卡”是记录问题，不等于漏服；“本次无需服用”是用户声明，不等于系统停药意见；BMI 是计算值，不是诊断。
4. 身体数据或通知不能自动触发处方变化。v2 不引入真实设备监测、疾病预警、紧急救援或医疗专业服务。

原型保留可见说明：`仅供演示，不提供医疗建议，不用于真实用药决策。` 真实产品也须说明适用范围与责任，并根据功能实际用途完成相应合规评估；一条免责声明不能代替必要的医疗安全设计。

### 13.2 健康数据和家庭共享

首轮全部使用本地演示资料，不采集真实麦克风、不向外部 ASR／模型上传内容，家庭共享以本地演示账号的权限为准。页面清楚标注本地模拟；以下真实个人信息处理和发布条件保留给未来服务，不要求首轮接通云端或取得虚构授权。

依据《个人信息保护法》，医疗健康、金融账户属于敏感个人信息；处理应有特定目的、充分必要性及严格措施，并依法取得单独同意、告知必要性与权益影响。处理敏感个人信息等场景应事前开展个人信息保护影响评估 [S7]。

需要分别说明：保存哪些健康资料、哪些子女可看、是否会发含详情的通知、是否使用云端语音、由哪些服务方处理、保存多久、如何撤回和删除。家庭绑定、麦克风权限和通知权限不是相互替代的授权。

长辈能查看和撤销家庭共享；撤销后停止后续访问和推送。真实服务应提供依法需要的查阅／复制、更正、删除／注销和投诉渠道，需继续保存的审计数据说明理由与范围。个人数据权益渠道不等于本次已取消的“健康报告导出”功能，不因此增加报告生成入口，也不承诺“注销后立即删除所有法定必须留存资料”。

### 13.3 语音与 AI

**首轮：**使用文字和明确标记的示例语音文本，无麦克风、真实音频采集或模型 API。进入聊天、长按入口、体验示例都不能被写成真实录音授权；页面不假称识别了用户现场随意说的话。示例选择仅准备输入，明确发送该示例消息或操作具体任务后，才按当前意图进入正常校验／声明流程；不能仅因展开示例列表就写入服药事实。

**未来接真实语音后：**明确录音开始和结束，不后台持续监听。优先只保存用户核对后的结构化信息，原始音频不默认长期保存；确需保留时说明用途、期限和删除方式。涉及可识别个人的录音也属于个人信息，不能因它是语音输入就排除保护。

本次没有拍照／扫码识药的采集或上传流程；历史来源文字可以保留，但不把临时图片 URL 写入持久存储。真实语音服务若上传资料，须告知并满足相应授权、委托处理和安全要求；不默认允许供应商将健康数据用于模型训练。若涉及数据出境，另行评估适用条件，不凭麦克风许可视为同意出境。

不以声音推断健康状况、年龄、身份或金融资格；不新增声纹认证。AI 输出的药品计划和测量值都能回看、更正，保留人工确认步骤。

### 13.4 运营边界与用户权益

**已移除：**充值奖励、随机现金、会员订阅、PRO 付费墙、存钱罐、提现、财神/元宝暗示收益。

**保留约束：**AI 输出不构成医疗建议；语音引导不承诺健康结果、不诱导用药行为。形象及气泡本次移除，其未来构想和约束只在独立后续清单保存。

真实发布前应按服务地区、平台及实际功能检查应用备案/上架、隐私标签或数据安全申报、AI 服务要求。本文提供设计边界与核查事项，不宣称已完成法律意见或资质审核。

## 14. 验收清单与可重复场景

以下全部是**后续开发的待验收项**，本轮只验收文档完整性。A 系列用于首轮比赛 Demo，其中语音、通知和离线同步均按正文的模拟范围执行；P 系列只用于未来真实服务，不计入首轮通过率。勾选必须有实际验证依据，不能因为文字已经写入就视为功能完成。

保留原编号便于追踪：A15 明确替换旧拍照案例；A34／A35 保留取消标记，不作为实现要求；A48–A60 保留此前业务规则；本轮新增 A61–A84。A30–A33、A45／A47 的真实服务部分移到 P01–P06，技术信息并未删除。

### 14.1 页面与用药录入

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A01 | 切换张阿姨与小李账号 | 长辈首项为服药打卡，子女为首页看板；同一长辈数据一致 |
| A02 | 查看本月含完成、未完成、未来日期的日历 | 只有符合规则的完成日期叠加黄色星星；日期清楚；未完成日期无背景、无星星 |
| A03 | 子女切换长辈并看今日用药 | 对象、任务、剂量、时段、餐时与状态一起更新；不能代打 |
| A04 | 今日血压、旧血脂、缺失身体指标混合 | 每类显示自己的实际测量时间；今日/最近一次不混淆；空值不填零 |
| A05 | 长辈首页标准/大字号 | 两张圆角卡片在问候与今日已记录之间左右分布，原文案完整、均衡可点 |
| A06 | 从长辈手机协助添加计划 | 默认对象为本人，最终核对后创建；不自动变成已服用 |
| A07 | 点账号切换左侧时钟 | 直接进入用药信息的打卡记录区域，保持对象，不出现演示时间弹窗 |
| A08 | 分别点用药信息、身体数据加号 | 直接进入对应表单，没有用药/身体二选一；数据内部类型选择仍可用 |
| A09 | 查看药名、单次量、单位 | 药名沿用；标签为“单次用量（请输入数字）”；单位支持直接输入“包”等自定义值 |
| A10 | 查看时段表四列四行 | 复选框、时段、提醒推送、餐时对齐；四个范围与 08:00/12:30/18:30/21:00 默认值正确 |
| A11 | 调整提醒的小时与分钟 | 可点对应区域上下滑动滚轮，步长 1 分钟；仅保存所属自然时段内的时间；确认回显，取消保留原值；未调整用默认时间 |
| A12 | 选择/取消餐前和餐后 | 单行互斥且可都不选；深色选中、浅色未选；标签在首页和用药信息对应药物时段后显示 |
| A13 | 手动输入／AI语音输入切换 | 恰好两种平行模式、同一草稿和校验；无照片／扫码或第三种占位模块，无付费墙，不丢已填内容 |
| A14 | 用示例语音／文字逐步创建多时段计划 | 有明确模拟按住体验和“说什么”提示；允许混合点选，收齐适用字段，次数／时段一致，最后到核对页 |
| A15 | 语音计划缺少剂量或时段（替换旧拍照案例） | 继续询问计划必填项，不凭药品常识造数据；不能混用服药打卡的免填实际细节规则；模拟标识清楚 |
| A16 | 任意模式缺必填后点核对 | 灰色星号初始可见；提交失败后主动弹出醒目错误提示层，同时显示错误星号、红框及就地错误说明；关闭提示或点“去完善”后聚焦首错，保留草稿 |
| A17 | 修正错误并最终确认 | 红框按项解除；一个核对页展示完整计划；返回可改；仅明确确认后创建 |
| A18 | 不同日程/重复点击/起止日期 | 合法任务按配置生成，不重复保存；不支持的分时剂量等情况明确处理而非错误合并 |

### 14.2 语音、记录与 BMI

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A19 | 从长辈／子女首页右下角进入助手 | 点击／长按进入同一有主动问候的聊天页，文字／模拟语音可用；加药、记录、延后、补记、身体数据、查询等按角色显示，不遮底部控件 |
| A20 | 只说“这次药吃过了”，或主动附带时间／数量 | 目标明确后保存本人声明，无实际细节也可提交；主动提供的细节可留存，不能追问成必填或自动拿计划值补齐 |
| A21 | 午餐 15:50 延后 30 分钟 | 原任务不变；16:19 仍受保护、不发 N2；16:20 无记录且保护结束后超时；只使用 1 次机会 |
| A22 | 13:00 打开早餐任务并选择三种声明之一 | 打开时仍未按时打卡；选择后才显示对应声明；详情显示补记及记录日期时间，不判断实际吃药时间或延时服用 |
| A23 | 记录一项后仍有逾期未记录 | 成功后出现“你有未打卡的记录”，按任务逐个让用户选择三种回复，不强制代选 |
| A24 | 回答“没有按时服用” | 只澄清是否吃过；前者记未服用，后者记已服用及补记，不追问时间／数量；不确定时不代选 |
| A25 | 选择本次无需服用及查看旧记录 | 当前按钮、确认、历史等全局新文案一致；当次状态改变，后续计划不被停用 |
| A26 | 示例语音／文字记录血压、血脂、身体指标 | 在支持表达中处理收缩压／高压、舒张压／低压；日期时间、数值单位及医生交代备注核对后真实本地保存；不能处理时手动继续 |
| A27 | 160 cm 与 60 kg | 身体指标自动显示建议精度下的 BMI 23.4，用户无 BMI 输入项 |
| A28 | 身高缺失/无效/与体重不同日期 | 不输出错误值；跨记录引用符合确定规则并标明来源日期；不出医学评级 |

### 14.3 通知、范围清理、样式与回归

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A29 | 桌面与手机查看演示舞台 | 桌面有默认 iOS 手机预览与外部控制，手机不二次缩小；独立应用仍可打开，不宣称真实安装包 |
| A30 | N1 默认／自定义提醒时间到点 | 用受控演示时钟和触发器显示符合条件的模拟横幅；点击按最新状态打开任务，保护期内不重复；真实系统送达另验 P02 |
| A31 | N2 到自然时段截止仍无记录且无保护 | 单浏览器在子女演示账号下呈现对应模拟通知，进入正确长辈看板／历史，每轮去重；模拟待同步只说暂未收到记录；真实双设备另验 P03 |
| A32 | N3 子女主动提醒 | 子女端经资格和共享冷却检查发起模拟提醒；显式切换到长辈接收场景可见原日期／时段、药名、计划量，点击不改事实；真实送达另验 P03 |
| A33 | 模拟通知关闭、桌面隐藏、离线、重复／失效场景 | 开关与失败状态明确标模拟；草稿、去重、过期处理、解绑权限及模拟隐私预览正确；不请求真实通知许可，不宣称测试过操作系统后台，后者另验 P04 |
| A34 | 原形象／换装验收 | **已取消**，仅保留编号追踪；移入后续构想，不作为本次验收 |
| A35 | 原气泡引导验收 | **已取消**，仅保留编号追踪；首页语音助手另按 A19 验收 |
| A36 | 全站付费门槛与取消范围检查 | 保留的手动／语音能力无会员门槛，角色权限仍有效；无资金、拍照／扫码、报告、形象／气泡当前入口或占位 |
| A37 | 长辈打开首页或点击关联通知 | 仅符合第 9.3 节条件才自动聚焦；有关闭与三种事实操作、队列进度；已记录／取消通知不重开旧动作 |
| A38 | 同一独立当次任务延后两次并刷新 | 第一次成功且保护有效；第二次禁用，提示“延后机会已用完，请按实际情况记录”；刷新不重置，不按药名混算别日任务 |
| A39 | 切换 iOS／HarmonyOS／Android | 桌面、通知与应用内部主题一起切换；长辈通知定位任务／结果，子女 N2 定位看板／历史；不绕过草稿保护和权限，不重载应用 |
| A40 | 检查全部添加入口和模式图标 | 用药、身体图标比例正确、位置正确、不被文字挤压 |
| A41 | 展开/折叠未按时卡 | 小圆红点替代警告三角；红点不随折叠箭头旋转；减少动态效果时静态可辨 |
| A42 | 窄屏／桌面 × 标准／大字号 | 卡片、日历、四列表、滚轮、核对页、聊天输入／消息卡、聚焦打卡、导航、外部控制和悬浮入口无明显溢出遮挡；键盘与读屏可用 |
| A43 | 旧存档、无效存档、空数据、恢复初始数据 | 版本迁移或合理降级；旧餐后／延时枚举保留来源而不造新事实，旧星星不沿用未服用也点星规则；取消字段不恢复，新规则及日期偏移可校验／重置 |
| A44 | 多长辈权限、子女代打／更正／延后尝试、解绑 | 不串档；语音与点击权限一致，子女不能改服药事实；编辑／停用不抹去已开始任务；解绑后停止访问与通知 |
| A45 | 首轮演示数据与输入来源检查 | 使用本地演示资料，文字／示例来源可辨，无真实麦克风、音频采集或外部 ASR／模型上传；无报告导出；真实敏感数据处理另验 P06 |
| A46 | 日历星星特殊情形组合 | 有任务且全部已服用／本次无需服用才点星；任意未服用或未记录、无任务均不点星；当天补记可恢复，次日首次补记不补星；身体数据无影响 |
| A47 | 对外展示与阶段边界 | 清楚区分可操作前端 Demo 与后续真实服务；不把模拟横幅／示例对话称为真实推送／识别，不宣称资金、会员或医疗能力；真实上线证据另验 P01–P06 |

### 14.4 典型验收数据组合

原型建议准备：同一家庭的张阿姨、小李及另一位可切换长辈；有星日（全部已服用／无需服用）、无星日（未服用或未记录）、无任务日；四时段任务；时段内延后到期、跨时段保护、已逾期首次延后及次数用完案例；一键补记且无实际细节的记录；停用后仍待处理任务；今天血压与旧血脂、有效和缺失身高体重；跨午夜补记及当日确认次日同步案例。全为演示数据，不伪造真实服药或设备送达。

新增准备：三系统本地视觉素材与统一参考；桌面 N1、子女 N2、子女发起长辈接收 N3 场景；已记录后重播旧横幅；填写一半计划、未发送聊天文字、账号隔离草稿；完整／缺项／早晚模糊／改口／否定的示例语音文本；可以实际键入的支持表达，以及明确触发表单回退的未知表达。

首轮至少走通三条互相关联的演示路径：

1. **家庭计划与通知：**子女 iOS 看板 → 手动或加药助手为当前长辈建计划并核对 → 生成适用任务 → 切长辈账号和所需系统 → 返回模拟桌面 → 触发／点击 N1 → 关闭提醒 → 对应时段列表本人打卡／撤销 → 切子女查看同一记录。
2. **提醒与历史：**受控时刻进入超时场景 → 子女 N2 看板／历史 → 满足资格时子女发起 N3 → 长辈查看通知、设置延后或补记 → 检查保护期、共享冷却、历史及严格星星条件。
3. **聊天与数据：**首页点击／长按自动问候 → 查询今天吃什么／几点／多少 → 改为加药或记录身体数据 → 示例语音＋实际文字＋点选卡补齐信息 → 确认保存 → 查询到新结果／显示 BMI → 中途换系统和回桌面仍可继续草稿。

聚焦卡直接声明的原有路径也需保留测试，不能代替通知关闭后列表打卡。首轮浏览器演练与未来真实设备验证分别记录，不互相替代。

### 14.5 最新十项规则的补充验收

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A48 | 08:10 延后至 08:15；恰好到 11:00／16:00／20:00／次日00:00 | 08:15 只再次提醒，不提前发 N2；各原时段到精确截止，无记录且无保护才超时；完整日期比较，无一分钟空档 |
| A49 | 13:00 只打开早餐逾期卡，再分别测试三种声明 | 打开不改数据；本人选择后才出现对应状态和补记时间，未服用分支真实可点，不自动选已服用 |
| A50 | 普通／逾期／历史已服用一键操作与 5 秒撤销 | 不出现实际时间／数量核对或“时间／用量不同”入口；声明与计划值分离；撤销恢复原声明，延后次数与冷却不恢复 |
| A51 | 先未服用，后来本人更正为已服用；子女尝试更正 | 同一任务保留前后版本及修改者／时间，不重复计数；当日星星重算，历史按结算规则；子女请求被拒绝 |
| A52 | 提醒前打开首页、关闭重返、两药同到点、填写中来通知 | 自动聚焦资格准确；同轮关闭不立即重开，逐药操作不批量确认，草稿保留，延后项暂出队；模拟桌面状态不由内层抢回全屏 |
| A53 | 18:00 新建早餐＋晚餐；18:40 新建18:30提醒的晚餐；编辑／停用 | 不追造早餐；晚餐保存后提醒一次且20:00才超时；未开始任务更新／取消，已开始任务及分母保留，停用后仍能补记 |
| A54 | 多名子女连续 N3；未到点／保护期／已有声明时点击 | 同任务共享5分钟冷却，重复请求只发一次；禁止条件及原因清楚；已记录未服用也不能继续催促 |
| A55 | 已逾期但未用延后，设置一次；保护到期后再进入 | 当前红点及N2暂抑制，原超时历史保留；到期重新按公式处理，每轮去重；重启／撤销不补回延后机会 |
| A56 | 23:50 延后至次日00:20；00:10首次补记 | 始终关联前一日睡前，保护按完整时刻结束，不归早餐；跨日首次补记不补前日星星 |
| A57 | 检查本次功能范围与后续清单 | 拍照／扫码、报告导出、形象／换装／气泡无当前入口或要求；首页语音助手与两种计划输入保留；后续清单不作为本次验收 |
| A58 | 模拟当日23:55离线确认、次日00:10同步；模拟离线延后 | 受控场景保留本机确认与模拟接收时间，按当日记录恢复结果，不当次日补记；明确待同步为模拟，不承诺真实服务端N2已抑制，重复应用不重记；真实同步另验 P01 |
| A59 | 全部无需服用、任意未服用、当天补记、更正／新增任务 | 有任务且全为已服用／无需服用可点星；其余不点；当天变化动态重算；历史后续更正显示痕迹而不伪装当日原结果 |
| A60 | 建计划缺必填；一键打卡无实际细节；语音主动说出细节 | 建计划仍阻止缺项且统一核对；打卡不强制采集实际细节，主动细节可保存，不自动填计划量；一药三时段为一条计划，完整适用日有三次任务，新建当天不追造已过时段 |

<a id="demo-acceptance"></a>

### 14.6 本轮三系统演示与两个助手的补充验收

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A61 | 分别打开 `demo.html` 与原 `index.html` | 同源静态服务下舞台嵌入现有应用；独立入口仍可用；没有复制另一套业务或重建家庭数据 |
| A62 | 初次打开与连续切三系统 | 内外层显式同步系统标记，应用就绪／主题就绪再显示，桌面与应用无旧主题闪现；不假设父 CSS 能影响 iframe |
| A63 | 完整体验 iOS 桌面到应用 | 默认 iOS；壁纸、系统图标组、Dock、状态栏、毛玻璃横幅及应用控件协调；图标展开／返回、面板与按压反馈可操作，药物文字可读 |
| A64 | 完整体验鸿蒙与安卓 | 各有对应参考的壁纸、图标／排布、状态栏、横幅和应用控件；不是只换颜色或横幅；同一业务流程可用 |
| A65 | iOS 打卡后换系统；填写一半时返回桌面再打开 | iframe 不重新加载；已保存记录、未发送文字、消息和配置草稿均保持，无重复创建／欢迎消息 |
| A66 | 子女 iOS、长辈鸿蒙及反向组合 | 系统与账号独立；切主题不换身份；账号／长辈由合法切换入口控制，显示对象清楚，数据和草稿不串档 |
| A67 | 桌面横幅→提醒→关闭→列表状态打卡 | 每一步亲手可点，关联正确日期／时段／药物；关闭不写记录、不消耗延后、不立即重弹，列表仍可选三种声明；保存结果更新双端 |
| A68 | 控制区准备并触发 N1／N2／N3 | 不等真实时间；读取有效任务与受控时刻；N3 从子女授权动作产生且受冷却约束；错误条件有原因；重播只重复外观，不重发逻辑通知 |
| A69 | 草稿中点击通知、旧通知、已记录／取消／解绑后再点 | 输入保留，处理后可恢复；最新状态与权限优先，结果／失效／拒绝显示正确，不恢复旧打卡动作；N2 不进入代打页面 |
| A70 | 应用未就绪、主题消息、重复或来源错误消息 | 就绪前指令不丢失；非法 origin／source／类型／参数被拒；重复消息不重复保存；外层不能任意改本地业务存储 |
| A71 | 两角色分别点击和长按首页按钮 | 均进入同一聊天子页并主动问候；点击文字、长按语音模式；松手不重复导航，无空白等待页，无虚假录音状态 |
| A72 | 有当前任务、无任务、不同授权对象进入聊天 | 问候及建议来自真实演示上下文，无任务不造任务；意图建议不会自动声明服药，角色动作正确 |
| A73 | 实际键入支持表达，再改内容或提出未知表达 | 对话、摘要和结果随文字变化，能往返处理；不支持时澄清／转表单并保留输入，不用固定答案冒充理解 |
| A74 | 查询今天／晚间吃什么、几点、多少；新建后再查 | 回答已有计划的药名、计划量、时段、提醒和可选餐时；多目标先澄清，空数据如实回答，刚保存结果可查，不产生医疗建议或服药声明 |
| A75 | 添加药品→AI语音输入 | 立即显示创建计划引导和记录对象，不重复问用户意图；首条提示药名、用量单位并允许一次提供更多信息 |
| A76 | 先只给药名／用量，再逐步补充 | 每轮确认已知、只追问缺项或冲突；不丢已填信息，不把餐时／备注当必填，不用药物常识造数值 |
| A77 | “早晚吃”后点开设置卡，混合文字、示例及手动模式 | 四时段范围、提醒滚轮、独立餐时、日期周期可点可改；一份草稿双向同步；范围和必填规则一致，最终统一核对 |
| A78 | “嗯／那个”“还没吃”“不是一片，是半片”“饭前改饭后” | 保留原文，忽略无关词不丢否定、0.5片或改口方向；无法可靠处理时让用户确认，计划用量不误写为实际服药事实 |
| A79 | 选择完整／部分／改口示例并体验模拟语音 | 选择只预览，明确发送后进入对话；状态和文本标示例／模拟，无真实麦克风权限或上传；取消不保存，未选择示例不伪造转写 |
| A80 | 两入口创建计划／测量，长辈声明，刷新与切账号 | 草稿未经确认不落为业务事实；确认后统一保存，首页、历史、子女看板、星星／BMI按适用规则更新，刷新可恢复，重复点击不重复写入 |
| A81 | 子女文字／示例／任务卡请求打卡、更正或延后 | 与手动权限一致地拒绝；查询、协助添加与符合条件的提醒可用，不因“AI助手”或系统主题绕过授权 |
| A82 | 聊天中转表单、切意图、换账号／档案再返回 | 消息、未发送文字、已知字段与手动修改可恢复；草稿按身份／对象隔离，不泄露无权消息，不静默换目标；再次保存前鉴权 |
| A83 | 正常／慢放、快速关闭／反向操作、减少动态效果 | 进入退出连续、图标原点正确、可中断不锁操作；无抖动、旧主题闪烁或重复记录；大字号与减少透明／动态效果下清楚可用 |
| A84 | 关闭外部网络，仅保留本地静态服务完成比赛流程 | 本地素材、文字／示例语音、三类模拟通知、查询和保存可用；无必须配置的 ASR／模型／推送密钥，不把后台服务或真实安装包列为首轮门槛 |

### 14.7 未来真实 APP／语音服务的独立验收

| 编号 | 后续验收对象 | 上线前要求 |
| --- | --- | --- |
| P01 | 真实客户端、身份与家庭同步 | 确定首发平台／系统版本，安装包可用；真实授权、撤销、跨设备任务与事件同步、离线及跨日冲突处理正确，不以本地角色开关替代服务端鉴权 |
| P02 | 真实 N1 | 在支持版本与合法权限下按默认／自定义时刻触发本机通知；计划修改／停用／记录后取消或更新，保护期与去重正确 |
| P03 | 真实 N2／N3 | 两个独立账号、不同真实设备端到端验证，接收对象和内容正确；N2轮次／N3家属共享冷却生效，点击读取最新权限和状态；不把已发送当已送达 |
| P04 | 真实通知可靠性与隐私 | 逐平台验证前后台、锁屏、断网恢复、关通知、重启、终止进程、系统限制、重复投递、token变化、计划更新、解绑和切设备；明确回执能力、延迟目标、失败容忍／重试／过期状态与锁屏预览 |
| P05 | 真实 ASR／语义服务 | 真正录音和转写，支持明确声明的语言范围；权限拒绝、网络或识别失败保留草稿；原文、解析和改口可核对，含否定与剂量信息不失真；服务不可用仍能手动操作 |
| P06 | 真实健康／音频及第三方处理 | 必要授权、最小化、保存期限、权益渠道与委托处理落实；密钥通过受保护服务中转，无未声明上传／训练／出境；不因数据权益恢复报告导出，不宣称未经验证的医疗能力 |

## 15. 实施顺序与待确认事项

### 15.1 先完成比赛 Demo，再进入真实服务阶段

| 阶段 | 主要交付 | 完成门槛 |
| --- | --- | --- |
| 文档阶段（本轮） | 仅写回本 v2；保留九组原始需求、十项业务规则及现有后续清单，新增 C01–C12 的正文与验收 | 范围、权限、数据、待定项无冲突，原文不丢失，不声称已实现 |
| 首轮 1：业务与共享状态 | 在现有前端落实首页／路由、四时段计划、声明／补记／更正、延后／星星／BMI和数据迁移 | 同一保存入口，权限与历史正确，能独立打开应用 |
| 首轮 2：iOS 演示舞台与通知 | `demo.html`、同源 iframe 联动、主题握手、桌面／通知／应用转场、三类模拟通知与关闭后列表打卡 | iOS 内外层风格连续，流程可亲手完成，保存结果真实，回桌面不重载 |
| 首轮 3：两个助手 | 通用助手与加药助手、主动问候、计划查询、文字／示例语音、已知摘要／缺项追问、点选设置卡及完整核对 | 真实输入可改变结果；角色与草稿隔离；有限理解有回退，模拟状态清楚 |
| 首轮 4：鸿蒙／安卓与全流程打磨 | 另外两套桌面和应用主题、控制区切换、动效审查、双字号／多视口、异常与重复场景 | 三套风格可辨，切系统不丢输入与记录；有效 A 项按证据验收，无取消模块或外部服务依赖 |
| 后续：真实 APP 与家庭数据 | 另选原生／跨平台客户端，接真实身份、授权、设备绑定和跨设备同步 | P01／P06；后续选型不阻碍比赛交付 |
| 后续：真实语音与理解 | 先接 ASR 复用文字处理流程，需要复杂自由对话时再接模型／解析服务 | P05／P06；保留原聊天、草稿、核对及手动回退，不假称ASR等于业务理解 |
| 后续：三类真实通知 | 接入 N1／N2／N3 对应系统能力和后端 | P02–P04／P06；逐平台实机验证，不与首轮模拟通过率混算 |

同一阶段的视觉和业务可按依赖并行，但比赛无需先选真实 APP 框架、购买语音服务或接推送。先做好 iOS 主演示体验，再完成鸿蒙与安卓风格及共同回归，不能以其他系统为由拖延核心闭环。第 15.2 节未决项保留真实状态：影响首轮的呈现细节由实施时形成明确方案，影响真实服务的事项在后续启用前解决，不把尚未选择的供应商、系统版本或数据期限写成已确认。

### 15.2 决策状态与剩余待确认事项

| 编号 | 问题 | 状态 | 已确认口径 / 仍待定 | 影响 |
| --- | --- | --- | --- | --- |
| D01 | 星星资格 | **最新已确认** | 有任务且全部为已服用／本次无需服用才点星；未服用虽为记录但不合格；未记录、无任务不点星；身体数据不影响，见7.4 | 月历、统计 |
| D02 | 端点、凌晨、逾期与时区 | **已确认** | 四时段左闭右开，北京时间；按7.3统一公式，截止用>=；凌晨不开新时段，旧任务不归早餐，见7.7 | 状态、N2、跨日 |
| D03 | 提醒范围及滚轮步长 | **已确认** | 普通提醒限定在所属时段，分钟步长1；延后可跨时段 | 表单、调度 |
| D04 | 简化服药记录与实际细节 | **最新已确认** | 一键本人声明，不必问实际时间／数量、不增相应入口或部分服用状态；主动信息可留存，不推断延时服用，见6.3、7.6 | 语音、打卡、补记 |
| D05 | 无计划药物的临时实际服药记录 | 待确认 | 人工核对或建立适用计划后关联；是否新增临时记录模型待定 | 全局助手的“记录新药”分支 |
| D06 | 延后资格、次数及保护 | **已确认** | 第7.3节：每独立当次任务一次5／30分钟，已逾期未用者可用；保护结束仍须同时到原截止且无记录才超时；次数不因刷新／撤销恢复 | 通知、红点、聚焦卡 |
| D07 | 历史补记／更正与计划生效 | **操作规则已确认；服务留存待定** | 所有仍保留且本人可访问的历史任务可简单补记／更正；5秒撤销保留；计划按7.5向后生效，停用不阻止既有任务记录；7.4／7.7定义跨日星星。真实数据留存期限未选定，不新增任意天数门槛 | 历史、权限、日历 |
| D08 | BMI 能否引用旧身高及多久有效 | 待确认 | 建议使用体重测量时点之前最近有效身高并标时间 | BMI 准确呈现 |
| D09 | 真实 APP 首发平台、最低系统版本、技术栈 | **后续阶段待确认** | 首轮纯前端三主题已确定，iOS是视觉主版本；不因此锁定未来原生／跨平台框架或首发安装包平台 | 后续 SDK、工期和实机矩阵，不阻碍首轮 |
| D10 | 通知频控、接收人与实机指标 | **首轮规则确认；真实配置待定** | N3家属共享每任务5分钟冷却、资格限制、N2轮次去重继续；首轮使用已授权演示账号模拟接收。真实多子女接收配置、延迟、重试、回执及测量样本后续确定 | 首轮模拟正确性、后续真实通知验收 |
| D11 | 资金归属、随机奖励、充值/提现 | **已取消** | 完全取消存钱罐、随机金额、充值与提现，不再实施 | 无 |
| D12 | PRO 定价、套餐、家庭共享 | **已取消** | 取消 PRO 与会员付费墙；本次保留能力全员开放，仍受角色授权约束 | 无 |
| D13 | 子女新增前是否每次强制选对象 | 待确认 | 建议带入当前对象并显眼核对；v1 强制选择器可保留 | 跨档案误录风险与步骤数 |
| D14 | 看板模块顺序、历史月份、子女直接加药动作 | **部分待定** | 本月必做；右下角两端助手已确认。模块顺序、历史月切换仍待定；子女直接加药可先放今日用药模块内，不与助手堆叠 | 首页密度与导航，不改变助手入口 |
| D15 | 陪伴形象、换装、气泡 | **本次移除，转后续清单** | 仅独立保留构想，不再评审本次落位／素材，不要求当前字段或验收；首页语音助手保留 | 本次无实现依赖 |
| D16 | 真实语音供应商、方言范围、资料保存与云处理 | **后续阶段待确认** | 首轮示例语音＋文字／点选、有限规则和手动回退已确认，不配API；真实 ASR／模型与音频规则后续选型 | 后续识别质量、费用、隐私与可用性 |
| D17 | 三主题具体视觉参考版本与素材 | 待实施选择 | iOS默认且重点打磨，鸿蒙／安卓风格可辨已确认；实施时各选一套一致参考、记录素材来源，避免跨代混搭；技能见11.6 | 首轮视觉一致性，不新增原生平台承诺 |
| D18 | 独立演示入口、三类通知交付层级 | **已确认** | 同源 `demo.html` 嵌入原 `index.html`、常驻实例、主题同步、外部控制与完整通知流程；三类通知首轮模拟，真实服务后置 | 架构、演示、A61–A70 |
| D19 | 消息协议名、视图及草稿具体存储结构 | 实现细节待落定 | 第9.2／12.4给出接口建议、单一业务来源及恢复边界；名称可在实现中确定，不能破坏就绪、权限、草稿和幂等要求 | 前端对接与数据迁移 |
| D20 | 两个助手的首轮范围 | **已确认** | 两端首页点击／长按主动问候、已有计划查询；加药直接收集已知／缺项；共用草稿与点选卡，首轮模拟语音、文字真实可用，最终保存 | A71–A82，后续接语音可复用 |

### 15.3 本轮文档交付检查

文档双向检查：原始九组需求能找到保留、替换或取消的去向；此前十项业务确认分别落到正文、数据／权限和验收；本轮 C01–C12 按第 0.5 节对应到正文与 A61–A84 等验收。不能从原始附录或已归档 v2.1 恢复旧规则，也不能把未来 P 项重新写成比赛必须接通的服务。

| 保留的十项业务确认 | 主体规则 | 对应验收 |
| --- | --- | --- |
| 1 自然时段与延后到期判定 | 5.3／5.4、7.3 | A10／A11、A21、A48 |
| 2 补记前后状态及记录时间 | 6.5、7.2／7.3 | A22、A49 |
| 3 聚焦卡出现、退出、排队 | 9.3／9.4 | A37、A39、A52 |
| 4 简单声明、撤销与本人更正 | 6.3／6.6、7.6、12.2 | A20、A24、A44、A50／A51 |
| 5 新建／编辑／停用向后生效 | 5.7、7.5 | A18、A53 |
| 6 严格星星资格、当天补记 | 2.2、7.4 | A02、A46、A59 |
| 7 仅手动／语音建计划，移除拍照和报告 | 5.1、5.7、10 | A13／A15、A36、A57、A60 |
| 8 每任务一次延后、家属提醒频控 | 6.4、7.3、9.5／9.7 | A31／A32、A38、A54／A55 |
| 9 形象／换装／气泡转后续清单 | 10.1、独立文档 | A34／A35取消，范围检查A57 |
| 10 跨午夜与离线同步 | 7.7、9.7、12 | A56／A58 |

本轮交付前另外核对以下内容：

- 以修订前工作区文件为基准保留用户已有修改；原始附录 A 原样保留，十项业务约束和取消范围没有丢失。
- 所有“真实系统通知／语音识别／两台实机／云服务”要求都有后续阶段标识，首轮正文与 A 项不依赖它们。
- 三系统主题覆盖桌面、图标、通知及应用内部；明确 iframe 内外 CSS 分离、同一主题握手、常驻实例和单一保存路径。
- 通知完整链中保留“关闭后在列表打卡”，关闭不会代记；N1／N2／N3 资格、接收方、延后与冷却未被演示控制绕开。
- 两个助手的入口、主动消息、文字／长按、已知／缺项、卡片点选、查询、改口、原文保留、模拟方式与确认保存均可找到正文及验收。
- 没有将两端助手开放解释成家属可代打，没有把“取消形象气泡”误解成删除聊天消息，也没有新增会员、拍照、报告或资金模块。
- 检查标题层级、链接、表格、编号和交叉引用；由独立读者从文档回答实现问题并修正歧义。

本轮仅修改本 v2 文档，未修改交互、数据或样式代码；不执行浏览器功能测试，也不得将有效 A 项或 P 项标成已通过。文档核对通过只表示需求可用于实现。后续前端开发仍按项目要求执行浏览器渲染、控制台、双端切换、记录／提醒、刷新持久化、权限及多视口／双字号检查，真实服务接入后另做 P 项验证。

## 16. 官方资料与证据范围

以下资料沿用并保留，用于约束未来真实 APP 的技术与隐私表述，不构成首轮需要接入这些服务的证据，不替代发布时对目标版本、发行地区和具体业务的复核。本轮没有重新联网核验这些官方页面，也没有引用旧 PRD 中未经验证的调研比例或医疗效果承诺。用户提供的前端技能另列第 11.6 节，属于设计／实现参考，不等同于官方系统规范或兼容性证明。

| 编号 | 来源 | 本文使用范围 |
| --- | --- | --- |
| S1 | [Apple User Notifications](https://developer.apple.com/documentation/usernotifications)；[Local and Remote Notifications Overview](https://developer.apple.com/library/archive/documentation/NetworkingInternet/Conceptual/RemoteNotificationsPG/) | 本地与远程通知的职责。第二项为归档概念资料；实现 API 以目标 SDK 为准 |
| S2 | [Apple：Creating the Remote Notification Payload](https://developer.apple.com/library/archive/documentation/NetworkingInternet/Conceptual/RemoteNotificationsPG/CreatingtheNotificationPayload.html) | 远程通知不保证送达、避免载荷中放敏感数据。归档资料不用于断言当前载荷大小或接口限制 |
| S3 | [Android：Notification runtime permission](https://developer.android.com/develop/ui/views/notifications/notification-permission) | Android 13+ 的通知运行时权限 |
| S4 | [Android：Schedule alarms](https://developer.android.com/develop/background-work/services/alarms/schedule) | 定时精度、精确闹钟适用权限及系统限制 |
| S5 | [HarmonyOS：Notification Kit](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/notification-overview)；[Push Kit](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/push-kit-introduction)；[代理提醒](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/agent-powered-reminder) | 定位通知、推送和后台提醒的官方能力入口；动态正文未完整提取，具体 API、支持版本和配额待实施时核实 |
| S6 | [Apple App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/) | 上架、隐私与业务说明要求。本版本已取消内购、订阅与现金奖励，不因本文引用而宣称已获审核 |
| S7 | [国家网信办：中华人民共和国个人信息保护法](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm) | 第28–30条敏感个人信息处理，第47条删除，第51条保护措施，第55–56条个人信息保护影响评估 |

**附录阅读提示：**以下附录 A 的文字整体保留用于溯源。其中“本轮一定实现真实手机通知”等早期表达，已经由第 0.5 节及第 9 章的首轮模拟、未来真实服务分阶段方案替换；相反旧要求不再构成首轮验收依据。其余取舍与业务规则仍以正文为准。

## 附录 A. 用户九项需求原文

以下保留用户表达用于需求溯源。正文中的术语规范、合规修正和待确认标记不改写原文，也不能把原文中的设想当成已经完成的功能。

**最新取舍说明：**以下原话原样保留。当前添加计划仅手动／AI语音输入，不做拍照／扫码或健康报告导出；服药打卡不再必问实际时间／数量；延后保护、星星资格和跨日按第7章最新规则；形象／换装／气泡转后续清单，存钱罐／奖励／PRO继续取消。原话中相反的旧设想不作为本次实现或验收要求。

### 原始第 1 项

新增子女首页看板，也就是说在子女端“服药打卡”这个页面改为首页看板。需要增加一个日历。就是本月的日历，每一天打卡如果全部完成了，当天的日期。上面就叠加一层黄色的星星图标来标志这一天已经完成全部打卡。未完全完成打卡的日子则没有背景，只是显示日期的数字。看板还需要一个模块能够直接看到老人今天的用药情况，还需要一个模块展示今天的身体数据或最新一次的身体数据。这一块的呈现我还没有想好，你可以先按照你的理解做一版出来让我看看。

### 原始第 2 项

老人端的首页依旧保持名称为服药打卡，但是这个页面稍微有一些复杂了，右下角的加号，我希望它移动到整个屏幕的中上方，就在今日已记录和问候语之间左右分布两个圆角方框，左边方框里呈现“点击添加用药打卡”，右边方框里呈现“点击添加身体数据”。记得要保持整个页面的视觉效果是均衡美观的，这两个按钮不需要太大，但也要方便点击。一般来说，这是为了让老人能够更加快捷地添加自己要打卡的药物。如果是子女帮忙添加，也可以用老人的手机从此页面进行添加。这个页面还有一个功能，我觉得需要小小的优化，就是右上角切换账号的那个图标左边的时钟，我不知道它的功能到底是什么，但是我希望它是一个查看历史打卡信息的入口，我希望它通向用药信息页面的那个打卡记录。

### 原始第 3 项

用药信息页面右下角的加号点开以后，不要进行二选一了，直接就是添加药品的页面，身体数据这个页面也是一样，点开加号以后，直接就是添加身体数据的页面。

### 原始第 4 项

从首页的“服药打卡”或“用药信息”页面，点击“+”号，进入的“添加药品打卡计划”页面现在需要增加一些新的功能，完善一些旧的功能。首先，药品名称不用变。我希望你在“单次用量”这四个字后面增加一对括号（），内容就写请输入数字，意思是每顿吃几粒或几包。然后用量单位这里，我希望可以增加直接输入的功能，不要只是下拉。服用时段这块问题最大，我需要它呈现的是一个列表的形式：

第一列是复选框。第二列是时段。第三列是提醒推送。第四列是餐前/后。我希望有四个时段，分别是：

1. 早餐：5 点到 11 点
2. 午餐：11 点到 16 点
3. 晚餐 16 点到 20 点
4. 睡前 20 点到 24 点

以此来辅助老人或者是子女选择其计划用药的时段。第三列的提醒推送每一个时段都会有一个默认时间：

1. 早餐时段：默认 8:00
2. 午餐时段：默认 12:30
3. 晚餐时段：默认 18:30
4. 睡前时段：默认 21:00

每一行的提醒推送时间都显示为可以调整的形式。也就是说，在设定的时候，你可以选择不去调整，按默认的来，也可以通过点开hour或minute部分，上下滑动屏幕来选择你想要设定的时间。我希望能够有一个推送功能：只要用户开启了消息通知，就可以按照用户对此应用消息通知的设定，在其设定的时间给老人端手机推送消息提醒其用药。第 4 列的内容是一个按钮，你可以选也可以不选，只要勾选了就会以一个带颜色的圆角标签显示在“服药打卡”页面和“用药信息”页面每个药品的后面。这个按钮分成左右两部分，左边叫餐前，右边叫餐后。所以这一栏的名称，我觉得你可以自己想一个更加简洁合适的名字，不一定就叫餐前或餐后，这也太长了。我希望这个按钮也是有弧度的轮廓，但是不要太过圆润，它的设计应该是简洁的。你选了“餐前”，这一半儿就会显示为深色（主色调），另一半则保持浅色。另一半也是同理。以上是对已有功能的优化，然后我需要在这个页面多一个平行于手动输入、拍照识别的模块，它名字叫“AI语音创建”。这个模块我打算把它设置为一个 PRO 的功能，所以我希望它显示为一个不同的颜色，或者有不同的标志，或者右上角有一个 PRO 的标志。总之不要太夸张，但是也能显得它是一个 PRO 的功能。不管从语音创建还是拍照识别，都要填写手动输入栏目下的所有内容。这个语音创建的模块一点开，我希望它有一个很明显的按钮让我可以按住就说话。这个功能要是想好用，我希望它是有提示的。我希望它按照手动输入模块里必须要输入的内容，给用户以明确的“你要说什么”的提示。或者我希望他能够在用户进入这个页面的时候，直接让 AI 引导用户比如说先引导他说出药品的名称然后他用语音回答，再引导他说出每次要吃多少粒或者多少包，一天要吃几次，然后你再引导他选择服用的时段，采用交互的方式完成所有信息的填写。最后，不管哪个模块，你都需要在一个页面让用户核对清楚用药的计划，最后再完成添加。我希望这个核对用药在点击之后，如果用户有没有填写的必填项，你要弹出非常明确醒目的提示，要求完成标红的框。所以我希望还有一个功能：把所有的必填项都加上星号。星号原来是现在的这个灰色的原色，但是当用户没有完全填完，就提交“核对用药计划”时，要把它显示为红色的框。我希望这个红色不要太丑，它和我们这个界面的配色要视觉和谐。

### 原始第 5 项

我刚才忘说了，在老人端的服药打卡页面（也就是首页），右下角还要有语音助手功能。这个语音助手功能跟添加用药信息里面的AI语音创建比较类似，但是它算是一个全局的语音引导入口。如果是从这个页面点进去，我希望 AI 直接引导用户选择当前的点击意图，比如说，很多老人或者是子女想通过直接点击语音助手添加用药，那么就和刚才跟你说的是一样的。如果他的点击意图是想记录，那么你就需要引导他说出今天什么时间，吃了什么药，吃了几片或几包。如果他点开的意图是想让你帮忙“过一会儿再提醒他”，因为他这会儿还吃不了药，那么你就需要帮他重新设定一个提醒时间，此时如果已经过了吃这个药的时段，依旧显示为未按时打卡。如果他是想进来打卡，但是已经过了时间了，那么你也可以帮助他在“未按时打卡”卡片里帮他补打卡。如果他是已经忘记本次服用了，点进来之后如果他开始记录新的药物打卡，你需要在完成该记录之后提醒他：“你有未打卡的记录”，并让他自行回复：

1. 已经服用
2. 没有按时服用
3. 本次无需服用。如果是没有按时服用，那你就要帮他在未按时打卡的提醒里点击未服用（总之记录该药物当天、当次属于未服用状态）。同理如果是本次无需服用，就记录为跳过本次。哦，对了，我希望把全局的“跳过本次”，都改为“本次无需服用”。因为跳过本次的意思不是很明确。如果他点击语音助手的意图是记录身体数据，那你要引导他说出自己的收缩压或者说高压，舒张压或者说低压，什么日子，什么时间测的，大夫有没有说什么？这部分可以记录到备注里。你也可以引导他说出自己的血脂信息和血压是同理的。身体数据也是一样的。

### 原始第 6 项

身体数据这块，我希望你能根据老人的身高体重显示一个 BMI 值。但是无需让老人自己填，只是自动计算并显示在身体数据的身体指标板块。

### 原始第 7 项

我想把它做成一个 APP，暂时给它长成苹果手机的样子，我做成 APP 的原因是，APP 的提醒功能更加直截了当，它就是联动手机的通知，但是我暂时不知道怎么去做，不同的手机，iOS 系统、鸿蒙系统、安卓系统是不是不一样的？总之这个功能一定要实现。提醒功能有三方面：

1. 到点提醒老人端的手机；
2. 超时未打卡提醒子女端的手机。
3. 子女因为发现老人未打卡而发起的远程提醒，由子女点击发起，老人端显示什么时段的什么药物没有按时服用规定的剂量（直接显示为几片几包）

### 原始第 8 项

还有一个功能，就是我希望能够让老人喜欢上这个 APP 老人现在都喜欢小孩或者猫猫狗狗，我希望有一个卡通的形象，它可以是：

1. 一个抱着元宝的小孩
2. 一只可爱的小狗
3. 一只可爱的小猫
4. 也可以是财神爷形象

总之就是他要有鼻子有脸，然后看着很喜庆。这个形象，我希望先是静态的，到后面可以给它做成动态的，这个形象我暂时没想好把它放在哪里，希望不要让页面变得非常拥挤。

这个形象主要是出现在老人端，老人点开它就可以看到当前因为打卡已经攒了多少钱。所以，这个形象其实是一个存钱罐，老人每打完一整天的卡，就能随机获得一些钱（0.1～1元），攒够10元可提现，每月底不清零。然后这个钱主要是从子女端的充值得来的，盈利模式我暂时没想好，但是我觉得按月/按季/按年的形式更好。pro功能主要是AI语音助手功能。开会员这个可以放在“我的”页面做，老人和子女端相同。可以先预留出来空间放这个部分。

### 原始第 9 项

UI方面的呈现需要优化。现在点开任何一个加号，你会发现用药信息、身体数据的图标都会有畸变，或者说它不显示为正常的形状，不在正确的位置。未按时打卡提醒不需要⚠️这样的表示，只需要显示为一个有点像红绿灯的红灯一样的红点，它可以闪烁，是圆形的一个小点，这样可以避免你打开折叠卡片之后，这个注意的图标就翻转了。

### 原始交付要求

以上是我能想到的第二版MVP的所有改动、优化、新增项。帮我把这些所有需求不丢失任何信息的、有条有理的、详细的、合规的记录到第二版MVP文档里
