# 药安心 MVP v2 演示说明

推荐从 `demo.html` 进入比赛舞台。它包含 iOS 18、HarmonyOS 5、Android 15 / Material 3 三种网页风格；`index.html` 是同一应用的独立入口。

## 打开

在项目根目录运行：

```bash
python3 -m http.server 8000
```

访问 `http://localhost:8000/demo.html`。端口被占用时换一个端口。舞台需要同源HTTP服务；独立应用也可直接打开 `index.html`。

## 三条演示路线

1. **家庭计划与通知**：控制区选小李，在早餐前添加计划并最终核对；切张阿姨，回桌面，将演示时钟调到提醒时间。点击N1横幅，关闭提醒进入对应列表，再打卡或撤销。切回小李查看同一记录。
2. **提醒与历史**：将时钟调到原时段截止，让小李看到N2；在子女端发“提醒TA（模拟）”，再切长辈打开N3。体验一次5/30分钟延后、本人补记和历史详情。已记录、未到提醒点、延后保护中及共享冷却内均不能继续催促。
3. **助手与数据**：首页右下角点击进入文字聊天，长按进入模拟语音；先选择示例，预览可改，明确发送后才处理。逐步补充计划或测量、转手动核对，保存后查询结果或查看BMI。中途切系统或返回桌面，消息和草稿仍保留。

可以使用控制区的预设时刻、通知场景、重播、隐私预览、通知开关和模拟离线/失败/同步设置。N3必须由子女授权动作产生；重播只展示既有通知。主题与账号独立，切主题不会重载应用。

需要从头演示时使用“恢复初始数据”，应用中再次确认后清除新增记录、草稿、延后/冷却及日期偏移，回到iOS默认风格。整页刷新保留已保存记录，但结束未提交草稿会话。

## 版本与验证

本地Git原五个步骤标记均保留。最终完整版本使用 `mvp-v2-step-05-fix-01`，它补齐原第5步提交漏收的一组解析测试源码。用 `git log -1 mvp-v2-step-05-fix-01` 查看最终代码；Git不备份浏览器localStorage中的打卡数据。

[实施总安排](实施步骤/00_总安排.md) · [82项验收台账](output/验收/验收台账.md) · [验证总览与限制](output/验收/验证总览.md)

当前是本地演示，所有通知、语音示例、离线与同步均为模拟；不连接真实麦克风、模型、推送、医疗或设备服务。仅供展示，不提供医疗建议，不用于真实用药决策。
