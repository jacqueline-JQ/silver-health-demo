# AGENTS.md

## 项目概览

本项目是“药安心”复客松展示版，一个面向老年人及其家属的本地静态 HTML 交互原型。它用于展示“子女协助建计划 → 长辈完成服药打卡 → 双端查看同一份记录 → 子女模拟提醒”的家庭协同闭环。

当前实现不是生产级医疗应用，也不连接真实医疗、登录、推送或硬件服务。修改代码时，必须保持这一定位，不得把演示能力描述成真实医疗能力。

产品名称统一为“药安心”，适用于页面、通知、文档标题及新增素材。已有内部存档键 `silver-health-data-v1`、`silver-health-view-v1` 和默认数据标识 `SILVER_SEED_DATA` 保留用于兼容，不因产品改名而更换。

## MVP v2 分步执行

- v2 开发的功能范围以[已确认的 MVP v2](药安心_MVP文档_v2.md)为准；详细步骤、完成条件和回退方式统一维护在[实施总安排](实施步骤/00_总安排.md)及其五份步骤说明中。
- 用户已确认自动连续执行：开发启动后，每步依次完成开发、检查本步及受影响的既有功能、更新记录、本地 Git 提交和版本标记；报告进度后自动进入下一步，不等待逐步批准。
- 必要检查不通过或尚未完成时，留在当前步骤处理，不标记完成、不进入下一步。普通实现细节自行决定并记录；实质性需求冲突、必须改变已确认范围或合理排查后仍无法解决的阻碍，按总安排暂停说明。
- 用户新的纠正、暂停或回退指示优先。会话恢复时先核对步骤记录与实际 Git 状态，再继续未完成工作。
- 执行方式已确认不表示开发已经开始；当前进度以步骤文档和实际提交为准。本节也不将仅更新文档的请求扩大为启动开发。

## 技术栈与运行方式

- 原生 HTML、CSS、JavaScript，无构建工具、无 npm 依赖、无前端框架。
- `index.html` 按顺序加载默认数据、纯规则、有限解析、消息桥和 `app.js`，共用 `styles.css`。
- 可以直接用浏览器打开 `index.html`。
- 需要稳定的本地预览或浏览器自动化时，在项目根目录执行：

  ```bash
  python3 -m http.server 8000
  ```

  比赛舞台访问 `http://localhost:8000/demo.html`，独立入口访问 `http://localhost:8000/index.html`。如果端口被占用，改用其他端口。具体操作见[演示说明](演示说明.md)。
- 无 `package.json` 和构建脚本；`tests/` 包含 Node 规则检查与使用已安装 Playwright/Chrome 的浏览器检查，不是应用运行依赖。不要引入打包器或框架。

## 文件职责

| 文件 | 职责 |
| --- | --- |
| `index.html` | 页面骨架、内置 SVG 图标精灵、脚本加载顺序和无 JS 提示 |
| `app.js` | 应用状态、页面渲染、表单、弹窗、导航、权限判断、本地持久化和演示交互 |
| `rules.js` | v2 纯业务规则：自然时段、任务快照、声明/更正、延后、历史星星、迁移与校验；不自行读写存储 |
| `demo.html`、`demo.css`、`demo.js` | 独立系统演示舞台；iOS 18 / HarmonyOS 5 / Android 15风格、受控设置与唯一应用 iframe，不操作业务存档 |
| `demo-bridge.js` | 原应用侧消息协议、类型校验、来源校验及请求幂等 |
| `conversation.js` | 两助手共用的有限意图与字段解析；仅返回候选/冲突，不访问存储或执行业务动作 |
| `seed-data.js` | 初始家庭、账号、长辈档案、药物计划、打卡事件和健康记录；只保存默认数据 |
| `styles.css` | 全部视觉样式、响应式布局、适老字号、状态样式和动画 |
| `药安心_MVP文档_v2.md` | 本次开发的已确认产品边界、数据关系、用户场景和验收逻辑；v1仅历史参考 |
| `药安心_PRD.md` | 产品方向和远期需求参考；其中未在 MVP 中实现的功能不能视为现有能力 |
| `output/playwright/` | 已生成的桌面端和移动端预览截图；属于验证产物，不是运行时输入 |
| `output/验收/` | 82项有效验收台账、具体检查名称/命令与验证范围 |

## 当前产品边界

### 已实现或可演示

- 张阿姨长辈端与小李子女端账号切换。
- 一个家庭组下多个长辈档案的切换与访问限制。
- 早餐／午餐／晚餐／睡前四自然时段；已服用／未服用／本次无需服用、一次延后、本人历史补记与更正、5 秒撤销及模拟提醒资格。
- 新计划仅生成适用的当前及未来任务；编辑／停用保留已开始任务快照，历史星星按当日截止前版本计算。
- 子女为指定长辈添加用药计划，并在确认后生成演示日任务。
- 血压、血脂、身体指标的手动记录和本地历史展示。
- 血压最近 7 次趋势图。
- 子女首页看板、本月星星日历、分类型日期摘要；长辈双快捷卡片，时钟直达打卡历史。
- 四列计划设置、小时分钟滚轮、餐时标签、共用核对、醒目的必填错误提示；支持半片、自定义单位与家属协助来源。
- BMI 自动计算，引用测量时间以前最近有效身高并标注日期；不作医学分级。
- 模拟设备导入、设置中演示时间调整和恢复初始数据。拍照／扫码入口已移除。
- 标准字号与适老大字号模式。
- iOS 18、HarmonyOS 5、Android 15 / Material 3三套本地网页风格；各自的壁纸、桌面排布、图标、状态栏、通知和应用控件；图标展开和返回、聚焦卡队列、N1/N2/N3 与共享冷却、隐私预览及通知开关。
- 模拟离线／失败／原单重试／跨日延迟接收；本机记录时间与模拟接收时间分别保存。没有真实服务器。
- 账号／档案独立内存草稿，返回桌面／通知打断后可继续；整页刷新结束草稿会话并在离开前提示。
- 首页通用助手与加药助手共用聊天、配置卡、手动草稿和最终核对；支持实际文字及先预览再发送的示例语音。
- 助手可查询已有安排、采集计划与三类测量；本人声明、延后、补记／更正及子女模拟提醒继续走同一规则，不因输入方式改变权限。

三系统切换已启用，主题与账号独立；减少动态/透明效果均有回退。AI语音输入为明确标记的示例输入，未接真实麦克风、语音识别或模型。完成状态、证据与未执行的真实服务/读屏软件验证以[验证总览](output/验收/验证总览.md)为准。

### 明确未接入

- 微信登录、真实身份认证、跨设备或云端同步。
- 真实药品 OCR、药品数据库核验、相互作用判断或用药建议。
- 真实推送、短信、电话、SOS、医生/社区端或在线问诊。
- 血压、血脂等指标的诊断、风险预测、阈值预警或处方调整。
- 血压计、手环、华为健康、Apple Health 等真实设备连接。

所有健康数据必须视为演示数据。涉及医疗相关文案时，应保留“仅供演示、不提供医疗建议、不用于真实用药决策”等安全说明。

## 核心数据与状态规则

- `app.js` 中的 `DAY` 固定为 `2026-09-13`，不要改成系统当天时间，否则比赛演示数据和逾期状态会不稳定。
- `data` 是运行时共享状态；`seed-data.js` 中的 `window.SILVER_SEED_DATA` 是默认数据源。
- `localStorage` 的键为 `silver-health-data-v1`，用于保存共享家庭数据；`sessionStorage` 的键为 `silver-health-view-v1`，用于保存当前账号、页面、筛选项和历史日期。
- 修改数据必须通过 `commit(change)`，由统一逻辑完成深拷贝、`validData` 校验和本地保存。不要在点击处理器中直接改变全局 `data` 后绕过校验。
- 存档键保留原名，数据结构已升级为 version 2。`rules.js` 提供 v1 迁移，旧枚举与来源保存在 legacy 中，不将旧餐后默认值、记录时间解释为实际服药细节。无效原存档保留不覆盖。
- 演示日期通过 `dateOffset`（0–31）受控偏移，完整时刻统一 `+08:00`。声明、更正、撤销追加版本，不回滚延后次数或家属共享冷却。
- 通知评估由应用的 `commit()` 调用规则协调器，渲染不产生通知。外层只通过 `yaoanxin-demo` v1 同源消息操作；iframe 保持挂载，隐藏时 inert。重播不写业务状态，重试复用原通知 ID。
- 聊天草稿以账号/档案为键；表单引用会话草稿并即时同步，恢复时不得让旧弹窗副本覆盖新修改。延迟文字处理在换账号、档案、页面或重置后取消，不迁移到新对象执行。
- “早晚”、模糊否定、未知药名及次数冲突必须澄清；分时不同剂量、非每日方案保持阻止状态，不能靠打开手动页清除。示例/文字/手动来源经过收集、核对和保存始终保留。
- 新增或调整数据字段时，同步检查：`seed-data.js`、`validData()`、创建表单、确认页、渲染页、恢复初始数据逻辑和相关文案。
- `doseEvents` 必须关联存在的 `medicationPlan`，且计划必须属于同一个 `profileId`。
- 子女可以查看已绑定长辈的数据并协助录入，但不能替长辈确认“已服用”。提醒记录只能表示“模拟提醒”，不能改变服药事实。
- 任何用户输入进入 HTML 前都要经过已有的 `esc()`；不要直接拼接未经转义的名称、备注、文件名或记录内容。
- 状态显示使用已有的 `STATUS`、`stateOf()` 和 `badge()`，不要在单个页面另造状态判断。

## 修改规范

- 优先沿用现有的函数式渲染和事件分发模式。除非需求确实需要，不要引入组件框架、模板引擎或新的状态管理层。
- `app.js` 的业务修改应集中在明确的 action、表单收集和 `commit()` 路径中；弹窗内容使用 `modalContent()` 及对应表单函数维护。
- `seed-data.js` 只放默认演示数据，不要把运行时生成数据写回该文件。
- 样式优先使用 `styles.css` 现有变量、卡片、按钮、字号模式和状态类。保持手机优先布局，桌面端维持居中的小程序预览感。
- 使用现有 SVG symbol 图标和 `icon()` helper。新增图标时先在 `index.html` 的图标精灵中添加 symbol，再在代码中引用。
- 保持中文界面文案与现有语气一致，避免增加营销式说明、医疗承诺或超出 MVP 的功能入口。
- 注释应说明不明显的业务约束，例如固定演示日、权限边界或本地存储降级；不要添加逐行翻译式注释。
- 编辑前先检查工作区是否有用户未提交改动。不要覆盖或回滚与当前任务无关的改动。

## 交互与可访问性要求

- 交互控件优先使用 `<button>`、`<label>`、`<input>`、`<select>` 等语义元素，不要用带点击事件的普通 `<div>` 代替按钮。
- 保留现有的 `aria-label`、`aria-live`、`role`、键盘焦点恢复和弹窗 `inert` 行为。新增弹窗或图标按钮时补齐可访问名称。
- 长辈端继续突出当前时段和大按钮；不要把关键操作隐藏在复杂菜单中。
- 检查标准字号和大字号两种模式下的文字溢出、按钮高度、卡片布局和底部导航遮挡。
- 当前范围不包含拍照／扫码或文件上传；不要重新加入已取消入口。

## 验证要求

每次涉及交互、数据或样式的修改，至少完成以下检查：

1. 用浏览器打开或启动本地 HTTP 服务，确认首页能够渲染且控制台无 JavaScript 错误。
2. 验证两个演示账号可以切换，张阿姨与小李看到的是同一家庭数据。
3. 验证一次服药打卡、一次跳过、一次模拟提醒或新增记录，并刷新页面确认本地存储行为符合预期。
4. 验证长辈端与子女端的权限边界，特别是子女不能代替长辈完成服药打卡。
5. 涉及布局时，至少检查窄屏移动视口和桌面视口；若使用 Playwright，截图可放入 `output/playwright/`，不要把临时缓存或调试文件放到项目根目录。
6. 修改数据结构时，额外验证恢复初始演示数据、无效存档降级和空数据状态。

没有自动化测试框架时，必须在最终说明中明确写出已执行的手动验证，以及未执行的检查。

## 提交前检查清单

- 没有引入未声明的外部依赖或网络请求。
- 没有把模拟功能写成真实服务或医疗建议。
- 新数据都经过 `validData()` 校验并能被恢复初始数据覆盖。
- 新增文字和用户输入经过 HTML 转义，未引入明显 XSS 风险。
- 移动端、桌面端、标准字号、大字号均无明显溢出或遮挡。
- 关键流程仍然是：添加/确认计划 → 生成任务 → 长辈打卡 → 双端共享 → 子女模拟提醒。
- `AGENTS.md`、产品文档和代码之间没有互相矛盾的实现描述。
